module sq01
fn mix(a: i64, b: i64) -> i64 { return a * 3 + b - 1 }
fn main() -> i64 {
    let mut acc : i64 = 0
    let mut i : i64 = 1
    while i < 10 {
        acc = acc + mix(i, i * 2)
        i = i + 1
    }
    if acc > 200 { return 61 }
    return 60
}
