/* sq05 -- nested + recursive calls (funcidx order, arg lowering). */
module sq05
fn fib(n: i64) -> i64 {
    if n < 2 { return n }
    return fib(n - 1) + fib(n - 2)
}
fn g(x: i64) -> i64 { return fib(x) + fib(x - 1) }
fn main() -> i64 { return g(9) }
