# Forward References

Central index of every promise made by one part of the III convergence to be
discharged by a later part. Append-only: rows are added when a forward
reference is made and marked **discharged** (with a pointer to the audit
subsection) when the promised work completes. Never remove a row.

This index was established during the V1 Stages 0–1 maximalist pass (adapted to
the real substrate's nomenclature: `COMPILER/BOOT/` not `COMPILER/SEMA/`,
`STDLIB/scripts/build_stdlib.sh` + `run_corpus.sh`, real module names). Rows
reflect the actual open items observed in the substrate at establishment time,
not the idealized plan's hypothetical ones.

| # | Promised by | Promised artifact / fix | Discharging stage | Verification gate | Status |
|---|-------------|--------------------------|-------------------|-------------------|--------|
| 1 | §4.11 | `numera/rsa.iii` RSA-3072/4096 sign/verify (EMSA-PSS) functional + KAT | §4.11 | corpus 210 keygen→sign→verify→tamper = 99 | DISCHARGED (corpus 373_rsa_pss_sign_verify=99: full keygen->sign->verify->tamper green at RSA-320/sLen=0 + RSA-576/sLen=32; rsa_pss_verify gained an sLen param; DOCS/CRASH-AUDIT.md sec.6) |
| 2 | §4.11 debug | Value-dependent fault in `numera/bigint.iii` mul/div over long modexp chains (collapses to len-0); RSA worked around via own Montgomery modexp | Stage 3 (compiler/stdlib bug fixes) | bigint modexp identity holds for full-size base+exponent; corpus asserts | DISCHARGED (STALE: bigint_mul/div/modpow proven correct -- probe1 (2^512-1)^2 known-answer, probe2 M521 521-bit Fermat chain + 39-seed scalar/avx512 differential; the rm_* workaround is RETAINED for performance, not correctness; DOCS/CRASH-AUDIT.md sec.6) |
| 3 | §4.11.3 | Wire ECDSA-P256/P384 + RSA-3072/4096 into `CRYPTO-AGILITY/src/crypto.c` (replace BAD_SUITE stubs) | §4.11.3 | `iii_crypto_test` exercises all 4 suites | DISCHARGED (CRYPTO-AGILITY/tests/test_crypto.c = 147 passed/0 failed: ECDSA-P256, ECDSA-P384, RSA-3072, RSA-4096 each full keygen->sign->verify-accept->tamper-reject. crypto.c dispatch bridges single-source to the proven .iii crypto via libiii_native.a; byte-ABI added to ecdsa_p256/p384/rsa.iii. Deterministic nonces: ECDSA via SP800-90A HMAC-DRBG seeded (d,z); RSA-PSS salt=0/sLen=32. Build recipe: CRYPTO-AGILITY/build_crypto_test.sh -> build/iii_crypto_test.exe. Corpus 293/0 no regression; 0 bridge syms in iiis-2 (no compiler drift)) |
| 4 | §4.12 | BLS12-381 replaces the ZK-PRUNING toy curve (p≈2^30) with real 381-bit Fp/Fr/G1/G2/pairing | §4.12 | pairing-correctness + Groth16 prove/verify KAT | pending |
| 5 | §4.13 | STARK boundary openings (open trace[0]/trace[N-1] with Merkle proofs) | §4.13 | tampered boundary rejected, honest accepted | pending |
| 6 | §4.14 | Port ZK-PRUNING (field/curve/snark/stark/prune) to `STDLIB/iii/numera/zk_*.iii` | §4.14 | KAT vectors + rollup corpus pass | pending |
| 7 | §4.15 | SHA-256 dedup: enforce byte-identity across the 6+ hand-rolled copies at build | §4.15 | build-time md5 cross-check fails on divergence | pending |
| 8 | Stage 1 | Hexad compose-rule reconciliation across `HEXAD/src/hexad_algebra.c`, `COMPILER/BOOT/hexad_check.c`, `TYPES/src/hexad.c` (see HEXAD_COMPOSE_AUTHORITY.md) | Stage 8.1 | Cartesian-product corpus agrees byte-for-byte | pending |
| 9 | Stage 1 | IRPD method-table unification between `COMPILER/BOOT/sid.c` and `sema.c` (see IRPD_METHOD_AUTHORITY.md) | Stage 8.3 | both files consume one canonical header | pending |
| 10 | Stage 1 | `DOCS/MATH_LIBRARY_QUEUE.md` populated with V1 theorem queue | Stages 2,4,6,8 | each stage appends per its discharging tactic | pending |
| 11 | Stage 1 | `MATH_LIBRARY_QUEUE.md` sealed for V2 consumption | Stage 10 | seal signature verifies vs Founders-Anchor | pending |
| 12 | Stage 5 | HotStuff BFT consensus — **spec written** (`DOCS/III-CONSENSUS.md` ✓); `aether/hotstuff.iii` + `quorum_tier.iii` + `hotstuff_predict.iii` + `hotstuff_heal.iii` impl still pending | Stage 5 | 4-node + Byzantine + leader-failure corpus | spec done, impl pending |
| 13 | Stage 6 | XII Ω1..Ω12 ceremony + real Founders-Anchor keypair (replace 0xCC placeholder) | Stage 6 / Stage 10 | `XII_R1.mhash` ≠ 0; anchor sig verifies | pending |
| 14 | Stage 10 | Founders-Anchor genesis ceremony (air-gapped seed → pubkey) | Stage 10 | pubkey committed, seed sealed off-device | pending |
| 15 | Stage 4-5 plan | Differential-witness dedup (`aether/witness_differential.iii`) against the V3 memoization lattice (content-addressed lookup replaces absolute diff payload) | V3 Phase 12 | dedup'd diff payload byte-equals hand-computed expected | pending |
| 16 | Stage 4-5 plan | Content-addressing + multiset-hash primitives (`content_addr`, `keccak256_multiset`) for V3 content-addressed memoization | V3 | permutation-invariance + length-prefix corpus | pending (deferred — no consumer until V3; avoids speculative bloat) |
| 17 | Stage 2-3 plan | Compiler annotation surface: recognize `@pure`/`@bijective`/`@effect` (table-driven, dup-tag rejected) in real `COMPILER/BOOT/` lex/parse/ast (`.c`, not the plan's `.iii`) | Stage 2 (real compiler) | annotation recognized + AST bit set + duplicate → error; theorem TH_ANNOTATION_ARITY_FIXED | pending |
| 18 | Stage 2-3 plan | Effect inference: 4-bit bitmap READ=0/WRITE=1/ALLOC=2/EMIT=3 (extends to 7-bit at Stage 8: +CAP_PROMOTE=4/RING_TRANSIT=5/REFLECT=6), wired into witness-fragment header (reserved byte) | Stage 2 / Stage 8 | byte-equal classification per family; `@pure`→empty-write theorem (TH_PURITY_EMPTY_WRITE) | pending |
| 19 | Stage 2-3 plan | Capability-flow codegen check: kind-match precursor at each `@cap` call site (CAP_KIND_FORGE/QUARANTINE/RING_PROMOTE/RING_DEMOTE/WITNESS_EMIT/FEDERATION_ADMIT/OPERATOR); scope+freshness deferred to V2 Phase 2 | Stage 2 / V2 P2 | kind-mismatch → reject; non-cap arg unchecked | pending |
| 20 | Stage 2-3 plan | BCWL audit counts **runtime-visible ops only** (LOAD/STORE/CALL/EMIT/ALLOC/ASSIGN/RETURN/ARITH/COMPARE/JUMP), excludes compile-time kinds (CONST_FOLD/TYPE_CHECK/ANNOTATION_PARSE/CAP_BIND); ≤256 bound — **fixes the BCWL complexity overstatement** | Stage 3 | count == runtime-op count on a known fn; ≤256 (TH_BCWL_BOUNDED) | pending |
| 21 | Stage 2-3 plan | Remove dead capability-check in the Ring R-1 LOW-dispatch codegen path (copy-paste residue from the R-2 path; the R-1 signature takes no `cap`) — compiler bug fix | Stage 3 | emitted-instruction count drops, behavior unchanged; requires careful C0 read of the real `COMPILER/BOOT/` cg path first | pending |
| 22 | Stage 6-7 plan | XII confluence — **already ACHIEVED** by the real 44-rule engine (`omnia/xii_rewrite.iii` R001–R044) + critical-pairs corpus (`omnia/xii_critpairs.iii`) + Knuth-Bendix completion (R041–R044, CP-212/222/230/266/286 re-enabled; `DOCS/XII-CONFLUENCE-COMPLETION.md`); spec/status at `DOCS/XII_CONFLUENCE_SPECIFICATION.md`. Residual: admit the 44 confluence theorems to the math-queue | #10 (queue admission) | XII critpairs corpus joins every enabled pair byte-equal; theorems admitted once #10 lands | impl done, theorem-admission pending #10 |
| 23 | Stage 6-7 plan | Dependent specialisation — **already IMPLEMENTED**: `@specialize_on_use` recognized by `COMPILER/BOOT/` + live in 10+ modules (handle/region/span/bigint_div/q128/scalar/sha256/iter/map/option); spec/status at `DOCS/SPECIALIZATION_SPECIFICATION.md`. Residual: admit equivalence theorems to the math-queue | #10 (queue admission) | per-module corpus asserts specialised==generic byte-equal (green today); theorems admitted once #10 lands | impl done, theorem-admission pending #10 |
| 24 | Stage 6-7 plan | Specialisation cache-invalidation: V2 Phase 2 ratifies `cp_specialization_cache` so a specialisation entry invalidates when its source function's constitutional clause is amended | V2 Phase 2 | synthetic-amendment corpus clears the matching cache entry's validity bit | pending (no constitutional surface pre-V2) |
| 25 | Stage 8-9 plan | Type-system deepening of the real `TYPES/src/cic.c` CIC kernel (Prop-irrelevance, universe polymorphism, general guarded fixpoint, dependent MATCH, coinduction) + proof inheritance + proof cache + 4-tactic library; spec at `DOCS/TYPE_SYSTEM_SPECIFICATION.md` | Stage 8 | `TYPES/build/iii_types_test.exe` accepts the 6 canonical inductives byte-exact; tactic dispatch + proof-cache keys verified | spec done, impl pending |
| 26 | Stage 8-9 plan | Witness-ephemeral module (`we_init`/`we_mark_ephemeral`/`we_recompute_verify`/`we_collect_recomputable`) against `sanctus/witness.iii`; spec at `DOCS/WITNESS_EPHEMERAL_SPECIFICATION.md` | Stage 8 | recomputed fragment master-hash == original; chain master-hash byte-equal across elide/non-elide | spec done, impl pending |
| 27 | Stage 8-9 plan | Effect-inference completion to 7 bits (+CAP_PROMOTE=4/RING_TRANSIT=5/REFLECT=6 on the #18 4-bit base), wired into the witness-fragment header reserved byte | Stage 8 (with #25) | `@effect cap_promote`→bit 4 set; header byte == classification on a known fn set | spec done, impl pending (extends #18) |
| 28 | Stage 8-9 plan | Subsystem test gate — **IMPLEMENTED**: `STDLIB/scripts/subsystem_test_gate.sh` runs `run_all_corpora.sh` + every `iii_*_test.exe` and exits 0 iff all green; spec at `DOCS/SUBSYSTEM_TEST_GATE_SPECIFICATION.md` | Stage 9 | gate exits 0 iff corpora FAIL=0 + every subsystem exe exits 0; non-zero names the failure | impl done (script runnable); thresholds recorded at first green run |
| 29 | Stage 8-9 plan | III ISA first canonical opcode set (6-bit opcode / 4-bit Ring / 22-bit operand per `DOCS/III_ISA_ROADMAP.md` + `DOCS/HARDWARE/I-INSTR-V1.0-spec.md`); V1 runs on the `COMPILER/BOOT/` C kernel until then | V2 Phase 3 | byte-equal output across C-kernel path and ISA path; resolver-unit 12-test equivalence (RTL/asm/sim) | pending (V2) |
