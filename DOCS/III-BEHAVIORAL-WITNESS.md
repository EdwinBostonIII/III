# THE BEHAVIORAL WITNESS — the trait seat pointed at the digested R1, through the wall

*Status: LANDED + GATED (`STDLIB/scripts/ethos_r1_gate.sh`, exit 0, byte-deterministic;
pure law with no Feast, real 671B walk when the Feast is present). Verified 2026-07-21 on
the resealed compiler (`ec0ad523`).*

## The question this closes

The installation thread (session `b4af7e2d`) drew a hard boundary: a trait claimed *into*
a model's weights — "elevated baseline cortisol", "D4 polymorphism" — is **a claim with no
referent and no witness**. METABOLE translates GGUF *numbers* losslessly because every
quantized weight has an exact referent; a *disposition* has none. What can be witnessed is
**behavior under law**.

THE TRAIT WITNESS (`ethos.iii`, commit `88956ad6`) answered the written form: the profile's
verdict-map is *derived* from III's own live primitives, assayed by DOKIMASIA (the three
reject-traits are the known-bad mutants), and judged — while the prose-only profile is
REFUSED. **Written is not installed.**

This closes the deeper case by swapping the answerer from *III's own law* to **the model
itself**. The measured fact is R1's real head order, walked live over the Feast; the same
machinery renders the verdict. The result:

> **MEASURED IS NOT INSTALLED.** A real, exact, two-engine-confirmed disposition of the
> model, sealed in the logos, is *still* refused canonical standing — because a measurement
> through the oracle is PROVISIONAL by type. No probe, however exact, installs a trait.

Symmetric to "written is not installed", now computed against the real 671B mind.

## What the probe composes (all parts separately gated)

`STDLIB/build/mantis/ethos_r1_probe.iii` — every clause a computed verdict, not an assertion:

| Clause | Machinery | The check |
|---|---|---|
| **the order, both ways** | probole two-engine krisis (`pb_head_order_trusted`), real 7168-dim head | `trusted(24792,1925)=+1`, `trusted(1925,24792)=−1` over 7165 live dims |
| **anti-commutation** | — | a real order flips with the frame; a frame-insensitive answerer is CAUGHT |
| **the assay** | DOKIMASIA (`ont_image_ext`/`ont_admissible`) | real map `{1,0}` → image 2 → **HEARD**; constant forgery `{1,1}` → image 1 → **REFUSED** |
| **the seal** | EIDOLOS `<`-claim (`man_seal_order`) | `[k…0785 < k…60d8]`, addr `5743271528433377647` = the pure-gate address |
| **the wall** | reach_oracle DEFAULT-DENY (ETHOS Law 7) | oracle-tier map → **REFUSED canonical**; witnessed tier → admitted |
| **the judgment** | PRAXIS over the pinned chain | `[utterance < exact_order]` STANDS; `[profile < behavior]` **REFUSED** |

The engines that *produce* the order are gated in `summit_gate` (probole arms 31–38: two
engines agree, a forged engine is caught). This probe adds the **composition** — assay +
anti-commutation + seal + wall + judgment over the order pair — and proves it two ways.

## The two modes (the gate runs both)

- **THE PURE LAW** (`ethos_r1_selfprove`, no Feast): the composition proven on *given* signs,
  with a mandatory negative — a non-anti-commuting answerer (the `+1/+1` constant-forgery
  shape) is caught, and the constant map is refused by DOKIMASIA. This is the gate's
  clean-checkout teeth; it needs no 377 GB model.
- **THE REAL WALK** (Feast present): the live forward pass decides R1's real head order,
  and the whole verdict is rendered against the model. Feast-gated in `summit_gate`'s
  fail-open idiom.

## Why it is not a tautology

The one lens that had to bite: *a co-designed probe measures nothing*. The escape is that
**R1's order is a fact this code does not control** — token 24792 outranks 1925 by two
independent exact engines over 7165 live dims, zero width; I do not get to choose it. The
wall's PROVISIONAL refusal is a *fixed* law, not tuned to R1. And DOKIMASIA gives it teeth:
a forged constant "measurement" is refused as a rubber stamp. The claim rendered is the
*opposite* of an overclaim — not "R1 has trait T" (unwitnessable), but "even this real
measurement does not install a trait." The profile lives in the LAW (`ethos_gate` GREEN),
never in the weights.

## Reproduce

```
bash STDLIB/scripts/ethos_r1_gate.sh          # pure law always; real walk if Feast/ present
```

Exit 0 = the law stands (byte-deterministic), and — with the Feast on the table — the real
model bears the verdict. Kill-switch honored: if a fresh walk did not yield a *decidable*
anti-commuting order (a tie, engine disagreement, or a Feast error), the probe refuses and
names it rather than forcing a verdict R1's behavior does not support.
