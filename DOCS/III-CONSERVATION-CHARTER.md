# Ξ-Σ THE CONSERVATION TIER — EXECUTED 2026-07-17 (gate 2762 = 99)

## WHAT LANDED (unified into existing organs — NO island; conserve.iii was
## drafted then DELETED once the parallel session's aether/reversibility.iii
## was found, and its unique value folded into the right homes):
##  - aether/reversibility.iii (the WORD hunter, parallel-session organ) GAINED
##    rv_mul_inverse / rv_mul_deficit / rv_mul_roundtrip: III's Newton inverse
##    for the NONLINEAR MUL-odd bijections its GF(2) matrix probe cannot invert
##    (rv_probe returns 0 for nonlinear) — closing that organ's own named gap.
##    VALIDATED: c·cinv==1 in-organ, round-trip over battery+walks+LCG, and the
##    numera/bv_ring polynomial-identity proof over ALL 2^64 (<1s; the bit-blast
##    miter TIMES OUT at 300s on double 64x64-MUL — MEASURED, so ring is the
##    engine). xorshift inverse is LINEAR/GF(2) → already inverted+proven by the
##    organ's Gaussian elimination; independently proven here via the doubling
##    unshift cascade (iii-prove, 2s) — a bug in the naive cascade (addition vs
##    doubling of the shift, R^51+R^51=0 silent-cancel) was CAUGHT by proving
##    before sealing, never reaching an organ.
##  - aether/xeno_ontogenesis.iii (the FAIR magma universe) GAINED the
##    CONSERVATION LENS: xo_quasigroup + xo_reversible_census — reversible
##    ONTOLOGY discovery (Latin squares) riding the anti-rigging fairness law.
##    Census by exhaustion reproduces 1,2,12 (n=1,2,3); OEIS A002860 pre-clause.
##  - gate 2762_conservation.iii (standing-owned; links the leaf organs) proves
##    both sides stored-answer-free + the negative arm; PASSES 99.
## The substrate `conserve` census verb + parliament chamber III (seal a
## conservation LEDGER as law consumed by the intent Oracle) remain NAMED
## near-rungs (they ride the next covenant). Uncomputation-aware codegen is the
## far rung — charter it, do not fake it.

# --- ORIGINAL CHARTER (design record) ---


## The being (transform-ontological)
III lacks a name for what rva/billiard/intent/NOMOS-involutions/friction each touch:
INFORMATION CONSERVATION. An operation either preserves information (bijective —
provably invertible, zero-erasure, oracle-recoverable, uncomputation-capable) or
destroys it (a measurable Landauer deficit). The missing organ is THE HUNTER that
decides this autonomously for every operation III owns — and the union with the
FAIR ONTOLOGY STREAM so generated algebras are selected FOR reversibility.

## THE JUDGE TRIAD (truth roads split by algebra — VALIDATED 2026-07-17, measured)
- **iii-prove** (bit-blast UNSAT): the engine for BIT-MIXING bijections — xorshift
  unshift chains, xor-k, not, neg prove instantly (the GLOSSA ladder measurements).
  MEASURED BOUNDARY: double 64x64-MUL miters TIME OUT (g(f(x))==x for f=MUL-c,
  g=MUL-cinv: rc=124 at 300s) — never send ring-polynomial facts here.
- **bv_ring** (numera/bv_ring.iii, the polynomial judge over Z/2^64, {var,const,
  +,-,*,<<}): certifies the SAME mul-odd inverse in <1s as a polynomial identity
  (sound: poly-equal ⇒ pointwise-equal for ALL u64), and REFUSES a wrong inverse
  (teeth witnessed rc-path).  The engine for MUL/ADD/SHL-polynomial bijections;
  conservative by design (may decline, never blesses unsoundly).
- **rva/smt.iii** (live SMT session): the RUNTIME auditor (UNSAT = reversible,
  witness published, counterexample on SAT) — already standing.
One fact, independent engines wherever domains overlap; SENTINEL on split.
The hunter's inverse-synthesis is III's own (Newton iteration VALIDATED in-organ:
c=13787848793156543929 → cinv=10871156337175269513, c·cinv==1 verified, then
ring-certified over all 2^64).

## THE HUNTER (substrate_cli verb `conserve <fuel> <outdir>`; organ logic joins
## reversibility_audit.iii — no islands)
Candidate stream (global pre-stated criteria, every disposition counted):
 [a] ledger unary atoms (NOT NEG BLSI BLSMSK BLSR POPCNT LZCNT TZCNT)
 [b] binary atoms sliced at fixed y over the probe constants (XOR-k ADD-k SUB-k
     MUL-c SHL-k SHR-k ANDN-k ...)
 [c] compositions to size <= fuel-bounded N over the proven-bijective set
INVERSE SYNTHESIS (the invention seat — all III-computed):
 - NOT/NEG: self-inverse (the NOMOS involution core, cross-cited)
 - XOR-k: self-inverse slice; ADD-k <-> SUB-k; ROT-k <-> ROT-(64-k) [if rot enters]
 - MUL-c (odd c): c^{-1} mod 2^64 by Newton iteration y <- y*(2-c*y), 6 steps from
   y0=c (or y0 = (3*c)^1 trick); even c: REFUSED with fiber census (deficit >= tz(c))
 - xorshift x^(x>>k) / x^(x<<k): closed unshift cascade (k, 2k, 4k, ... < 64)
 - compositions: reverse-composed synthesized inverses
VERDICTS: REVERSIBLE (inverse synthesized + SMT UNSAT + pressed for iii-prove) |
 DESTROYS (fiber census over exhaustive 2^16 slices + battery/LCG sampling at 64-bit:
 deficit = 64 - log2(|image|) MEASURED — named measured-not-proven) | REFUSED (named:
 no synthesis rule; capacity). Landauer leg ties to friction: erased bits vs measured
 cost — the thermodynamic ledger the user's arc named.

## THE XENO UNION (the novel-ontology mandate)
xeno_ontogenesis's fair bijective magma stream gains THE CONSERVATION LENS:
 - per enumerated magma: quasigroup test (every row+column a permutation — total,
   n<=XO_NMAX) = FULLY REVERSIBLE ontology; census over the fair universe
 - law-stream correlation: which enumerated laws co-occur with reversibility
   (machine-derived structure theory under the fairness law; ASSOC ∧ quasigroup ⇒
   group re-derived as a SWEPT theorem, rediscovery-as-proving-ground)
 - THE DIE BRIDGE: u64 REALIZATIONS — the hunter's proven-bijective word-formulas
   (xor/rot/mul-odd/xorshift chains) are the die's OWN reversible algebra;
   TAXIS-ordered by measured friction; the sovereign object = the die-native
   reversible instruction algebra with measured order structure (element orders,
   cycle invariants — computed by III organs, prover-anchored where closed)
GRADING: schema charted (group/quasigroup theory named as such); CONTENT sovereign
 (this die's friction-ordered bijection algebra + the fair-universe conservation
 census); NOVEL reserved for what survives the standing adjudication (OEIS
 pre-clause mandatory on any census claim).

## Seal + law
Parliament chamber III (katabasis consent organ extension `katabasis_conserve_consent`,
same 5-voter shape: self-agreement / truth / teeth / no-collision-with-standing-law /
continuity). Sealed artifact: the CONSERVATION LEDGER (machine-written module: op-id,
verdict, inverse-ser or deficit) consumed by the intent Oracle of Rejection (grounded
recoverability) + the standing arm + gate 276x (stored-answer-free: re-hunt, re-prove
spot rows, negative arm = corrupted inverse REFUTED). Uncomputation-aware codegen is
the NAMED far rung (charter it, do not fake it).

## Nonlinear finds mandate (task #11)
MUL-odd inverses, xorshift chains, BLSI-composition fibers — every one computed by
III's own organs (Newton in-organ; unshift press; fiber census), proven by III's own
engines. No Claude-side eyeballing anywhere in the road.
