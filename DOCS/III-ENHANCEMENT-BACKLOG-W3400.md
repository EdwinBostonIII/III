# III Enhancement Backlog (W3400 ultracode discovery, wf_b17b2ec5)

63 candidates discovered, 36 verified REAL (completeness-accepting rubric, adversarial verify). 18 implement-now (safe, additive, composes existing organs, single in-session gated unit) + 18 dedicated-wave (compiler self-host / bigger crypto -- per feedback_triage_selfhost_latent_separate, the cg_r3/cg_r0/sema/ast items are latent self-host edits = careful bootstrap+seal-gated effort).

## Implement-now
### 1. [STDLIB/iii/aether/develop_up.iii] du_encapsulate / du_write / du_health  (sev 3, conf 0.93, capability-gap)
- **Current:** The capstone gateway admits a compute box WITH a CPU budget -- du_encapsulate passes cpu_cap straight into cb_admit (L69, cb_admit(guest_cap,0,mem_cap,cpu_cap)) -- but the gateway then meters only memory. du_write charges cb_charge_mem (L91) and never CPU; du_health (L157-161) reports headroom from cb_mem_remaining (L159) alone; there is no du_charge_cpu / du_run export (export list L64-160). compute_box already implements and exposes the CPU dimension -- cb_charge_cpu (compute_box.iii:60), cb_cpu_remaining (:67), cb_run (:71) -- and corpus 1581 exercises all three directly, proving the underlying capability works.
- **Proposed:** Wire the CPU half of the availability forcefield through the gateway: add du_charge_cpu(guest_cap, micros) -> cb_charge_cpu and du_run(guest_cap, kind, ctx) -> cb_run(guest_cap, DU_CMP_ID, kind, ctx) (refused at zero quota), and fold cb_cpu_remaining into du_health so a box with exhausted CPU but spare memory reports unhealthy.
- **Impl path:** Compose the already-exported compute_box CPU surface (cb_charge_cpu/cb_cpu_remaining/cb_run) exactly as du_write composes cb_charge_mem; the box id DU_CMP_ID is already held (develop_up.iii:58,72). Extend corpus 1585 to charge CPU and assert du_health flips to 0 on CPU exhaustion (mirror corpus 1581's cb_charge_cpu assertions).
- **Regression:** Low risk, fully traced. du_health is consumed only by corpus 1585 (L71 pre-open->0; L83 post-open-with-headroom->1). Adding `if cb_cpu_remaining(DU_CMP_ID)==0 {return 0}` cannot break either: 1585 charges no CPU (cpu_cap=100000, only 64x1-byte mem writes), so cb_cpu_remaining stays non-zero through every existing assertion; du_encapsulate itself charges no CPU against the box. Production-hardening corpus 1597/1599/1600 reference develop_up but never call du_health and never charge CPU (all cpu_cap=100000, untouched). The new du_charge_cpu/du_run are additive exports called by no existing test, so they cannot regress current behavior. Only behavior change is the du_health tightening, which is gated by 1585 and updated in the same change.

### 2. [STDLIB/iii/sanctus/observe.iii] obs_observe (REPLAY branch, lines 90-116) + obs_commit_of (119-122)  (sev 3, conf 0.88, incomplete-hardening)
- **Current:** In LIVE mode obs_observe computes the observed value's content-address with cad_oneshot into OBS_COMMIT (line 104) and publishes it as the fragment's out_commit (line 110); obs_commit_of (119-122) exposes that recorded commit. In REPLAY mode (91-99) it re-injects the recorded payload via wh_get_payload and validates ONLY the length (`OBS_OUTLEN[0] != len`, line 96). The `source_id` parameter is wholly ignored on the replay path, and the recorded out_commit is never recomputed/compared against the re-injected bytes.
- **Proposed:** On the replay branch, after wh_get_payload: (a) recompute cad_oneshot(OBS_SUITE_KECCAK,...) over the re-injected bytes and compare to wh_get_out_commit(idx) (already externed at line 23), returning OBS_SENTINEL on mismatch; (b) fetch wh_get_producer(idx) and confirm its low 4 bytes equal source_id, so a divergent replay sequence (wrong source or wrong call order) is rejected rather than silently returning the cursor's stale value. This makes the declared @safety(UNREPLAYABLE) bricking class (docstring 11-13) and the bit-identical-replay contract (7-9) actually enforced.
- **Impl path:** Compose existing organs already externed in this file: cad_oneshot (line 28) for the re-hash, wh_get_out_commit (23) for the recorded commit, plus wh_get_producer / wh_is_redacted from witness_hook.iii. The divergence detector in aether/replay_box.iii (rb_replay_check) sits on top of this and would inherit the stronger guarantee; preserve corpus 1100 (=99) which replays with matching source_id=7.
- **Regression:** Traced all 4 consumers (corpus 1100, 1580, 1583, 1600 — the only callers of obs_observe/rb_*). Every one records and replays with the SAME source_id (1100/1580: 7/SID both ways; 1583: df_ingress forwards source_id unchanged to rb_record/obs_observe, SID both ways and provisional-record path is LIVE so no producer check; 1600: du_replay forwards source_id=0 unchanged to rb_replay_check). For all faithful replays the re-injected bytes equal the recorded payload, so cad(re-injected)==recorded out_commit and producer low-4-bytes==source_id — green paths unchanged. 1580(A) divergence detection recomputes over the RE-INJECTED recorded bytes (out_ptr), not the live decoy, so rb_replay_check's DIVERGED verdict is untouched. No test pins a fail-open/redaction-tolerant replay. Low residual risk only on check (b)'s new positional source_id-match contract, but it does not regress any existing test.

### 3. [STDLIB/iii/numera/ec256.iii] ec256_set_affine (line 174) consumed by iii_ecdsa_p256_verify (ecdsa_p256.iii:153)  (sev 3, conf 0.86, incomplete-hardening)
- **Current:** ec256_set_affine just loads the supplied (x,y) into Montgomery slots and sets Z=mont(1) (lines 174-180); there is NO check that the point satisfies y^2 = x^3 - 3x + b, that it is non-identity, or in the prime-order subgroup. iii_ecdsa_p256_verify (ecdsa_p256.iii:153-180) calls ec256_set_affine(33, qx, qy) and uses Q directly; it validates r,s ranges (sig_range_ok) but never validates the public key. A repo-wide grep for on_curve/point_valid/pubkey_valid in numera returns no such export in ec256.iii or ec384.iii. The identical pattern holds for ec384.iii / ecdsa_p384.iii.
- **Proposed:** Add an exported ec256_is_on_curve(p) (compute y^2 and x^3-3x+b in Montgomery form via the existing fp_mul_x/fp_add_x/fp_sub_x and compare) plus an identity/zero-coordinate reject, and call it from iii_ecdsa_p256_verify (and the P-384 siblings) before scalar-mul, returning 0 (reject) for an off-curve or identity Q.
- **Impl path:** Implement ec256_is_on_curve using slots in the free temp region (e.g. 20-26): t=mont_mul(x,x), t=mul(t,x) (x^3); sub 3*x; add curve-b slot 50; compute y^2; fp_eq. Export it and insert a guard at ecdsa_p256.iii:154 after fn256_boot. Mirror in ec384.iii/ecdsa_p384.iii. Add a KAT feeding a known off-curve (qx,qy) and asserting verify rejects.
- **Regression:** Low. The only consumers of iii_ecdsa_p256_verify(_x) are corpus KATs 208/913/994 (P-256) and 209/958/959 (P-384) plus the modules themselves; every test derives Q from keygen, i.e. a legitimate on-curve public key, so an additive off-curve/identity reject cannot regress them. A wrong-reject implementation bug (e.g. mishandling Montgomery form of b or 3x) would immediately fail 208/994 (valid signature must return 1u8), so existing KATs gate the correctness of the new check. No production/non-numera consumer relies on accepting an off-curve Q. No deliberate/tested fail-open posture pins the current behavior (the low-s opt-in is a separate, documented malleability choice; on-curve validation has no interop argument for acceptance).

### 4. [STDLIB/iii/aether/develop_up.iii] du_ingress_admit  (sev 3, conf 0.85, missing-integration)
- **Current:** du_ingress_admit (L154) is `return df_admit(tier)`, and df_admit (determinism_firewall.iii:39) just forwards reach_oracle_admit_canonical -- a pure canonical/provisional verdict. The determinism_firewall's headline export df_ingress (determinism_firewall.iii:60-76), which RECORDS a provisional value through rb_record before denying it (the reach_oracle<>replay_box composition the slice exists to express, header L18-20), is never reachable through the gateway. The gateway DID open a replay log (du_encapsulate calls rb_begin, L80) and exposes du_replay (L135), so the record sink exists -- but the ingress membrane never feeds it.
- **Proposed:** Expose the full membrane: du_ingress(tier, source_id, live, len, out) -> df_ingress(...), so a provisional value crossing the box boundary is recorded into the box's replay log (and deny-from-core) in one act, completing the record(rb_begin) -> ingress-record -> du_replay loop the gateway already half-wires.
- **Impl path:** df_ingress is already implemented and corpus-proven (corpus 1583 L69-76). Add the thin du_ingress passthrough next to the other membrane lines (develop_up.iii:151-154) and extend corpus 1585's membrane block to assert a provisional value is both denied AND subsequently replay-checkable via du_replay.
- **Regression:** Low / purely additive. The only consumer of du_ingress_admit is corpus 1585 (L99-100), which asserts verdict-only ROR_OK/ROR_REFUSED; keeping du_ingress_admit preserves it. Adding du_ingress composes df_ingress (determinism_firewall.iii:60-76), already implemented and corpus-proven in 1583 (L69-78). No load-bearing path changes. One refinement the implementer must resolve: df_ingress RECORDS into the box's replay log, so du_ingress should gate on DU_OPEN like du_replay (develop_up.iii:136) rather than being an ungated passthrough next to the other three membrane lines (which are stateless verdicts) -- a coherence point, not a regression.

### 5. [STDLIB/iii/numera/sov_isa.iii] sov_register_rules Rule H (the single shift-combine instance) vs cga_shift_discover  (sev 3, conf 0.8, partial-impl)
- **Current:** cga_shift_discover (cg_autocatalyst.iii:431-446) certifies the WHOLE shift-combining grid -- shapes 1/2 over the (a,b) grid with a+b<64 and shape 3 over a, each UNSAT-proven behind the count-mask wall. But sov_isa adopts exactly ONE instance: Rule H shl(shl(?0,c1),c1)->shl(?0,c2), gated by cga_dispose_shift(2,1,1) at sov_isa.iii:442 (comment :435-441). There is no parametric registration of the certified family; cga_shift_count/cga_shift_get_shape are exported (cg_autocatalyst.iii:448-452) but unused for adoption (no a/b getters even exist).
- **Proposed:** A parametric shift-adoption that walks the certified shift registry and registers shl(shl(?0,ca),cb)->shl(?0,c_{a+b}) (and the right-shift-combine and mask-recovery shapes) for every certified (shape,a,b), using the SI_CPOW/SI_CNUM parametric symbol slots sov_isa already builds for strength adoption.
- **Impl path:** Add cga_shift_get_a/cga_shift_get_b getters (cg_autocatalyst.iii has CGA_SH_A/CGA_SH_B arrays at :351-352), then in a sov_isa adopt loop register one rule per certified triple via eg_register_rule with the parametric slots (si_slot_pow/si_slot_cnum, sov_isa.iii:603-604).
- **Regression:** Low, and the one-instance hardwiring is incompleteness not deliberate design. (1) sov_isa is an egraph (eg_saturate + min-cost eg_extract): adding more proven-equal shift-combine equalities cannot make it unsound -- non-confluence is harmless, extraction picks min-cost. (2) eg_saturate(16u32) is step-bounded so extra rules cannot cause non-termination. (3) Clinching evidence is the sibling registry: the 0xB6 strength family (mul->shl) got the FULL parametric treatment via sov_isa_adopt_dream (sov_isa.iii:587, corpus 1370) -- "walk the certified registry, adopt the whole family" is the team's own pattern. The 0xB7 shift family adopting exactly ONE instance (Rule H, :442) IS the asymmetry/gap. No corpus test pins "exactly one shift rule": 1358_shift_laws pins the registry sweep (78 laws) and 1362_lattice_cited_combine pins Rule H firing via sov_isa_shiftcombine_kat (:750) -- an ADDITIVE operator-invoked entry point regresses neither; existing rules() set, Rule H, and all KATs stay byte-identical. sov_isa is NOT in the self-host codegen path (grep: referenced by sovereign_optimizer/daemon_dream/sov_pipeline + corpus, NOT cg_r3/lex/parse/sema), so no bootstrap exposure; reseal corpus+golden after.

### 6. [STDLIB/iii/aether/sid_router.iii] sr_route (SR_ACT_COW_RESTORE / SR_ACT_REG_RESTORE)  (sev 3, conf 0.77, partial-impl)
- **Current:** sr_route (L59-66) returns SR_ACT_COW_RESTORE (L46) for an NPF exit and SR_ACT_REG_RESTORE (L47) for a VMMCALL exit, but sid_router implements an executor for ONLY the recorded-memory path: sr_record_and_mutate (L74) / sr_undo (L86) over REV_TAG_MEM_RESTORE_U64. No copy-on-write page-restore executor and no guest-register-file restore executor exist here -- and reversible.iii carries no matching tag either (its undo tags are only mem_restore_u8/u64, drop_bigint, release_slot, witness_revoke -- reversible.iii:58-62). So two of the four non-fail-closed reversal actions are classified but cannot be performed by any organ.
- **Proposed:** Implement the two missing executors: a CoW page-restore (record the pre-image page, restore on undo -- composing reversible with a page-granular tag) and a guest-register-file restore (snapshot/restore the captured register block), so every exit kind that sr_route promises a reversal for actually has one.
- **Impl path:** Add a REV-style page-restore tag in reversible.iii (mirroring REV_TAG_MEM_RESTORE_U64's registered undo, reversible.iii:248-252) and a register-block restore, then add sr_cow_record/sr_cow_undo and sr_regs_capture/sr_regs_restore executors keyed off sr_route's codes. Add a corpus test that performs and reverses each.
- **Regression:** Low-to-medium and bounded. sid_router additions are purely additive (new sr_cow_*/sr_regs_* fns + new SR_TAG_* consts). The reversible.iii change (new page-restore + register-restore tag, registered undo body in rev_init L248-252, plus a buffer-pool for the pre-image since the 4-u64 record cannot hold a page) is additive and preserved-byte as long as the new tag values do not collide with 1..5 (use >=6) and the existing KATs are re-run. Two real consumers traced: corpus 1584 (pins routing codes + recorded-value execution only -- NOT the absence of CoW/regs executors, so no pinned behavior regresses) and develop_up.iii:153 du_reverse_route (forwards sr_route, still returns the same codes). One latent trap to handle: a new reversible tag must also be added to rev_classify (L589-599) or it falls through to REV_TIER_UNREPRESENTABLE (wrong -- it IS reversible); rev_classify_total/verify/falsify don't probe it so they won't redden, making the inconsistency silent. Must re-run rev_selftest + rev_compromise_charter_selftest + full corpus, and add a toy-proof oracle corpus test (CoW restore really restores a mutated page; reg restore really restores a clobbered block; a no-op toy fails).

### 7. [STDLIB/iii/forcefield/ripple_journal.iii] rj_record (and the run-history surface rj_run_root/rj_verify/rj_resolve)  (sev 3, conf 0.62, missing-integration)
- **Current:** rj_record (line 53) publishes an admitted move crystal into the rn_* content-addressed store and appends its 32-byte address to an order-sealed Merkle table (rj_run_root:65, rj_verify:105, rj_resolve:86), giving permanence + sequential integrity + tamper-evidence. The module header (lines 7-8) explicitly names 'forcefield/ripple_synthesizer.iii RSY_CKPT, a 32-byte move crystal' as 'EPHEMERAL -- overwritten every pass, with no persistent record' -- the exact problem rj_record exists to fix. But ripple_synthesizer.iii rsy_run (line 103) seals the crystal via pru_move_crystal into RSY_CKPT (line 111) and never calls rj_record; the synthesizer's externs (lines 26-37) do not include ripple_journal. rj_record's only callers are its own selftest (lines 123-124) and corpus/1450.
- **Proposed:** On the RSY_ADMIT path of rsy_run (immediately after pru_move_crystal at line 111), call rj_record((&RSY_CKPT as u64) as *u8) so every admitted optimization move is permanently recorded and order-sealed into the run history, exactly as the journal header promises.
- **Impl path:** Add `extern ... fn rj_record(ckpt: *u8) -> i32 from "ripple_journal.iii"` to ripple_synthesizer.iii, call rj_init() in rsy_reset(), and call rj_record(&RSY_CKPT) on the RSY_ADMIT branch of rsy_run. Composes the already-built rn_* store (ripple.iii) the journal externs; no new proof.
- **Regression:** Traced both consumers directly. rsy_run is called ONLY by ripple_synthesizer_selftest (KAT 1091); rj_record only by its own selftest and corpus/1450 (which feeds synthetic 0xAA/0xBB, not real crystals -- so the journal has no real producer today). Wiring rj_record into the RSY_ADMIT path does NOT regress 1091: RSY_CKPT is unchanged, so the step-6 determinism check cad_eq(CK1,CK2)==1 still holds; ADMIT fires <=4 times per selftest run, well under RJ_MAX=64; and the proposed full-table guard keeps an admit from spuriously failing. No corpus test pins the separation (nothing asserts rsy_run must NOT journal), so no tested/deliberate behavior is broken. The new transitive link dependency (ripple_synthesizer -> ripple_journal -> ripple/rn_*) is satisfied by the unified libiii_native.a archive. Two caveats, not blockers: (a) editing the seal-gated LIBNATIVE synthesizer moves its compiler-unreferenced seal -- expected drift, handled by the seal-gated build; (b) putting rj_init() inside rsy_reset() wipes the run history on every reset, which is a per-run-history semantic choice (acceptable, but if cross-reset persistence is wanted, rj_init belongs in a separate init, not rsy_reset).

### 8. [STDLIB/iii/verba/csv.iii] csv_parse  (sev 3, conf 0.6, partial-impl)
- **Current:** The module header (line 6) promises 'double-quote field quoting with embedded "" -> "'. The implementation does NOT perform that unescape: the quoted-field branch (lines 105-125) records the field as a RAW offset+len span into the input (field_start..field_len, stored at lines 143-144), and the inline comment (lines 102-104) states 'we point to raw bytes and accept that "" sequences will appear as " in the spans (consumer can handle)'. So a field `"a""b"` is returned as the span `a""b`, not `a"b`. Separately, the `arena` parameter is accepted but explicitly UNUSED (lines 74-76, 'Reserved for a future arena-backed table').
- **Proposed:** Add csv_field_unescaped(id,row,col,builder_id) that copies the field span while collapsing each `""` to a single `"`, OR materialize the unescaped bytes into the (currently dead) arena during csv_parse and store the cleaned span. Either way the documented RFC 4180 unescape becomes real and the reserved arena finally does work.
- **Impl path:** Compose with verba/builder (builder_push_byte/builder_push_bytes already used across verba) or arena_alloc1 (arena.iii) to produce the cleaned bytes; mirror the row/col bounds guards already present in csv_field_base/csv_field_len (lines 194-208) on the new accessor.
- **Regression:** SPLIT — the candidate's two variants have OPPOSITE profiles. Variant A (new additive csv_field_unescaped(id,row,col,builder_id)): non-regressing. It leaves csv_parse / csv_field_base / csv_field_len byte-identical, so the three consumers stay green — corpus 91 (which PINS raw span ab""cd: csv_field_len==6, qp[2]==34, qp[5]==100 at lines 46-50, audit tag H-CSV-1), corpus 1396 (lifecycle only), corpus 1467 (caps only). Variant B ("materialize unescaped bytes into the arena during csv_parse and store the cleaned span") is a HARD REGRESSION: the cleaned span for ab""cd is 5 bytes (ab"cd), so corpus 91 line 46 (==6) fails and line 50 (qp[5]) reads past the 5-byte span. A "real" verdict is scoped to Variant A ONLY; Variant B must NOT be taken.

### 9. [STDLIB/iii/numera/aes_gcm.iii] aes_gcm_init (line 416) / aes_gcm_init_256 (line 422)  (sev 2, conf 0.9, missing-integration)
- **Current:** GCM exposes a 128-bit key path (aes_gcm_init -> aes128_set_key, line 418) and a 256-bit path (aes_gcm_init_256 -> aes256_set_key, line 423). There is no aes_gcm_init_192, even though aes.iii exports aes192_set_key (aes.iii:279) and the GCM core (H/J0/GHASH/CTR) is key-size-agnostic. A grep for init_192 / gcm.*192 across numera returns nothing. Separately, gcm_init_after_key rejects every nonce length != 12 (line 397, GCM_E_NONCE), so the SP 800-38D GHASH-derived J0 for non-96-bit IVs is also unimplemented.
- **Proposed:** Add aes_gcm_init_192(key, nonce, nonce_len) that calls aes192_set_key then the shared gcm_init_after_key, exposing standard AES-192-GCM. (Secondary: implement the SP 800-38D arbitrary-length-IV J0 = GHASH(IV || 0^s || len) path so non-96-bit nonces are supported instead of rejected.)
- **Impl path:** Clone aes_gcm_init_256 to aes_gcm_init_192 calling aes192_set_key (aes.iii:279); no other change since gcm_init_after_key derives H/J0 from the active key schedule. Add a NIST AES-192-GCM KAT (key/iv/aad/pt/ct/tag) to the corpus.
- **Regression:** Primary path is purely additive and low-risk. aes_gcm_init_192 would clone aes_gcm_init_256, swapping aes256_set_key->aes192_set_key, then call the shared gcm_init_after_key. Verified the GCM core is key-size-agnostic: aes_encrypt_block (aes.iii:456) drives the round loop on AES_NR, and aes192_set_key sets AES_NR=12 + AES_KEY_SET, identical mechanism to the 256 path that corpus 69 already proves works. No existing caller is touched; the seal/open path is unchanged. The SECONDARY proposal (SP 800-38D arbitrary-length-IV J0) is NOT low-risk and should be split off: line 397 is a deliberate fail-closed posture, and while no corpus test pins the non-96-bit rejection (all tests use nonce_len=12u64, so flipping it does not redden a pinned KAT), it is a real GHASH-derived J0 implementation, not a 3-line compose, and changes crypto-acceptance behavior -- it needs its own gated unit, not this one.

### 10. [STDLIB/iii/numera/hkdf.iii] hkdf_sha256_extract/expand/oneshot (also pbkdf2.iii: pbkdf2_sha256_derive)  (sev 2, conf 0.9, missing-integration)
- **Current:** HKDF imports and uses ONLY hmac_sha256 (hkdf.iii:28-29) with HKDF_HASH_LEN fixed at 32 (line 35); every export is hkdf_sha256_*. PBKDF2 likewise uses only hmac_sha256 as its PRF (pbkdf2.iii:25-26, 85, 95). Yet hmac.iii fully implements and exports hmac_sha512 / hmac_sha512_tag (hmac.iii:147-164, 128-byte block, 64-byte tag). A grep for hkdf_sha512 / pbkdf2_sha512 across numera returns nothing -- no sibling file supplies the SHA-512 KDF either.
- **Proposed:** Add hkdf_sha512_extract/expand/oneshot (32->64-byte PRK/T blocks, max out 255*64) and pbkdf2_sha512_derive/oneshot driven by the existing hmac_sha512/hmac_sha512_tag, so the SHA-512 PRF variants RFC 5869/RFC 8018 define are reachable.
- **Impl path:** Parameterize or duplicate the extract/expand loops with hashlen=64 and the 128-byte buffers, calling hmac_sha512/hmac_sha512_tag (hmac.iii:147/162); same for PBKDF2's U_1/U_j loop. Add RFC 5869 Appendix-A-style SHA-512 test vectors plus an RFC 6070-equivalent PBKDF2-SHA512 KAT.
- **Regression:** Low. Change is purely additive: new hkdf_sha512_* / pbkdf2_sha512_* exports with their own 64-byte work buffers; the SHA-256 KDF code paths (hkdf.iii / pbkdf2.iii) are byte-untouched. The composed organ (hmac_sha512/hmac_sha512_tag) is already production-used by drbg.iii:102-103 and KAT-proven by corpus/203 (RFC 4231 TC2, full 64-byte vector), so it is not a fragile dependency. Three watch items, all standard: (1) project's v3 REACHABILITY ratchet requires every new @export to have a corpus use site -- the proposed RFC 5869/8018 SHA-512 KATs supply exactly that, so they are mandatory not optional; (2) adding symbols to libiii_native.a may move the seal -- handled by the normal determinism-gated build, not a design decision; (3) MUST allocate NEW 64-byte PRK/T/U buffers (reusing the [u8;32] HKDF_WORK_PRK/_T or PBK_WORK_T/_U would corrupt -- the existing buffers are sized for the 32-byte digest only). No tested/deliberate SHA-256-only posture exists to regress: there is no pinning test asserting "SHA-512 KDF absent," and RFC 5869/8018 explicitly define the SHA-512 variants.

### 11. [STDLIB/iii/tempora/duration.iii] duration_to_* conversion family  (sev 2, conf 0.9, capability-gap)
- **Current:** The from-side is complete: duration_from_nanos/micros/millis/seconds/minutes/hours all exist with precomputed saturation limits (lines 34-61). The to-side is only partial: duration_to_nanos (63), duration_to_millis (67), duration_to_seconds (71). There is no duration_to_micros, duration_to_minutes, or duration_to_hours (grep over STDLIB confirms only the three to_* exist).
- **Proposed:** Add duration_to_micros(d) = d / DUR_NANOS_PER_MICRO, duration_to_minutes(d) = d / DUR_NANOS_PER_MINUTE, duration_to_hours(d) = d / DUR_NANOS_PER_HOUR -- one line each, mirroring duration_to_seconds.
- **Impl path:** Append three exported one-liners after duration_to_seconds using the existing DUR_NANOS_PER_MICRO/MINUTE/HOUR constants (already defined lines 18-22). Add a corpus arm in 1058_duration_ops.iii. No new dependency, no overflow concern (division shrinks).
- **Regression:** No regression risk. The change is purely additive @export total functions; adding symbols cannot break existing callers, and no consumer depends on the absence of these names (grep across STDLIB + _scan/islands.txt confirms only the 3 to_* exist and are referenced by corpus 40/1058/1395). Division shrinks the value, so no overflow path exists. The only real-correctness concern -- the W11 division caveat -- was traced and refuted: the header note is about compile-time constant folding (DUR_MAX/k), while the proposed code is RUNTIME division of a parameter. The emitted asm for the existing duration_to_seconds (tempora_duration.iii.o.s:402-423) is a real divq over a rip-relative const divisor with xorl %edx,%edx unsigned setup; corpus 893 (passing) proves iiis-2's runtime divq is correct for large high-bit dividends. Adding exports only triggers the routine stdlib reseal/mhash + corpus-green determinism gate, not a behavior change.

### 12. [STDLIB/iii/memoria/span.iii] span__copy / span__cmp (absent from the generic @specialize set)  (sev 2, conf 0.86, capability-gap)
- **Current:** The hand-written u8 surface provides span_u8_copy (lines 54-67) and span_u8_cmp (lines 74-90). The generic element-width surface added under @specialize(u16,u32,u64,i8,i16,i32,i64) only covers span__load (112), span__store (120), span__fill (128), span__find (139). There is no generic span__copy or span__cmp (grep confirms).
- **Proposed:** Add span__copy<T> and span__cmp<T> over the same @specialize set, mirroring the u8 bounds-checked copy and lexicographic compare (including the W82 reflexivity fix at line 81), so wider-element spans get the full move/compare surface the byte span already has.
- **Impl path:** Mirror span_u8_copy/span_u8_cmp with *T strides; the @specialize sizeof/stride machinery is already exercised by span__load/store. Extend a span corpus test (1059_span_ops.iii / 1564_span_cmp_reflexive.iii) with a u32 copy+cmp arm. No new dependency.
- **Regression:** Effectively nil. The change is purely additive: new @specialize(u16,u32,u64,i8,i16,i32,i64) exports mangle to span_u16_copy/..._cmp etc., which do NOT collide with the hand-written span_u8_copy/span_u8_cmp (u8 is excluded from the specialize set, same pattern as the existing load/store/fill/find). It touches neither the u8 functions nor the 4 existing generic ops, so corpus 1059 (u8 cmp/find), 1564 (W82 reflexivity), and 396 (specialize stride guard) are untouched. No consumer of an as-yet-nonexistent function can regress. The only NEW codegen surface is a relational </> compare under a @specialize type-param T -- unproven today (find uses == only; load/store/fill never order), so a signed-compare defect could ship silently if the gating test is unsigned-only.

### 13. [STDLIB/iii/sanctus/seal_resolver.iii] closure_includes_resolver_seal (lines 201-206)  (sev 2, conf 0.85, missing-integration)
- **Current:** The function's name and docstring assert the closure ROOT absorbs SEAL_RESOLVER.mhash, but the body is `return g_seal_resolver_computed` (205) — it reports only whether the seal was COMPUTED, a strictly weaker property than closure inclusion. The genuine fold exists in a separate organ, sanctus/closure.iii::closure_compute_with_resolver (90-104) with closure_with_resolver_is_computed (112-114), and mandate_m22.iii consults THAT one (extern line 22, check line 29). closure_includes_resolver_seal is referenced only by corpus 1399 (166-170), which pins exactly the weak compute-flag semantics.
- **Proposed:** Have closure_includes_resolver_seal extern and return sanctus/closure.iii::closure_with_resolver_is_computed() (the real absorbed-root property), so the two organs compose and the export's guarantee matches its name. Update corpus 1399 to drive closure_set + closure_compute_with_resolver and assert the true inclusion both ways.
- **Impl path:** Compose with the already-built sanctus/closure.iii::closure_with_resolver_is_computed (112-114) / closure_compute_with_resolver (90-104). seal_resolver already participates in this graph (closure.iii externs seal_resolver_byte/is_computed at 80-81), so the delegating extern is consistent with the existing dependency direction.
- **Regression:** corpus 1399 (lines 166/170) is the only consumer and pins the weak compute-flag semantics. The fix breaks line 170: post-`seal_resolver_compute` it expects 1, but the real property (`closure_with_resolver_is_computed`) is still 0 there because `closure_compute_with_resolver` is not called until line 171. So the closure section of 1399 must be rewritten to: assert 0 pre-fold (negative arm), then closure_set + seal_resolver_compute + closure_compute_with_resolver, then assert 1. This is a strengthening aligned with FROZEN ADR-RES-010 sec.18 (which defines `closure_includes_resolver_seal()==1` AS 'closure root absorbs SEAL_RESOLVER.mhash'), not a regression of intended behavior. M22's runtime verdict is unaffected: mandate_m22.iii calls `closure_with_resolver_is_computed` directly, and after delegation the two are equivalent. Implementer must confirm the mandate-audit bits stay byte-identical and pass the determinism/seal gate + full corpus. Watch the seal_resolver<->closure circular extern: it is fine in a single static archive (forward decl only), and corpus 1399 already externs both sides.

### 14. [STDLIB/iii/verba/regex.iii] rx_parse_class  (sev 2, conf 0.85, capability-gap)
- **Current:** rx_parse_class (lines 259-294) supports explicit chars and 'a-z' ranges, but line 257 states "'[^...]' negation is NOT supported (would need the full byte alphabet)". Positive classes are expanded into an ALT chain of individual RX_LITERAL nodes (lines 283, 288). The sibling shell-glob matcher glob.iii fully supports negated classes (glob.iii line 40 leading-'^' detect, lines 76-78 invert membership).
- **Proposed:** Add a dedicated negated-class node kind (e.g. RX_NCLASS) carrying a compact byte-set, with derivative D_c(NCLASS) = EPSILON if c is NOT in the set else NULL, and nullable=0. This represents `[^...]` directly without enumerating 256-|set| literals, matching glob's existing semantics.
- **Impl path:** Introduce RX_NCLASS plus a small set payload in the 16-byte node layout (or a side table); extend rx_parse_class to detect a leading '^', and add RX_NCLASS cases to rx_nullable and rx_derivative. Reference glob.iii::glob_class_match for the negation+range membership logic.
- **Regression:** Low. Additive node kind (RX_NCLASS=7) only produced when '^' follows '['. Both regex consumers (35_regex_basic.iii, 1129_regex_phase_c.iii) were re-read: neither references negated classes, so no tested behavior is pinned. Current incidental behavior — '[^abc]' falls through rx_parse_class's else and treats '^' as a literal, compiling to ALT{^,a,b,c} — is untested and not a deliberate feature (the line 256-257 comment frames negation as a known unsupported limitation, not a design choice). Switching it to negation aligns with POSIX and the sibling glob.iii. Existing positive patterns are byte-for-byte unaffected. Only added load: reset a new class-set counter in regex_compile alongside REGEX_NODE_COUNT=2.

### 15. [STDLIB/iii/verba/html_escape.iii] html_try_entity / html_unescape  (sev 2, conf 0.85, capability-gap)
- **Current:** html_try_entity (lines 83-164) decodes only the five named entities &amp;/&lt;/&gt;/&quot;/&apos; plus a single HARDCODED `&#39;` matched as the literal bytes '#','3','9',';' (lines 134-146) -- not a parsed number. There is no general numeric `&#NNN;` or hex `&#xHH;` character-reference decoding. Consequently `&#34;` (=") and `&#8364;` (=€) are NOT decoded, even though the equivalent `&quot;` is. html_unescape (lines 166-186) drives only this fixed table.
- **Proposed:** Generalize the numeric path the `&#39;` special-case half-implements: parse `&#` digits `;` and `&#x` hex `;` into a codepoint, validate the range, UTF-8 encode, and push the bytes; keep the five named entities. This makes html_unescape a real HTML/XML character-reference decoder rather than a 6-token lookup.
- **Impl path:** Compose with verba/rune for UTF-8 encoding of the decoded codepoint (rune already owns UTF-8 in this tree); replace the literal `&#39;` branch in html_try_entity with a general decimal/hex numeric-reference parser; reject out-of-range/overlong values. The markup MK_TEXT/MK_ATTR_VALUE slices then become decodable end-to-end.
- **Regression:** Low. Corpus 1053 is the only pinning test; it pins &#39;->1 byte 0x27, &apos;->0x27, &amp;->&, and truncated "&apos" (no ';')->literal pass-through. A correct generalized decimal/hex parser preserves all four: cp=39 is ASCII so still 1 byte; named entities are kept; the trailing ';' remains mandatory; empty/invalid digit runs fall through to literal. No test pins "numeric refs stay undecoded," so no tested behavior is regressed. The one real implementation change is widening html_unescape's single-byte push (line 174 pushes only HTML_LAST_DECODED) to push the n bytes from rune_utf8_encode; this only affects previously-undecoded multi-byte refs and leaves every pinned single-byte case identical. html_escape (the escape direction) is untouched; markup.iii is a read-only consumer that benefits. No consumer regresses.

### 16. [STDLIB/iii/verba/uri.iii] uri_parse / uri_authority_base / uri_authority_len  (sev 2, conf 0.85, capability-gap)
- **Current:** uri_parse (lines 200-284) splits a URI into scheme/authority/path/query/fragment, but per the header (lines 14-17) treats authority, path, query, and fragment as OPAQUE byte spans. There is no decomposition of authority into [userinfo '@'] host [':' port] (RFC 3986 §3.2), and no decomposition of the query into '&'/';'-separated k=v pairs. uri_authority_base/len (lines 300-310) return the whole authority span; callers must re-implement the sub-grammar.
- **Proposed:** Add uri_authority_userinfo_base/len, uri_authority_host_base/len, uri_authority_port (split the authority span on the last '@' then the final ':'), and a query-pair iterator (uri_query_pair_count / uri_query_pair_key / uri_query_pair_value, splitting on '&'/';' then '='), all computed over the already-parsed spans and decodable via the existing uri_pct_decode.
- **Impl path:** Additive accessors over URI_AUTH_BASE/URI_AUTH_LEN and URI_QUERY_BASE/URI_QUERY_LEN; reuse uri_is_digit for port validation and uri_pct_decode (already exported) for value decoding. No change to the existing parse or span accessors.
- **Regression:** Very low. Purely additive read-side accessors over the already-populated URI_AUTH_BASE/LEN and URI_QUERY_BASE/LEN slots; the slot table, allocator, uri_parse, and all existing component getters are untouched. Traced both consumers: corpus 66_uri_parse.iii pins only the 5 existing base+len getters and 1410_semver_uri_sha512_tp.iii uses only scheme/path/drop -- neither touches host/port/userinfo/query-pair, so no tested behavior is regressed. Standard stdlib rebuild + corpus-green covers it (compiler-unreferenced @export adds are determinism-safe per project convention).

### 17. [STDLIB/iii/tempora/rfc3339.iii] rfc3339_parse / rfc3339_format / rfc_fmt_ok  (sev 2, conf 0.85, partial-impl)
- **Current:** rfc_fmt_ok (lines 90-112) hard-requires exactly 'YYYY-MM-DDTHH:MM:SSZ': offset 19 must be 'Z' (line 96) and src_len must be >= 20 (parse line 115), and rfc3339_format always emits 'Z' (line 62). Any RFC 3339 timestamp carrying a numeric UTC offset ('+HH:MM' / '-HH:MM') or fractional seconds ('.sss') is fail-closed-rejected with the 0xFFFFFFFFFFFFFFFF sentinel. Header declares this a 'basic subset' (line 5).
- **Proposed:** Extend the parser/formatter to the full RFC 3339 time-offset production: accept the trailing 'Z' OR a numeric offset (+|-)HH:MM, normalizing the offset into the returned Unix seconds; optionally accept and discard/round the fractional-seconds field. Format could gain an offset parameter or continue emitting 'Z' while the parser accepts both.
- **Impl path:** Generalize rfc_fmt_ok to validate either 'Z' at the known terminus or a (+|-)HH:MM offset (with digit checks like the existing per-position checks), then in rfc3339_parse subtract/add offset_minutes*60 from the computed Unix seconds with the same saturation discipline used elsewhere. Add corpus arms for an offset round-trip and an offset-vs-Z equivalence. Depends only on the existing calendar externs.
- **Regression:** Low risk, but scope must be parser-only. CRITICAL CONSTRAINT 1 (formatter ABI is pinned): corpus 95 and 1621 both call rfc3339_format(unix_seconds, out_20) with the 2-arg signature and assert 'Z' (90u8) at byte 19. The proposal's optional "Format could gain an offset parameter" would break both callers -- so keep rfc3339_format's signature and 'Z' output byte-identical; any offset-emitting formatter must be a NEW export, not a mutation. CRITICAL CONSTRAINT 2 (OOB read): corpus 95 does malloc(20) -- exactly 20 bytes. The widened rfc_fmt_ok/parse must require src_len >= 25 BEFORE reading offset bytes 20-24, and the existing src_len<20 (line 115) + Z-at-19 (line 96) path must stay byte-identical so the Z case never reads past offset 19. Traced all consumers: prespec.iii (lines 1692/1698) only takes &rfc3339_format/&rfc3339_parse as registered addresses -- no semantic dependence, unaffected. The W78 falsifier (1560) pins REJECTION of malformed terminators, not rejection of valid numeric offsets -- so widening is additive, not a regression of a tested behavior.

### 18. [STDLIB/iii/numera/aes_siv.iii] siv_s2v (line 131) / aes_siv_encrypt (line 207) / aes_siv_decrypt (line 225)  (sev 2, conf 0.82, partial-impl)
- **Current:** siv_s2v takes exactly one associated-data argument (ad, ad_len) before the plaintext (line 131); aes_siv_encrypt/decrypt expose a single (ad, ad_len) component (lines 207, 225). The key is hard-wired to AES-SIV-CMAC-256: aes128_set_key is the only key setter used (lines 70, 213, 220, 237), K1 and K2 are fixed 16-byte halves of a 32-byte key (header line 5, code lines 209, 227). The module header documents both limits ('single AAD component', line 15; 'AES-SIV-CMAC-256', line 5).
- **Proposed:** Generalize S2V to a vector of associated-data strings AD_1..AD_n (the doubling-chain D = dbl(D) ^ CMAC(AD_i) per RFC 5297 step 4), exposing an n-component AAD API; and add AES-SIV-CMAC-384/512 by splitting a 48/64-byte key into K1||K2 and selecting aes192_set_key/aes256_set_key.
- **Impl path:** Refactor siv_s2v to loop a caller-provided array of (ptr,len) AAD components folding each into the dbl/XOR chain before the final plaintext step; parameterize the key setter on key length (32->aes128, 48->aes192, 64->aes256) in aes_siv_encrypt/decrypt. Add RFC 5297 Appendix A.1/A.2 multi-AD test vectors.
- **Regression:** Low. The only consumer is corpus test 2408_aes_siv_rfc5297.iii (RFC 5297 Appendix A.1), which calls the single-AAD signature aes_siv_encrypt/decrypt(key32, ad, ad_len, ...). The proposal is additive: keep the single-AAD wrapper, add vector-form exports + larger key sizes. The S2V vector refactor for n=1 (D=CMAC(0); dbl(D); D^=CMAC(AD_1); then plaintext) is byte-identical to the current single-AD path, so 207 stays green provided the wrapper is preserved. Implementation care items: key-half split must track key length (16/24/32 byte K1) and the new extern decls for aes192/aes256_set_key must be added to aes_siv.iii. Not load-bearing for self-host/seal/consensus.

## Dedicated-wave
### W1. [COMPILER/BOOT/ast.iii] iii_ast_let_mutable (ast.iii:4211) — the swallowed assign-to-immutable check belongs in sema.iii s_walk_node STMT_ASSIGN arm (sema.iii:1732)  (sev 3)
- **Proposed:** Wire the existing iii_ast_let_mutable accessor into sema: add a parallel SEMA_LOCAL_MUT:[u32;256] column; in s_walk_stmt_let set it from iii_ast_let_mutable(SEMA_AST, node) (and for-loop / sanctum-frame / match-pattern binders => 0); in the STMT_ASSIGN arm, when the lvalue is an EXPR_IDENT (or EXPR_INDEX/EXPR_FIELD rooted at one) whose binder resolves to a local with MUT==0, emit a diagnostic (reuse SEMA_E_DECL_DUP's class or add a dedicated TYPE-ASSIGN code).
- **Regression:** HIGH, and I traced it through the binary path, not just the source. cg_r3.iii (the active codegen) handles R3_K_STMT_LET at line 2673 using only iii_ast_let_value (off 24), iii_ast_let_type (off 20), and iii_ast_let_name_off (off 12); it never reads STMT_LET offset 8. Tree-wide, the ONLY reads of STMT_LET off-8 are the accessor's own body (ast.iii:4212) and the node-size registration (ast.iii:2242); no .c file reads it either. So the mut flag is fully inert: a plain `let` reassignment compiles TODAY as benign mutable stack storage with no diagnostic. Adding the front-end check therefore newly REJECTS code that compiles and runs correctly now -- any live bootstrap/stdlib site doing `let x = ...; x = ...` (rather than `let mut`/`var`) would fail to compile, breaking the self-host fixpoint and the stage1_corpus byte-equivalence gate. This is the "rejects currently-working code," not "rejects latently-wrong code," variant. The risk cannot be cheaply ruled out (requires a scoped dataflow audit of the whole tree), and the lvalue->binder resolution must also correctly classify parameter / module-var / for-loop / match-pattern binders to avoid false positives.
- **Notes:** All six factual claims verified line-by-line against live code. parse.iii:2652-2653,2679: mutable_ computed (var=>1, `let mut`=>1, plain let=>0) and written to STMT_LET off 8. ast.iii:4211-4215: iii_ast_let_mutable reads off 8, normalizes to 0/1, @export. Grep for the symbol matches ONLY its own definition => provably dead code / zero callers. sema.iii:348-353 local stack = NAME_OFF/NAME_LEN/BINDER/DEF, no mut column; s_local_push (791-801) takes no mut arg; s_walk_stmt_let (1531-1537) pushes without the flag; STMT_ASSIGN arm (1732) just walks lvalue+value with no immutability check. No SEMA_LOCAL_MUT / immutable / E_ASSIGN anywhere in sema.

Verdict=real: this is a genuine inert-feature / island. The parser computes and the accessor exposes per-binding mutability, but nothing consumes it -- a standard front-end safety check (assign-to-immutable) is entirely absent and its feeding data is unused. NOT a deliberate fail-open design: there is no pinning corpus test asserting "assign to immutable let is accepted," and the orphaned-but-built accessor is the signature of an intended-yet-unwired feature, which is what closes the otherwise-tempting spurious reading ("let just isn't immutable by design"). impl_is_concrete=true: bounded compose of existing organs -- add a parallel SEMA_LOCAL_MUT:[u32;256] column, set it in s_walk_stmt_let from iii_ast_let_mutable(SEMA_AST,node) (binders from for/sanctum/match => 0), and in the STMT_ASSIGN arm resolve an EXPR_IDENT (or EXPR_INDEX/EXPR_FIELD root) lvalue via the existing binder/local-lookup and emit via s_emit_error; the iii_ast_assign_lvalue/value accessors already exist (ast.iii:1867-1868).

recommend_now=false is MANDATED by the rubric independent of the regression question: sema.iii is the LIVE self-hosting semantic analyzer (load-bearing self-host compiler core). Plus the verified HIGH regression risk above. This needs a dedicated gated wave: full clean build + tree-wide audit proving no live .iii assigns to a non-mut binding, plus a design decision on the diagnostic code class (reuse vs dedicated TYPE-ASSIGN code). A single in-session unit is not appropriate.

### W2. [COMPILER/BOOT/cg_r3.iii] r3_emit_expr (EXPR_MATCH, ~2634-2659) and r3_emit_stmt (STMT_MATCH, ~2945-2967)  (sev 3)
- **Proposed:** After r3_emit_pattern_bind in both EXPR_MATCH and STMT_MATCH, if iii_ast_match_arm_guard(node,arm) != 0: emit the guard expr, r3_pop_rax(), R3_STR_TESTRRAX, then R3_STR_JZ + R3_PFX_LSKIP + skip_lbl + NL -- byte-identical to cg_r3.c:2244-2249 / 2852-2857.
- **Regression:** No regression to any tested/deliberate behavior. The added guard-emit path is dead on the existing corpus: grep for `pat if cond =>` is 0 hits and the C-differential/fixpoint gate is green, which only holds because no gated source exercises a guard. Adding the code leaves all currently-gated emitted output byte-identical (path unreached on guard-free sources), and the inserted `if guard != 0u32` uses a plain conditional, not a match-guard, so cg_r3.iii's own self-compile is unaffected. The guarded-arm fall-through to the next arm / `movq $0` default matches the C seed and is not a deliberate fail-open/closed posture being violated. Caveat: to PROVE the fix (positive arm), a `pat if cond =>` corpus test must be added and checked identical under cg_r3.c -- without it the new code is unverified dead code, which the project's no-dead-stub mandate forbids.
- **Notes:** VERIFIED REAL gap. cg_r3.iii EXPR_MATCH (2634-2659) and STMT_MATCH (2945-2967) compute pattern_compare + pattern_bind but never call iii_ast_match_arm_guard (declared cg_r3.iii:148, sole occurrence). C seed cg_r3.c:2244-2249/2852-2857 emits the guard verbatim; parse.iii:2098-2108 stores it; sema.iii:1584 + sid.iii:900 walk it; ast.iii:2024 accessor reads child slot 12. CORROBORATION the proposal omitted: emit_sanctum.iii lines 618/636 ALREADY handle the guard correctly with the same accessor + testq/jz idiom -- proving the full chain works in-tree and cg_r3.iii/cg_r0.iii are the outliers. Impl is concrete: mirror cg_r3.c exactly using R3_STR_TESTRRAX (2174, ='    testq %rax, %rax\n'), R3_STR_JZ (379, ='    jz '), R3_PFX_LSKIP (307, ='L_skip_'); the identical emit idiom is already live at cg_r3.iii:2846-2847. recommend_now=false: this is load-bearing self-host compiler core; per the proposal's own note it needs a full seal + stage1_corpus + fixpoint gated wave, plus a positive guard-using corpus test validated against the C-seed differential, and the cg_r0.iii sibling (ungated, structurally different match codegen at 1037) needs its own careful mirror of cg_r0.c:704-705/877-878. Dedicated gated wave, not a single in-session unit.

### W3. [COMPILER/BOOT/cg_r3.iii] r3_emit_expr EXPR_BINARY op==17 (logical `and`, line 2511)  (sev 3)
- **Proposed:** Define a multi-line R3_STR_LAND const equal to cg_r3.c's 7-instruction LAND byte sequence and emit it for op==17 in place of the TESTRRAX+SETNE pair.
- **Regression:** Low functional-regression risk, but the change is bootstrap/seal-moving. Re-read confirms op==17 emits bool(lhs) (TESTRRAX+SETNE, defs 2174/2164) and discards %rcx/RHS, while op==18 LOR is byte-correct vs cg_r3.c 1487-1488 -- LAND is the lone broken arm. No built source uses `and` in expression position: the green C-differential gate + iiis-2==iiis-3 fixpoint + corpus 1069/0 would all break if a stage1/self-host source used `and`, since LAND is precisely the op that diverges between the correct C seed and the broken .iii compiler. So for every CURRENT source the emitted bytes are unchanged -> output byte-identity and the fixpoint are preserved, and NO tested behavior is regressed (the corpus pins no `5 and 0` case). HOWEVER the cg_r3.iii SOURCE changes (new R3_STR_LAND const + new op17 emit), so the iiis-2/iiis-3 compiler BINARY changes and the golden/seal must be re-rooted via the DRIFT-driven reseal. The identical cg_r0.iii twin (line 987) is largely ungated; cg_r0.c is already correct so fixing cg_r0.iii re-converges it, but it needs a dedicated kernel KAT to verify. Net: safe correctness improvement, but it must pass the full byte-identity + C-differential + fixpoint + corpus gate and a reseal.
- **Notes:** Confirmed by primary-source re-read at C:/Users/Edwin Boston/OneDrive/Desktop/III/COMPILER/BOOT/cg_r3.iii (line 2511 op==17 = TESTRRAX def 2174 + SETNE def 2164; line 2512 op==18 LOR = ORQ def 2134 + TESTRRAX + SETNE) and cg_r3.c (III_BIN_LAND 1482-1486, III_BIN_LOR 1487-1488, emit_line trailing-\n at 243-254). The LAND lowering computes bool(lhs) and drops the RHS held in %rcx -- a genuine incomplete port of a real non-short-circuit operator, not an intentional simplification (LOR being fully correct makes the asymmetry decisive). Existing R3_STR_ANDQ is `andq %rcx,%rax` so a new const with `andq %rdx,%rax` is legitimately required; the proposed 7-instruction byte sequence (incl. trailing \n) matches the C seed exactly. verdict=real because the symbol is genuinely below full capacity and the fix is a concrete, bounded composition of the existing r3_emit_arr primitive against the byte-identity-gated C reference. recommend_now=false because this is load-bearing self-host compiler codegen feeding the bootstrap/seal: it requires a dedicated gated wave (build_iiisN seal gate + C-differential + iiis-2==iiis-3 fixpoint + corpus regression + golden re-root, plus a kernel KAT for the ungated cg_r0.iii twin), not a single in-session unit.

### W4. [COMPILER/BOOT/cg_r0.iii] r0_emit_expr EXPR_BINARY op==4/op==5 (DIV/MOD, lines 958-959); R0_STR_IDIV/R0_STR_IDIVMOD (537/539)  (sev 3)
- **Proposed:** Branch DIV/MOD on operand signedness (reuse r0_ord_is_unsigned or an either_is_signed analog): for unsigned, emit `xorl %edx,%edx; divq %rcx` (and `... movq %rdx,%rax` for MOD), mirroring cg_r3's DIVU/DIVUMOD bytes.
- **Regression:** No tested CORRECT behavior regresses: cg_r0 op==4/op==5 currently emit cqto+idivq (R0_STR_IDIV/IDIVMOD, lines 537/539) unconditionally; the only behavior that would change is u64 division, which is precisely the buggy case (idivq reads a bit-63-set u64 dividend as negative -> wrong quotient or #DE). u32/i*/unknown keep IDIV (unchanged & already correct). The candidate's regression-risk reasoning is partly WRONG and I verified why: (1) cg_r0 is NOT 'largely ungated' -- cg_seam_gate.sh is a DIFFERENTIAL runtime gate (`--ring R0` cg_r0 vs default cg_r3, XOR-fold compared, Ring-3 harness) that already pins the sibling u64-ordering fix (ord_u64_bit63 probes), and cg_r0_width_gate.sh / cg_r0_crypto_gate.sh likewise; a u64-division seam probe is the in-session verification (FAILs pre-fix on cg_r0, PASSes post-fix) and a permanent slip-guard -- no kernel .sys metal KAT strictly required. (2) cg_r0.c (569-570) is the FROZEN iiis-0 seed and ALREADY diverges from cg_r0.iii: cg_r0.c compares (578-581) are signed setl/setle/setg/setge while cg_r0.iii has the unsigned-setcc dispatch (967-986); cg_r0 is outside the stage1_corpus byte-equivalence set (only cg_r3/stage1), so the 'dual-edit cg_r0.c required' claim is overstated -- editing cg_r0.iii alone matches the established compare-fix precedent. THE REAL DESIGN RISK (advisor-confirmed): the proposed 'reuse r0_ord_is_unsigned' is NOT equivalent to cg_r3's predicate. cg_r3 dispatches on r3_either_is_signed (line 2492: default UNSIGNED -> DIVU for unknown/u32/u64, IDIV only for proven-signed), whereas r0_ord_is_unsigned (877) returns 1 only for u64 (default SIGNED). They diverge on an unknown-typed operand carrying bit 63 -> cg_r0 would still emit idivq there (still broken) AND would FAIL a seam probe that cg_r3 passes. The correct seam-conformant fix mirrors cg_r3 by dispatching on the shared signedness resolver (iii_tc_expr_is_signed, callable in cg_r0 exactly as it already calls iii_tc_expr_is_u64), default-unsigned -- NOT r0_ord_is_unsigned.
- **Notes:** VERIFIED against live code (III root). Claims confirmed: cg_r0.iii op==4/op==5 (lines 958-959) unconditionally emit R0_STR_IDIV (537: '    cqto\n    idivq %rcx') / R0_STR_IDIVMOD (539: +'    movq %rdx, %rax') -- signed for ALL operands incl. u64. r0_ord_is_unsigned exists (876-880, u64-either) and is applied ONLY to ordering compares ops 13-16 (967-986), NOT to division. Reference fix in cg_r3.iii confirmed: R3_STR_DIVU (2128: 'xorl %edx,%edx; divq %rcx') / R3_STR_DIVUMOD (2130: +'movq %rdx,%rax'), dispatched at 2497-2498 on `signed = r3_either_is_signed` (2492), with the corpus-890/893 + 0xFFFF..FE/2 rationale comment (2123-2127). cg_r0.c (569-570) carries the identical signed-only div. So this is a genuine PARTIAL PORT: the unsigned-COMPARE rigor reached cg_r0.iii but the unsigned-DIVISION rigor did not, leaving kernel-backend u64 division latently wrong (#DE / wrong quotient on bit-63 dividends). Concrete bounded impl exists: add R0_STR_DIVU/R0_STR_DIVUMOD (copy cg_r3's byte arrays verbatim) + branch op 4/5 on a shared-resolver signedness predicate (iii_tc_expr_is_signed analog, default-unsigned -- NOT r0_ord_is_unsigned, per the divergence analysis above) + add a u64-division probe to cg_seam_gate.sh. Composes existing organs (cg_typeclass shared resolver + cg_r3's proven DIVU bytes + the existing differential seam gate). recommend_now=FALSE: this is Ring-0 kernel-backend CODEGEN compiled into the self-host compiler binary; the predicate choice (u64-only vs full signedness; default-signed vs default-unsigned) is a real seam-design decision with cg_seam_gate implications, and the cg_r0.c twin policy is a second decision -- this is a dedicated gated wave (seam-probe + resolver-predicate + twin policy + corpus reseal), not a mechanical in-session drop-in. Relevant files: C:/Users/Edwin Boston/OneDrive/Desktop/III/COMPILER/BOOT/cg_r0.iii, .../cg_r3.iii, .../cg_r0.c, .../cg_seam_gate.sh.

### W5. [COMPILER/BOOT/cg_r0.iii] r0_emit_expr EXPR_INDEX (1003-1009, R0_STR_INDEX_LD) and r0_emit_stmt STMT_ASSIGN INDEX-lvalue (1100-1112, R0_STR_INDEX_ST / R0_STR_INDEX_NTI)  (sev 3)
- **Proposed:** Port r3_index_obj_elem_kind + the width-aware INDEX_LD_B/W/SB/SW/L/SL and INDEX_ST_B/W/L variants into cg_r0, dispatching subscript load/store on element kind.
- **Regression:** cg_r0 is the Ring-0/kernel codegen backend and is NOT covered by the determinism/stage1 corpus (per project memory: only cg_r3/stage1 are gated) -- so no corpus test pins the quad-uniform subscript (the 'gap' is not a deliberate/tested design), but that also means the existing gate will NOT catch a regression here, raising risk. BSOD-class surface. Three trace concerns: (1) cg_r0.iii and cg_r0.c must stay byte-identical to each other -- a desynced dual-edit is a kernel-crash vector; (2) the elem_kind dispatcher returns 0 (quad) for *u64/unknown, so the packed-u64-read idiom kernel crypto relies on is PRESERVED -- good -- but any kernel code that accidentally depended on the buggy scale-8 stride of a *u8/*u16/*u32 base would change behavior (this is the CORRECT direction, matching cg_r3, but must be traced on the actual kernel .sys sources); (3) the secmem/NTI store has no byte/word form of movnti, so a width-aware secmem path needs an explicit decision (movb/movw fallback vs NTI-only-for-L/quad) -- a naive mirror that emits movntiq with a sub-quad register is malformed asm.
- **Notes:** Verified every cited line. GENUINE capability gap: cg_r0.iii:1007 (load) and :1111 (store) emit fixed scale-8/8-byte movq/movntiq with no element-width dispatch, while (a) cg_r0's own scalar ident load IS width-aware (line 918 movl for u32) and (b) the sibling cg_r3 backend has a full dispatcher (cg_r3.iii r3_index_obj_elem_kind 1926-1968 + r3_emit_indexed_load 1971-1979 + r3_emit_indexed_store 1983-1991), whose width variants change BOTH move-width and addressing scale (LD_B scale1/movzbq, LD_W scale2/movzwq, LD_L scale4/movl; ST_B movb %dl, ST_W movw %dx, ST_L movl %edx). Register layout matches cg_r0's store path (obj->rax, idx->rcx, val->rdx), so the port is a bounded composition of an existing organ. cg_r0.c:659 and :787-789 confirm the C twin shares the limitation (dual-edit claim correct). The project memory records this exact width-bug class biting cg_r0 (keccak lane / u32 high-half), and the scalar path was hardened while the subscript path was left quad-uniform -- so 'genuinely below full capacity' holds. recommend_now=FALSE: this touches kernel codegen (BSOD class), is ungated (the determinism corpus does not cover cg_r0, removing the usual safety net), requires a synchronized cg_r0.c + cg_r0.iii dual-edit, a design decision on the secmem/NTI sub-quad case (movnti has no byte/word form -- a detail the 'just mirror cg_r3' impl-path omits, since cg_r3 has no NTI path), and machine-code verification on a real .sys KAT that subscripts u8/u16/u32 buffers. That is exactly a dedicated gated wave, not a single in-session unit.

### W6. [STDLIB/iii/numera/egraph.iii] eg_extract_dijkstra  (sev 3)
- **Proposed:** Route the production descent (sov_isa si_extract) and nous_search extraction through eg_extract_dijkstra with the caller's pq arena; its built-in byte-identical fallback to eg_extract preserves correctness while large terms get the asymptotic win the W3.7 incremental index was built to enable.
- **Regression:** Medium-real, traced. eg_extract_dijkstra has two fallbacks (egraph.iii:1332 heap-alloc fail, :1384 incomplete-drain) but the fallback fires ONLY on capacity/heap-wall, NOT on a successful-but-divergent drain. Byte-identity vs the fixpoint is gated only by corpus 1481's tiny adversarial fixtures. The fixpoint eg_extract_relax (:1164) selects a class representative by strict-improvement across ascending passes; eg_dj_offer (:1290) selects by explicit lowest-node-id. These coincide on 1481 and on 'single-pass-achievable optima' (the :1278 comment's own caveat), but for a DEEP term where two equal-cost operator nodes finalize in different passes the emitted skeleton could differ while cost stays optimal. Consumers that pin the exact emitted term would be at risk: discovery-pipeline corpus 1370/1371/1373, 915_sov_self_improve, sov_isa/psi KATs, sov_pipeline, and sov_isa's golden seal (MEMORY: sovereign-optimizer golden re-root 8fb044cb->825635ea; sov calc optimizer sealed in cg_r3). Also the 3 call sites (sov_isa_descend, psi_collapse, nous_search_egraph) currently own no pq arena, so threading arena_new + arena lifetime into these load-bearing modules is part of the change, not a free rename.
- **Notes:** Re-read egraph.iii:1241-1387 (both extractors + shared emit tail), the 3 production callers, corpus 1481, and the design docs. The inert-feature gap is REAL: eg_extract_dijkstra is fully built + fallback-guarded + 1481-proven, yet a whole-tree grep finds its only caller is corpus 1481 -- every production extraction uses the O(passes x N) fixpoint eg_extract (sov_isa.iii:519, :1126; nous_search.iii:202). The asymptotic capability the W3.7 parents index was built to deliver is genuinely unrealized, and the impl path is concrete (thread arena, call dijkstra, rely on the documented fallback) and was the explicit unfinished tail of the structural plan (PLAN-1-WAVE0 Step 3-5, all unchecked: 'Make eg_extract dispatch to dijkstra'). BUT recommend_now=false: (1) sov_isa is the golden-sealed sovereign optimizer / self-host-adjacent codegen -- the rubric's exact 'dedicated gated wave' case; rerouting moves seals and needs a reseal + golden re-root + determinism gate. (2) The realized W0.6 decision (COVERAGE-LEDGER:517) DELIBERATELY kept the Bellman fixpoint as 'the pinned default, the fallback, and the ORACLE'; flipping production to dijkstra reverses a recorded posture and demands a design decision about which extractor is trusted-default. (3) The byte-identity gate is tiny-fixture-only; the emitted-representative tie-break mechanisms differ (strict-improvement-ascending vs explicit min-node-id) and the heap-wall fallback does not catch a result divergence, so a large/deep-term differential must be added BEFORE rerouting. Verdict real (genuine island + concrete organ-composing impl), but it belongs in a dedicated security/seal-gated wave, not a single in-session unit.

### W7. [STDLIB/iii/numera/slhdsa.iii] slh_n / slh_h / slh_d / slh_hp / slh_a / slh_kf (params) + iii_slhdsa_keygen/sign/verify  (sev 3)
- **Proposed:** Add the three FIPS-205 'fast' parameter sets (128f/192f/256f: h=66/66/68, d=22/22/17, h'=3/3/4, a=6/8/9, k=33/33/35, lg_w=4) as a parallel selector axis (e.g. a variant flag or distinct lv ids), with their own slh_len2 (which is no longer the constant 3 for the f sets), and wire new suite ids (0x0124-6 / 0x0134-6) through pq_params + pq_dispatch. The hypertree/WOTS+/FORS core is parameter-generic and already takes h/d/hp/a/kf as arguments, so only the param tables, suite map, and a KAT vs ACVP-f need adding.
- **Regression:** Existing s-set callers are safe IF f uses fresh lv ids (3/4/5) — s stays lv 0/1/2, core path unchanged, all s functions thread params through lv only. BUT the candidate's specific recipe collides with two PINNED E-PQD-2 security-falsifier tests it did not account for: (1) STDLIB/corpus/967_pq_dispatch_nibble_guard.iii line 18 explicitly asserts the candidate's own proposed suite id 0x0124 (nibble 4) MUST return E_BAD_SUITE (-1) — wiring 0x0124 as a valid SHAKE-128f route makes that line return 4i32 => corpus FAIL; (2) STDLIB/corpus/766_pq_params.iii line 27 pins pq_params_count()==9, which any PQP_ROWS bump breaks. Both tests must be legitimately revised (extend pq_nibble_ok to permit the new nibbles, move the out-of-range falsifier example to a still-unrouted id like 0x012F, update the count) WITHOUT masking the original underflow protection (0x0120 nibble-0 check and 0x0100/0x0114 -> NONE must still hold). So 'low risk / additive' understates it — it is a careful security-test revision, not zero-touch.
- **Notes:** Re-read confirms the gap is genuine. slhdsa.iii lines 112-119 encode ONLY the s sets (slh_d=7/7/8 never 22/22/17; slh_h=63/63/64 never 66/66/68); pq_params.iii carries exactly 3 SLH rows (0x0121/2/3) all s; pq_dispatch.iii routes families 0x0120 (SHAKE)/0x0130 (SHA2) only. FIPS-205 defines 12 sets (3 levels x {s,f} x {SHAKE,SHA2}); this implements the 6 s sets. The 6 f sets are normatively absent and interop partners select them. The core IS parameter-generic: slh_xmss_*/slh_fors_*/slh_wots_* take n/len/len1/len2/hp/a/kf as args (lines 366-775), and keygen/sign/verify derive all params from lv (lines 775-909), so f sets compose the existing organ unchanged. Reference oracles already exist (STDLIB/scripts/fips205_slhdsa_{shake,sha2}_ref.py) and ACVP KATs 770/771 gate the s sets. CORRECTION to the candidate: its claim that f sets need 'their own slh_len2 (no longer the constant 3)' is FALSE — w=16/lg_w=4 and len1=2n are invariant across s/f, so len2=3 for all 12 sets; the f/s split changes only h/d/h'/a/k. The line-119 'for all S sets' comment scopes to what was built, it does not imply f differs. recommend_now=false: this is normative post-quantum SIGNATURE crypto. Adding 6 parameter sets requires byte-exact ACVP KAT generation/verification across SHAKE+SHA2 f variants (a wrong param silently produces non-interoperable signatures) AND careful revision of two pinned E-PQD-2 security-falsifier corpus tests. That is a dedicated security-gated wave with a design decision on the suite-id/selector encoding, not a single in-session unit. Not self-host/compiler core, but it is crypto security posture.

### W8. [COMPILER/BOOT/sema.iii] s_walk_expr_call (sema.iii:1646)  (sev 3)
- **Proposed:** In s_walk_expr_call, when the callee is an EXPR_IDENT that is NOT a local binding (s_local_lookup_src==0, so it is not a fn-pointer value) and NOT a builtin (s_is_builtin_ident_src==0), resolve it via s_decl_table_lookup_src; if the resolved decl kind is FN/CYCLE/SEALED/EXTERN, fetch its declared param count and emit a diagnostic when it differs from iii_ast_call_arg_count.
- **Regression:** MEDIUM-HIGH but manageable in a gated wave. The proposed check only fires for EXPR_IDENT callees that are neither locals (s_local_lookup_src!=0, excludes fn-pointer values incl. corpus 26_fnptr) nor builtins (s_is_builtin_ident_src), and only for resolved decl kinds FN/CYCLE/SEALED/EXTERN — so indirect calls and builtins are correctly excluded. Confirmed no variadic externs and no ellipsis/va_arg grammar, so no valid call would be falsely rejected on that axis. The genuine risk: sema.iii is the active self-host analyzer and the new diagnostic must compile the compiler + full STDLIB/corpus body with zero pre-existing tolerated wrong-arity call sites, or the byte-equivalence/bootstrap gate reddens. No evidence the omission is deliberate (parse.iii itself labels wrong-arity a [soundness] hazard; corpus 35_unify_arity is unrelated runtime-lib arity). Not a pinned fail-open design.
- **Notes:** Verified every citation against live code (root C:/Users/Edwin Boston/OneDrive/Desktop/III). The gap is genuine: s_walk_expr_call (COMPILER/BOOT/sema.iii:1646-1675) never compares iii_ast_call_arg_count (already read at line 1649) to the callee's declared param count; arity mismatches pass straight to codegen. All organs the impl composes already exist: s_decl_table_lookup_src (sema.iii:1918) + SEMA_DECLS_KIND (308) + the param-count accessors iii_ast_fn/cycle/sealed/extern_param_count (ast.iii:4256/4260/4268/4272), guarded by the existing s_local_lookup_src/s_is_builtin_ident_src exclusions. Kind constants align (SEMA_K_*==III_AST_*; FN=4,CYCLE=3,EXTERN=7,SEALED=11) and ast_kind_child is kind-guarded, so accessor selection by kind is safe. Concrete bounded composition -> verdict real. recommend_now=false: load-bearing self-host compiler code + a newly-firing diagnostic gated on the bootstrap/byte-equivalence build; needs a dedicated wave proving clean self-host + full-corpus 0-fail, plus a stage1_corpus case (correct arity passes; under/over-arity rejected). Minor scope note for the implementer: the EXPR_IDENT-only restriction means module-qualified (Module.fn -> EXPR_FIELD) and sealed-method calls won't be covered, which is an acceptable first cut but not full coverage.

### W9. [STDLIB/iii/aether/develop_up.iii] du_encapsulate (behavioral baseline seal)  (sev 3)
- **Proposed:** Derive the baseline from the actual guest: split encapsulation into du_encapsulate (arm + open, no seal) and du_seal_baseline() -> sentinel_seal_baseline, called by the operator after a window of real du_observe samples -- so the sealed-good fingerprint reflects the guest's true benign IO physics rather than a placeholder constant.
- **Regression:** Contained. Of the four corpus callers of du_encapsulate (1585, 1597, 1599, 1600), only 1585 (`STDLIB/corpus/1585_develop_up_gateway.iii`) actually exercises the gateway's behavioral guard (du_observe -> du_guard, L110-122) and depends on the encapsulate-time seal; it must be reworked to: encapsulate (arm+open) -> du_observe a benign window -> du_seal_baseline -> du_guard. That rework PRESERVES the tested semantics (benign cadence -> SNT_COMMITTED; ransomware ramp -> SNT_ROLLED_BACK + byte-for-byte recovery), so it is fidelity improvement, not regression-masking. The other three are unaffected: 1597 (re-verified) only checks du_write disk-gating after open; 1599 (re-verified via grep: no du_guard/du_observe/du_seal) is a pure double-open DU_E test; 1600 (re-verified) uses du_encapsulate only for the rb_begin/du_replay arm and seals its own sentinel path directly. All three still pass as long as du_encapsulate keeps arm+vbd_open+cb_admit+rb_begin+DU_OPEN=1 and drops only the L77-79 observe-loop+seal. The unsealed window (open-but-not-yet-sealed) is provably fail-closed: du_guard -> sentinel_anomalous -> entropy_baseline_matches returns 0 when ENTROPY_BASE_SET[slot]==0 (entropy_monitor.iii L285), so it rolls back rather than committing. NOTE: the candidate cited sentinel.iii:92 for this, but L92 only covers SNT_INVALID (unarmed); the correct source is entropy_monitor L285 -- citation wrong, conclusion holds.
- **Notes:** Re-read develop_up.iii, sentinel.iii, entropy_monitor.iii, and all four corpus callers. The gap is genuine: du_encapsulate (L77-79) observes 64 samples of the hardcoded DU_BENIGN_CADENCE=3 then seals that as the behavioral allow-list baseline. A constant-3 window yields a DC-only spectrum (bin0=192, rest 0, per entropy KAT-3 analogy); the verdict is an EXACT elementwise match (entropy_monitor L282-293), so any real guest whose genuine benign cadence != constant-3 is judged anomalous at the very first du_guard and auto-rolled-back. The gateway exposes du_observe (L109) but NO re-seal entry -- sentinel_seal_baseline (already extern-declared at develop_up L35, and exercised directly by corpus 1577/1579/1597/1600) is reachable through the gateway ONLY inside du_encapsulate. So the lifecycle is half-wired and the box's behavioral immunity is unusable for any real opaque guest; corpus 1585 only goes green because L112 re-feeds the same synthetic constant-3 the gateway sealed. That is a textbook toy-trap on the production path, not a deliberate protocol invariant (the L55 comment literally calls 3 'the sealed-good behavioral baseline sample'). Impl is concrete and bounded: drop L77-79 from du_encapsulate; add `fn du_seal_baseline() -> i32 @export { if DU_OPEN==0u32 { return DU_E } return sentinel_seal_baseline() }`; rework only corpus 1585. It composes an existing organ (sentinel arm/observe/seal already wired). recommend_now=false: the literal triggers (self-host compiler, consensus-core, compiler seal/bootstrap) do NOT fire -- this is aether STDLIB. The reason to defer is narrower and honest: the change converts a one-call 'armed-and-sealed-ready' box into a two-phase 'armed-then-operator-must-seal' contract on the R-1 sealed box, which is a deliberate lifecycle/security-contract decision (who seals, when, documented unsealed-window semantics) touching all callers + DOCS/III-DEVELOP-UP-ENCAPSULATION.md. That belongs in a named gated wave with the contract decided up front, not a drive-by during verification -- even though the impl itself is small and the unsealed window is provably fail-closed.

### W10. [STDLIB/iii/aether/witness_hook.iii] wh_compute_frag_id (line 109, NOT @export) — absence of a wh_verify_frag_id export  (sev 3)
- **Proposed:** Add `fn wh_verify_frag_id(idx: u64) -> u8 @export` that recomputes into scratch via wh_compute_frag_id(idx, scratch) and constant-time-compares against the stored WH_FRAG_ID[idx] (written at 201-204); optionally a wh_verify_all() loop. This exports the field->id binding check that the redaction 'verifiable hole' (215-229) and every downstream proof anchor implicitly rest on.
- **Regression:** Merely adding the @export symbol regresses nothing: it changes no existing path, and all consumers (witness_compactor, topology_atlas, quarantine, manifest, h8_charter, witness_spine) call other exports that are untouched; corpus gate would catch any build break. HOWEVER the IMPLEMENTATION carries a real regression-against-tested-design risk: wh_redact (219-228) deliberately zeroes the payload while leaving WH_PAYLOAD_LEN and the stored WH_FRAG_ID intact (the 'verifiable hole', pinned by corpus 988_witness_redact.iii check (4) + self-test codes 17-21). A straight recompute-and-compare wh_verify_frag_id would fold the zeroed payload and MISMATCH the stored id for every redacted fragment -> a false tamper alarm that contradicts that deliberate, tested behavior. The verify MUST special-case WH_REDACTED[idx] (or verify against WH_REDACT_COMMIT) to avoid masking/contradicting the redaction design.
- **Notes:** Gap is genuine and verified by re-reading. witness_hook is two-level (fields -> stored WH_FRAG_ID at L201 -> root fold L316-331); the sole runtime verifier ws_chain_replay_verify (witness_spine 414-437) re-folds STORED ids only, so it proves root==fold(ids) but NOT field->id binding. Sibling sanctus/witness.iii::witness_verify_chain (228-247) folds field bytes directly, so it DOES bind fields->root -- the claimed asymmetry holds. wh_compute_frag_id is confirmed non-@export (asm L_ local label), called only at L201, and proven once for frag 0 in the locked self-test (452-491). A thin @export wrapper over wh_compute_frag_id + a 32-byte compare is a concrete, bounded impl that composes an existing organ. Two corrections to the proposal: (1) 'ident_eq externed at 24' is WRONG -- L24 is ident_copy; ident_eq is externed in witness_spine.iii:25, not witness_hook.iii, so the wrapper must add the extern or inline-compare; (2) the redaction interaction (above) is a genuine design decision the proposal omits. Plus wh_compute_frag_id swallows all cad i32 returns (always WH_OK; flagged in COVERAGE-LEDGER), so a trusted verify inherits a primitive that can't signal hash-engine failure. recommend_now=false: this is load-bearing provenance-integrity teeth that downstream proofs would anchor to; getting redaction semantics or the error-swallow wrong undermines the trust the export is meant to provide -> dedicated gated wave with a design decision, not a single in-session unit. Note: a consumer CAN already approximate this by reassembling the preimage from the field getters (exactly what 452-491 does) and comparing to wh_get_frag_id, but that re-implements the canonical encoding off the one source of truth -- which is precisely why exposing the canonical derivation is the right, non-spurious fix.

### W11. [STDLIB/iii/numera/zk_air.iii] air_fs_field_compute / air_derive_alphas / the FRI fold in air_prove/air_verify (@export region)  (sev 3)
- **Proposed:** Sample the Fiat-Shamir / FRI fold / query challenges from a large extension field (a degree-2 or degree-3 extension of 998244353, mirroring zk_field's tower idiom, or a >=2^64 field) so the STARK reaches 80-128-bit soundness.
- **Regression:** HIGH but containable. The seal (air_proof_cad / zk_proof_seal, line 833-843) and the KATs are byte-pinned by corpus 1007 (air_proof_seal_kat), 999 (zk_air_stark_selftest), 997, 1045 -- changing the challenge field changes proof bytes, so reseal + KAT rewrite is mandatory (the forge KAT->seal cascade). The dangerous trap: the FRI fold organ ntt_fri_organ.iii (fri_eval_and_fold/fri_fold_point, zk_air lines 46-47) and the sf_* base-field ops are SHARED with zk_stark (verified: zk_stark.iii lines 34-35, 89-95) plus the spine/gate/demo seals (corpus 376/1005/1006). Editing the shared organ to fold over an extension would regress all of those. The fix MUST add a PARALLEL extension-FRI path and leave the shared base-field organ untouched. Crucially the trace/NTT/LDE can stay in GF(998244353) (only the FS challenges in air_fs_field_compute + the fold alpha move to the extension), so the header's deliberate 'Field sf ... identical to zk_stark's sf so the two compose' contract (line 30-31) need NOT break -- this is what makes the change containable rather than a regression of a pinned design. No corpus test pins the soundness LEVEL, so the ~30-bit ceiling is a side effect, not a tested posture being protected.
- **Notes:** Re-read zk_air.iii lines 463-469 (air_fs_field_compute returns a u32 via sf_mul/sf_add), 569-579 (air_derive_alphas -> AIR_ALPHA[k] = air_fs_field_compute()), 625-630 + 738-749 (FRI fold alpha is a u32, q=998244353u32). All soundness-determining randomness IS drawn from the 30-bit base field GF(998244353). VERIFIED FACTS: (1) zk_field.iii's Fp2 = Fp[u]/(u^2+1) is over the BLS12-381 prime (line 41 'BLS12-381 group order', ~381-bit), NOT an extension of 998244353 -- so it cannot be borrowed, as claimed; fp256/fp384 are unrelated big primes. (2) No extension-field challenge sampling exists for the STARK. (3) The FRI fold organ is shared base-field-only. The gap is genuine and matches standard practice: production small-field STARKs (Plonky3/Winterfell/RISC0) keep the trace in a small field but draw FS/FRI challenges from a degree-2..5 EXTENSION to reach 80-128-bit soundness; a ~30-bit challenge space is a toy soundness level, below the 'real, not toy' bar the module sets for itself ('APOTHEOSIS C.3 ... general STARK'). impl_is_concrete=true: a new Fp2/Fp3 over 998244353 is a handful of sf_mul/sf_add per op, composed with the existing NTT/Merkle/FRI structure as a parallel extension-FRI path; grinding is explicitly optional. CAVEATS that hold my confidence at ~0.65 (I endorse the GAP, not the claim's framing): the claim's soundness reasoning is sloppy -- '~2^-30 regardless of query count, so 16 queries buy nothing' is wrong; query-phase error scales ~(1-delta)^q (delta tied to blowup B=4), and it is the FRI FOLDING/commit-phase error (~rounds*D/|F|, here ~256/2^30 per round) that the small field actually caps and the extension fixes. Also a degree-2 extension (~60-bit) alone will NOT reach 128-bit; needs degree-3+ and/or PoW grinding -- a security-parameter design decision. recommend_now=false because this is a SEALED, gated crypto artifact whose change requires reseal + KAT rewrite AND a security-parameter decision touching code shared with zk_stark/spine/gate -- exactly the rubric's 'dedicated gated wave', not a single in-session unit.

### W12. [STDLIB/iii/numera/rscode_ec.iii] rse_decode (@export, Gao RS error-correction decoder)  (sev 3)
- **Proposed:** Widen the RSE_* buffers to support standard GF(2^8) lengths up to n=255 (e.g. RS(255,223)); replace the truncating `(n+k)/2` stop with a parity-correct test (`2*deg(r) < n+k`) so odd-distance codes decode their full t=floor((n-k)/2); expose it as an error-correcting decode mode in erasure_store alongside the erasure-only rs_decode.
- **Regression:** Mixed across the proposal's three legs. (A) Buffer widening (48->>=256): mechanical, additive, self-contained to rscode_ec.iii. Verified gf_poly's gpoly_mul/divmod/add/scale/eval/trim operate purely on caller-provided pointers (the internal GPOLY_A/B/O/Q/R [u8;64/160] buffers at lines 171-178 belong only to gf_poly's own KAT), so no shared organ is touched and corpus 1223 (RS(7,3) t=2) stays byte-identical. Low risk. (B) Threshold change: provably behavior-neutral on all valid inputs (see notes), so it neither regresses nor benefits -- a non-fix. (C) erasure_store integration: WOULD regress/garbage a production path. erasure_store stores SYSTEMATIC rscode.iii codewords (data+parity shards); rse_decode is EVALUATION-FORM (c_j=M(j)), so swapping it in for rs_decode_apply decodes garbage. It is also redundant: keccak256 content addresses already convert every corruption into a KNOWN erasure, which the existing path recovers at up to n-k (double the t=floor((n-k)/2) of error correction). Leg C must be rejected.
- **Notes:** Re-read STDLIB/iii/numera/rscode_ec.iii, gf_poly.iii (ops), erasure_store.iii, corpus 1223, islands.txt. The candidate is a COMPOUND: 1 genuine leg + 2 bogus legs.

REAL part (why verdict=real): rse_decode is a genuine island (only consumer = corpus 1223 via rse_kat; flagged in _scan/islands.txt) capped at n<=24 (line 71) purely because every working poly is [u8;48] (lines 44-57). A standard RS(255,223) is impossible. Widening RSE_G0/G1/REM/RP/RC/RN/VP/VC/VN/Q/QV to >=256 is concrete, bounded, and composes gf_poly/galois at larger n with NO change to those organs (their ops are pointer/len based, no internal cap on the path used). So the symbol IS below full capacity with a concrete impl composing an existing organ -> real.

SPURIOUS part 1 (threshold): the claim that floor((n+k)/2) "over-reduces by one Euclid step for odd n-k, preventing full-t decode" is FALSE. By the EEA cofactor identity deg(v_i)=n-deg(r_{i-1}): at the decode step, deg(predecessor)=n-e>=n-t=(n+k+1)/2=m+1 and deg(M*E)=deg(M)+e<=k-1+t=(n+k-3)/2=m-1, where m=(n+k-1)/2. Degrees strictly decrease, so NO remainder ever has degree exactly m. Hence code's `deg<floor((n+k)/2)=m` (i.e. deg<=m-1) and proposed `2deg<n+k` (i.e. deg<=m) stop at the IDENTICAL remainder. Behavior-neutral cosmetic non-fix; the code already decodes full t for odd n-k. The header's "assumes n+k even" caveat is over-cautious. Do not apply this as a correctness fix.

SPURIOUS part 2 (erasure_store integration): architecturally unsound -- eval-form vs systematic encoding mismatch (garbage out), redundant with and strictly weaker than the existing content-address+erasure path (CA localizes every error -> known erasure -> recover up to n-k > t). Also caps mismatch (ES_MAX_N=16).

recommend_now=false: the proposal bundles a false-premise threshold change and an unsound production-path integration. Only the narrow buffer-widening is safe, but on an island nothing calls it is low value; the value-delivering integration needs a dedicated design decision (resolve eval-form vs systematic; justify error-correction over CA-erasure) rather than an in-session unit. Line citations (71, 44-57, 112, 26-27) are all accurate; only the behavioral claims about lines 112/26-27 are wrong.

### W13. [COMPILER/BOOT/sema.iii] s_register_struct_layout (sema.iii:1791) / s_struct_add_field (sema.iii:766)  (sev 2)
- **Proposed:** In s_register_struct_layout, before s_struct_add_field, call the existing s_struct_field_slot_cstr against the field's interned name; if it returns >=0 (already present), emit a diagnostic (reuse SEMA_E_DECL_DUP) and skip the duplicate add.
- **Regression:** LOW but non-zero. The change converts accept->reject for the class "struct with two identically-named fields." It can only regress the bootstrap if a struct in the live tree or stage1_corpus already declares duplicate field names; none observed (stage1_corpus 23_struct_decl.iii and 29_struct_instance.iii both use distinct fields). Tracing consumers: SEMA_STRUCT_FIELD_COUNT feeds iii_sema_struct_size_slots (sema.iii:2125), which cg_r3 r3_try_struct_slots (cg_r3.iii:1294-1301) uses to reserve let-binding stack slots; skipping the duplicate add corrects the (currently inflated) count, so any struct WITH duplicates would shift codegen slot reservation -- but that is exactly the latent bug, and for all well-formed structs the count is unchanged => byte-identical. No corpus test or comment pins the current silent-accept behavior as deliberate; on the contrary the sibling structural checks (top-level decl-dup at sema.iii:1863-1865 -> SEMA_E_DECL_DUP; s_seal_scan seal-collision -> SEMA_E_SEAL_COLLISION) establish that this pass is SUPPOSED to emit dup diagnostics, so the absence is an omission. Must still gate with a full build_iiis2 + corpus 0-fail byte-equivalence run.
- **Notes:** Re-read all cited lines; they say exactly what was claimed. s_register_struct_layout (sema.iii:1791-1814) iterates PARAM fields and calls s_struct_add_field (1809) with NO dup check. s_struct_add_field (766-774) only checks SEMA_STRUCT_FIELD_CAP then appends. s_struct_field_slot_cstr (776-787) returns the FIRST matching slot (return j as i32), so a duplicate name makes the 2nd field a dead, unaddressable slot while still incrementing SEMA_STRUCT_FIELD_COUNT (772) which iii_sema_struct_size_slots (2125) returns raw. EMPIRICAL CONFIRMATION (the premise was an inference, so I probed): compiled a 2-identically-named-field struct through the PINNED COMPILED/iiis-2.exe (--ring R3 --emit-asm-only) -> exit 0, zero diagnostics; the valid control (23_struct_decl.iii) also exit 0. So the front-end genuinely accepts the malformed struct silently today. Gap is real and below full capacity. IMPL IS CONCRETE: s_arena_strdup NUL-terminates its arena copy (sema.iii:422), so after fa = s_arena_strdup(fo,fl) the address s_arena_addr(fa) is a valid cstr_addr for the existing organ s_struct_field_slot_cstr(s, addr); if it returns >=0, emit SEMA_E_DECL_DUP (const at 191, already wired into s_error_name_addr slot 1) and SKIP the add. Two correctness must-dos for the gated wave: (1) SKIP the duplicate add, not merely emit-and-continue, or the count stays inflated; (2) emit against the field PARAM node fp (1801) for precise line/col. recommend_now=false: this is the active self-host sema struct table consumed by cg_r3 layout and it changes accept->reject semantics -- needs a dedicated security/design-gated wave (decision that hard-error is the right posture + full build + corpus byte-equivalence + a new negative stage1_corpus case proving rejection), not a fire-and-forget in-session unit. Probe artifacts were cleaned up; no repo changes left.

### W14. [COMPILER/BOOT/sema.iii] s_pass2_decl param-binding loop (sema.iii:1968-1986) / s_local_push (sema.iii:791)  (sev 2)
- **Proposed:** After the [mark, SEMA_LOCAL_COUNT) param-push loop in s_pass2_decl, scan the just-pushed params for any two sharing the same source-slice name (reuse the byte-compare already inlined in s_local_lookup_src) and emit a diagnostic on the duplicate, reusing SEMA_E_DECL_DUP so no new error code / main.c name-mapping change is needed.
- **Regression:** The change adds a NEW rejection where none exists. Primary risk is the self-hosting fixpoint + the bootstrap differential gate: sema.iii is the live front-end, and the C seed reference (sema.c:1543-1549) ALSO lacks a dup-param check, so iiis-2 with this check would reject signatures the C-seed-built iiis-1 accepts. If any compiler source (.iii) or any stage1_corpus program contains a duplicate param name, self-compilation / build_iiis2 --check-corpus would break. However: (a) the differential gate checks CODEGEN byte-equivalence on well-formed corpus programs, and a new diagnostic on malformed input does not alter codegen of well-formed programs; (b) no corpus test pins the current accept-dup behavior, and the C reference omits the check by omission (no deliberate 'we allow dup params' comment) — so this does NOT regress a tested/deliberate design. Net: not regressing a pinned behavior, but must be validated by a full build + corpus 0-fail + bootstrap-fixpoint run before landing.
- **Notes:** Claims verified line-by-line. s_local_push (791) appends without dedup; s_local_lookup_src (805-826) is top-down so a duplicate param shadows the earlier binding (first becomes unreferenceable); s_pass2_decl param loop (1970-1986) pushes blindly; SEMA_E_DECL_DUP (=1, slot 1, 'TYPE-DECL-001') is wired and reusable with no new error code / no main.c name-map change. No dup-param check anywhere in COMPILER/BOOT. KEY extra finding: the C reference sema.c also lacks the check, so this is a genuine new front-end capability (standard duplicate-parameter rejection), not a port-completeness restoration. Impl is self-contained: add a post-loop O(k^2) scan over [mark, SEMA_LOCAL_COUNT) using the inlined byte-comparator (sema.iii:813-821) and emit on the duplicate; add a negative stage1_corpus case (fn f(x:u32, x:u32)) per the prove-the-negative discipline. recommend_now=false because it touches load-bearing self-host compiler code and alters the compiler's accept/reject posture relative to the C seed — needs the dedicated gated wave (full build + corpus byte-equivalence + self-compilation fixpoint) the candidate itself specifies, not a single in-session unit.

### W15. [COMPILER/BOOT/cg_r3.iii] r3_emit_expr UNARY op==5 ADDR-of-INDEX (`&arr[i]`, lines 2392-2419); scale select 2416-2417; r3_index_obj_is_byte (1915-1920)  (sev 2)
- **Proposed:** Select the leaq scale from r3_index_obj_elem_kind: ek 1/2 -> scale 1, ek 3/4 -> scale 2, ek 5/6 -> scale 4, else -> scale 8; add R3_STR_LEAQ_RCX2 and R3_STR_LEAQ_RCX4 consts.
- **Regression:** Dual-edit required and the byte-identity bootstrap gate pins it. cg_r3.iii:2416-2417 and the frozen seed cg_r3.c BOTH emit only scale 1 (u8/i8/bool) and scale 8 (default) for &arr[i]; u16/i16/u32/i32 element address-of strides by 8 (wrong). The LOAD/STORE paths are width-correct (R3_STR_INDEX_LD_W=,2) at 2203, _LD_L=,4) at 2207; ST_W/ST_L likewise) and treat %rcx as the raw element index with hardware scale doing width conversion -- so the address-of path is the lone width-blind spot, internally inconsistent with the established convention. No corpus test asserts scale-8-for-subword as correct (the 2392-2394 comment admits the limitation), so the fix is NOT regression-masking. BUT: changing only the .iii makes iiis-2 emit scale-2/4 bytes the C-seed-built iiis-1 does not -> gate reddens; the fix must mirror into cg_r3.c. CITATION ERROR: candidate (and the stale comment at cg_r3.iii:2393) cite cg_r3.c:1341-1395 as the mirror; that range is the bare-ident fn-pointer leaq + binary strength-reduction. The real mirror is cg_r3.c:1544-1596 with the scale decision at 1591-1594. An implementer following the cited range would edit the wrong C code.
- **Notes:** Gap is genuine and verified in both organs. r3_index_obj_elem_kind (1926-1968) already returns the exact 1..6 kind (plus @specialize TP-buf hook), so the proposed dispatch (ek1/2->1, 3/4->2, 5/6->4, else->8) composes an existing organ and is concrete/bounded; adding R3_STR_LEAQ_RCX2 (digit byte 50) and _RCX4 (byte 52) by cloning the RCX1/RCX8 arrays is trivial. recommend_now=false: this is load-bearing self-host codegen under the seal/byte-identity bootstrap gate and requires a frozen-C-seed dual-edit + full seal + corpus gate -- a dedicated gated wave, not an in-session unit. Implementer caveat: edit cg_r3.c:1591-1594 (NOT the candidate's cited 1341-1395) and add a corpus test exercising &u16arr[i]/&u32arr[i] (currently low/zero reachability, which is why the latent bug survived).

### W16. [COMPILER/BOOT/cg_r0.iii] r0_emit_expr EXPR_BINARY (lines 949-989) -- absence of strength-reduction / identity folds  (sev 2)
- **Proposed:** Compose the same three fold helpers (mul-pow2 -> shl, const-shift, arith-identity) into cg_r0's EXPR_BINARY prologue, byte-identical in effect to the general path.
- **Regression:** Low correctness risk, but seed-divergence risk. The only cg_r0 binary-op pin I found is cg_r0_width_gate.sh -- a RUNTIME differential gate that compiles each probe through --ring R0 (cg_r0) AND the default backend (cg_r3, already folded) and compares the XOR-fold output value; it does NOT golden-compare emitted assembly text. It currently PASSES with cg_r3 FOLDED vs cg_r0 UNFOLDED, which directly demonstrates the folds are result-equivalent to the unfolded path (x*2^k == x<<k mod 2^w; x*1/x+0/x-0 == x). A faithful cg_r0 port (replicating the u32 MOVL_EE truncate that cg_r3's fold and cg_r0's unfolded path both emit -- lines 2464/2475/2489 vs 957/963) keeps the gate green; a botched width-dropping fold would be CAUGHT by it (e.g. probe dirty_shl_shr: x<<8>>8 on a wrapping u32). The crypto gate is also runtime/absolute, not golden-text. So no tested emission behavior is regressed by adding the fold. The real hazard is the candidate's own caveat: cg_r0.iii is NOT in the 59/0 determinism gate, and a cg_r0.c seed exists -- a fold added to .iii but not byte-matched in cg_r0.c silently diverges the two backends. The cg_r0 div/mod signedness divergence (op 4/5 always IDIV vs cg_r3 DIVU selection, lines 958-959) is a SEPARATE correctness candidate, out of scope here.
- **Notes:** Claims verified against live code at C:/Users/Edwin Boston/OneDrive/Desktop/III/COMPILER/BOOT/. (1) cg_r0.iii r0_emit_expr EXPR_BINARY (lines 949-989) goes straight to emit-lhs/emit-rhs/pop-rcx/pop-rax then op-dispatch with NO peephole/strength-reduction/identity fold -- confirmed. (2) cg_r3.iii EXPR_BINARY composes the live self-optimizer: r3_mul_pow2_k dispatched at 2457-2466, r3_shift_const_k at 2467-2481, r3_arith_identity at 2482-2491 -- exactly as cited. (3) The three helpers (defined 851/867/881) are self-contained: they only use iii_ast_node_kind, R*_K_EXPR_INT, iii_ast_expr_int_u64 -- all of which cg_r0 already has (R0_K_EXPR_INT=37 at line 56; iii_ast_expr_int_u64 used at 884). Genuine island: a proven, kernel-justified self-optimizer organ in the sibling backend is not shared with cg_r0, so identical mul/shift/identity exprs emit longer code in kernel mode. Impl is concrete and bounded but slightly more than a pure organ-compose: cg_r0's R0_STR_SHLQ/R0_STR_SHRQ (lines 547/549) are %cl-form ('shlq %cl,%rax'), so the fold needs NEW immediate-shift emit constants ('shlq $' + ', %rax') plus the 3 ported helpers + a fold prologue + the u32 MOVL_EE replication + a dual-edit into cg_r0.c. recommend_now=FALSE per rubric: this is load-bearing Ring-0 KERNEL codegen (CRASH-PROTOCOL territory; a divergence here is BSOD-class), cg_r0 is outside the determinism/seal gate, and a cg_r0.c seed must be dual-edited to avoid silent backend divergence -- this needs a dedicated gated wave with kernel-KAT + width-gate verification, not a single in-session unit. Candidate itself rates it lowest-value, land-after-correctness.

### W17. [STDLIB/iii/numera/rsa.iii] rsa_pss_encode (line 131) / rsa_pss_verify_em (line 176)  (sev 2)
- **Proposed:** Parameterize EMSA-PSS over the digest (hLen + a hash/MGF function selector) so PSS works with SHA-384/SHA-512 as RFC 8017 and FIPS 186-5 require; secondarily add RSASSA-PKCS1-v1.5 sign/verify (RFC 8017 §8.2, the DigestInfo-prefix scheme still mandatory for X.509/TLS/JWT RS256 interop). (RSAES-OAEP encryption is out of this module's declared signature scope -- a separate module if needed.)
- **Regression:** Moderate-to-high and security-sensitive. Corpus 373 (sign/verify/tamper at RSA-320, sLen=0) and 1079 (keygen wrappers) pin the SHA-256 path; the literal 32/33/34 constants in rsa_pss_encode/verify_em are tightly coupled, so any parameterization must keep the SHA-256 emLen/dbLen layout byte-identical or it silently breaks PSS verification (an off-by-one in dbLen for a larger hash would not throw, just fail to verify). The secret-exponent constant-time path (RM_CT_BITS in rsa_pss_sign->rsa_modexp) is in scope. PKCS1-v1.5 *verify* is a classic signature-forgery surface (Bleichenbacher/BERserk) if DigestInfo is not parsed strictly with full-length comparison -- a security design decision, not a mechanical add. Consumers traced: only rsa_pss_sign/verify + the iii_rsa_* wrappers (which hardcode mHash=32/sLen=32) call the EM layer; no other module depends on the SHA-256-only assumption, so a correctly-gated parameterization need not regress callers.
- **Notes:** Re-read STDLIB/iii/numera/rsa.iii in full. Every cited line is accurate: PSS is hard-wired to SHA-256/MGF1-SHA-256 with literal 32/33/34 throughout, and there is NO RSASSA-PKCS1-v1.5 path (grep across numera for pkcs1/DigestInfo/v15 = 0 hits; exports are PSS-only). Genuine capability gap vs RFC 8017 / FIPS 186-5 (which define PSS over the full SHA-2 family) and vs on-the-wire RS256 interop (PKCS1-v1.5). Impl-path is concrete for the substantial part: sha512_init/update/final exist (sha512.iii:286/314/392) so PSS-SHA512 composes an existing organ, and PKCS1-v1.5 composes the existing rsa_modexp + a static DigestInfo prefix table. ONE INACCURACY: the claim that sha384 'already exist as siblings' is false -- no sha384 module exists in the tree (SHA-384 = SHA-512 IV variant + 48-byte truncation would be a new, small organ). recommend_now=false: this is load-bearing cryptographic signature code (security posture). It needs a dedicated gated wave -- FIPS 186-5 PSS-SHA384/512 + PKCS1-v1.5 KAT vectors, a Bleichenbacher-safe strict-DigestInfo verify design decision, byte-identical preservation of the SHA-256 path (corpus 373/1079), and care around the constant-time RM_CT_BITS path -- not a single in-session unit.

### W18. [STDLIB/iii/numera/mldsa.iii] iii_mldsa_sign_sealed (line 968) / iii_mldsa_verify (line 1138)  (sev 2)
- **Proposed:** Add the FIPS 204 §5.2 domain-separation wrapper: an exported sign/verify taking (ctx, ctx_len, <=255) that absorbs 0x00 || ctx_len || ctx before msg into the mu computation (and a HashML-DSA variant with the 0x01 || OID prefix), keeping the current functions as the named *_internal core.
- **Regression:** Low IF strictly additive. The cited lines are exact: sign mu=SHAKE256(tr||msg) at 993-996, verify mirrors at 1239-1242, and no exported entry (sign_sealed/sign/verify) nor the pq_dispatch router carries a ctx param. The context-free core is pinned by the C-ref cross-KAT (header L16), corpus 198 round-trip+forgery, the separately-verified keygen exact-match, and the pq_dispatch router. The proposal keeps these byte-identical and adds NEW exported wrappers, so no consumer regresses. The proposal correctly warns that an IN-PLACE edit would break the cross-KAT, hence the additive-only constraint is load-bearing and must be honored.
- **Notes:** Claims verified against source. mldsa.iii exposes only the FIPS 204 INTERNAL (Sign_internal/Verify_internal-equivalent, context-free) core; the mandated externally-interoperable ML-DSA.Sign/Verify over M'=0x00||len(ctx)||ctx||M (and HashML-DSA 0x01||...||OID) is absent everywhere (module + pq_dispatch). Even with empty ctx, a conforming external impl prepends 0x00 0x00, so this module is non-interoperable with external ML-DSA -- a real capability gap, not a memory bug. Impl is concrete and bounded: one extra absorb of a 2+ctx_len header before the msg absorb, ctx_len<=255 validation (must REJECT, not truncate), composing the already-used keccak/shake organ (mldsa_sh_init/absorb/finish); additive exports leave the internal core intact. NOT recommend_now: this is crypto/security-core conformance. It needs a dedicated security-gated wave -- (1) gate with real FIPS 204 ACVP external/context-string vectors (shipping the API without them would be an unverified crypto stub, against project standard), (2) a design decision on whether pq_dispatch grows a ctx-carrying route, (3) the HashML-DSA variant pulls in per-parameter-set OID tables. The borderline pull toward spurious is that the entire PQ stack was deliberately scoped to the internal C-ref core; but missing the FIPS-mandated external API is a legitimate, well-bounded enhancement when done additively.
