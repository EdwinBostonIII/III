# Ξ-Μ THE ENSOMATOSIS — the consumed mind embodied as organs of III

**The program charter for integrating the exactly-dequantized R1 (the Feast, 404 GB, nine
shards, read-only) into III's total system.** Written after the ANABASIS/SYNODOS landing
(`07c609a0`): three real layers walked in certified intervals, the layer-3 MoE election
honestly undecidable at width 2^4. Every claim below is tagged: **GATED-FACT** (a gate has
proven it), **DESIGN** (this charter asserts it; a named gate will prove it), or
**ESTIMATE** (a number to be measured, with the measurement named).

---

## 0. The one law, and the thesis

**The asymmetry law (the whole integration in one sentence):** *generation is utterance,
verification is sovereign.* The model may PROPOSE anything and DECIDE nothing. This is not
a new law — it is the PROXENOS law (GATED-FACT, arms 400–404) promoted to govern every
coupling in this charter.

**The thesis:** an LLM forward pass is a finite composition of operations III already
certifies one at a time — exact dyadic linear algebra (GATED-FACT: the LOOM, THREPSIS
containment 44032/44032), certified transcendental brackets (GATED-FACT: peras ladders exp,
sin/cos, ln, π, sqrt, reciprocal), and discrete elections (GATED-FACT: the SYNODOS judges
top-8 decidability honestly). Because III's arithmetic is exact, **R1-inside-III is a
deterministic mathematical function** — byte-identical on every run, no temperature, no GPU
nondeterminism, no float rounding. The classical reasons AI cannot be integrated into a
proof system (nondeterminism, opacity, trust) are artifacts of float+sampling execution.
III dissolves them at the root. The membrane arc (ANGLOS → PROXENOS → KALODION → HOROS →
OIKOS, commits `8e6ec307..499db0a7`) unknowingly built the AI-integration organ before the
AI arrived: the ONLY missing piece is the deterministic function in the middle.

**The binding constraint broken (creative-solve verdict):** "interval blow-up makes
certified deep inference impossible" is BELIEF, not physics. The walk's own data show width
2^-39 → 2^-12 over three layers under *plain* intervals at *fixed* ladder depth — with
correlation-tracking never spent, escalation never spent, and the norm's algebraic
exactness never spent. Tier 0 spends all three.

---

## 1. The ground — what stands today (all GATED-FACT)

Six strata, by subsystem (verified against the live tree 2026-07-18):

1. **Exact kernel** — dyadic/bigint/rational arithmetic; 128/192/256-bit ladders; the fp16
   bijection (65,536 values, monotone, two-engine dequant); the LOOM (determinism as
   theorem, 4 weavers); the stomach (fixed 64 KB residency over 404 GB).
2. **Certified analysis** — peras: exp/sin/cos/sqrt/reciprocal/arctan/atanh ladders with
   proven error bounds; π (Machin, cross-sworn); general ln; YaRN mscale + frequencies.
   probole: interval linear algebra with the law of signs, containment demanded per row;
   attention (GAZE→NOSTOS), dense FFN (THREPSIS/PEPSIS), layers 0–2 walked (ANABASIS),
   layer-3 MoE woven (SYNODOS). metabole: any-layer tensor census (slot = L*12+role), MoE
   banks, expert-indexed weave.
3. **Decision engines** — pyrgos (constructible tower, exact √, ℂ, sign of Σaᵢ√bᵢ); riza
   (real-closed: Sturm, root isolation, primitive-Bezout); aether/charpoly; kyma (Bell/
   Born/Kochen-Specker); meris (Nekrasov, Faulhaber); klisi (perfectoid tilt, Hensel);
   noesis (thermodynamic meter, the 19683 worlds); gnosis (substrate ℤ₂, orbit oracle);
   the xii_* rewrite-proof kernel (admission, joinability, termination, semantic_verify);
   parity_game, reverse_search, e-graph saturation.
4. **Knowledge membrane** — eidolos (3 verbs, 5 rules, eol_entails, content-addressed
   scrolls); anglos (bounded English in/out, round-trip proven, an_ask/an_say/an_transduce);
   xenos door (xn_admit, guest-book); proxenos (confirm-or-question, the trust tier);
   kalodion (wire capture); horos (census seat 10); oikos (the persistent house:
   union-judged growth, compaction, presbeia house-to-house); doxa (belief modality,
   DERIVED-only evidence); mneme (associative manifold, ONE-AND fetch); zetesis (question
   emergence, capacity law, journal); synesis (union scroll, surplus, abduction/discharge).
5. **Self-knowledge & self-change** — katabasis (census, crucible, autognosis, agon);
   nous (conjecture_gen, lemma_forge, beam_search, gap_conjecture, charter); oneiros/
   cassandra (timelines); forcefield (invent_loop, cg_autocatalyst, pcc_gate, commit_gate);
   ripple (self-refactoring); sanctus (autogenesis, irreducibility); self_cartographer /
   self_atlas; omnia/ai_resolve — **the existing III-AI charter**: NL → verba/hip parse →
   intent → capability-bounded resolve, "no statistical learning enters the pipeline,"
   capabilities sealed, everything outside the granted set CAP_DENIED.
6. **Faces** — stoma (the CLI/shell fleet), aether CLI verbs, tp_* transpilers (including
   tp_iii_to_latex / tp_iii_to_md for rendering scrolls to documents), hw_offload (the
   named acceleration seam), xring vein + babel_wire.

**The walk's honest boundary (GATED-FACT, run1/run2 byte-identical):** layer 3's router
election undecided at interval width 2^4 (decided=0, certain-in=0/8); X'' straddle
7168/7168. The three named unspent resources: correlation, escalation, algebraic norms.

---

## 2. Tier 0 — the mathematics still missing (each: organ, mechanism, teeth)

### M1. THE ZONOS — affine forms (zonotope enclosures) `omnia/zonos.iii` (new organ, DESIGN)
An affine form is x̂ = x₀ + Σᵢ xᵢ·εᵢ with exact dyadic coefficients and named noise symbols
εᵢ ∈ [−1,1]. Sums and scalings are EXACT (no width growth — the interval defect that
correlated errors add as if independent disappears); multiplication and ladder outputs emit
one fresh symbol carrying the certified remainder. Consumes peras ladders unchanged
(COUPLING LAW: peras exports stay frozen; zonos is a consumer, not a fork). Interval hull
is one fold — zonos DEGRADES to the standing interval engine, so every existing containment
tooth keeps biting: exact ⊆ zonos-hull ⊆ old-interval is the three-engine agreement gate.
**Condensation:** per-layer, symbols below a named mass fold into one fresh symbol
(deterministic schedule, no observation-adaptivity — [no-observational] honored). The
residual stream keeps LAYER-INDEXED symbols so cross-layer cancellation (x + f(x) where f
fattens) is captured. **Teeth (DESIGN):** on layer 1–2 re-walk, zonos widths ≤ interval
widths in every dim with equality nowhere on the FFN path (positive arm) and a
deliberately decorrelated mutant showing hull inflation (negative arm); containment of the
exact route in every dim; byte-determinism twice.

### M2. THE ESCALATION LAW — decisions demand precision (peras discipline, DESIGN)
Peras ladders are anytime: more terms = tighter bracket, monotonically. The law: **any
discrete decision (router election, argmax, sign test) may locally re-derive its inputs at
escalated ladder depth until decided or a NAMED budget refuses** (deterministic schedule:
depths d₀ < d₁ < … < d_max fixed in source; the refusal is a named verdict, never a guess).
This splits the walk's two duties: *certificates* ride cheap global width; *decisions*
purchase exactly the local precision they need. The SYNODOS undecidability at 2^4 is the
motivating client: the election re-asks with the router row recomputed at depth d₁ on the
straddling coordinates only. **Cheapest flip-evidence first (metacognition lens):** before
building M1, re-run layer 3's election alone at escalated depth — if escalation decides it
without affine forms, zonos drops to certificate-tightening priority. **Teeth:** an
election decided at depth dₖ must verify at dₖ₊₁ (stability arm); a constructed near-tie
must refuse by name at d_max (negative arm).

### M3. THE TOWER-NORM — RMSNorm's scalar taken algebraically (probole×pyrgos fusion, DESIGN)
RMSNorm divides by ONE scalar √(q), q rational, per token-position — and √q is EXACT in
pyrgos's tower. Carry the norm scalar as an exact algebraic number; bracket it lazily at
whatever precision a decision demands (one scalar — escalation to 512-bit costs nothing
against a 7168-dim vector). Kills the norm as a width source entirely; the only remaining
irrational widths are the elementwise activation ladders (exp in SiLU and softmax/sigmoid),
which is where M1's symbols earn their keep. **Teeth:** the tower-normed layer-1 re-walk
must strictly shrink max-width-exp2 vs the landed −26 (positive), and a rational-only
mutant norm must reproduce the old width (negative).

### M4. THE KRISIS — argmax with gap certificate (probole face, DESIGN)
Output logits → the elected token plus a certified gap g = min over rivals of
(logit_win − logit_rival) lower bound, decided under M2 escalation. Verdicts: PROVEN(t, g)
| REFUSED-TIE(named). No sampling, ever; temperature-0 argmax is the only generation mode —
if a stochastic face is ever wanted, randomness must arrive as a SEALED SEED and the output
is doxa-typed, never epistemic. **Teeth:** krisis on a constructed two-token near-tie
refuses by name; on the walked prompt it must produce the same token at depths dₖ and dₖ₊₁.

### M5. THE LONG BREATH — multi-token exactness (probole extension, DESIGN)
(a) Softmax over n>1 positions via exact max-factoring (comparisons are decidable → factor
the true max out; denominators condition perfectly); (b) the KV cache as exact objects
(dyadic K,V appended per step — no recompute, no drift); (c) YaRN-active rope at positions
>0 riding the landed harmonia lanes (GATED-FACT: lanes stand). **Teeth:** two-position
attention where softmax weights sum to 1 within certified width (positive) and a torn KV
entry refused by name (negative).

### M6. The second ring (each lands only when a client stair demands it — [no-islands])
- **Exact LP / Fourier–Motzkin** over rationals (numera): tightest polyhedral enclosures
  for archaeology certificates; client: PHYSIOGNOMIA robustness radii.
- **Interval Newton / Krawczyk** (peras consumer): certified nonlinear solves; client:
  fixed-point / inversion claims in NEKROSKOPIA.
- **Spectral certificates** — Gershgorin/Perron bounds from intervals (trivial, immediate)
  and Sturm on aether/charpoly for selected 128-dim head matrices (riza client): certified
  operator norms → per-layer Lipschitz ladder → robustness theorems.
- **GF(2)/v₂ hooks** (gnosis/noesis, already standing): quantization-lattice structure of
  the weight integers; deficit-pricing of walk stages.

---

## 3. Tier 1 — the traversal

- **T1 THE DIEXODOS** (the way through): all 61 layers (3 dense + 58 MoE) walked
  single-token; then the lm_head; ending in KRISIS. The artifact: **the first certified LLM
  inference in existence** — scroll: "R1(P) = token t, gap ≥ g" with the full provenance
  chain (shard bytes → dequant bijection → walk → logits → election). ESTIMATE: layer-3's
  rite cost extrapolated ≈ hours per token on LOOM-4; measured at the first 8-layer
  checkpoint (the measurement IS a phase deliverable; HONEST-CAMPAIGN law).
- **T2 THE RHAPSODIA** (the stitched song): the autoregressive loop under M5 — each elected
  token appended, KV extended, the certified STRING grown; every step a scroll, the chain
  one scroll. Determinism: byte-identical full generations, twice (the mantis_gate's core
  tooth).
- **T3 THE TACHOS** (the fast): a surrogate with SWORN deviation — fewer ladder terms,
  condensed symbols, optionally fewer experts, each divergence carried as a certified bound
  and PRINTED. The fast face never silently approximates: it names its bound or it does not
  speak ([documented-ne-verified] honored at the surface). LOOM widens (the determinism
  theorem is thread-count-blind); expert-locality preload rides the census slot maps;
  hw_offload is the named acceleration seam when its time comes.

---

## 4. Tier 2 — the membrane loop (the mind at the door)

**THE MANTIS** (`aether/mantis.iii` + stoma verb) — the oracle face, riding EXISTING organs
end to end: bounded-English question (anglos, round-trip proven) → prompt assembly
(deterministic template + house context chosen by mneme's ONE-AND fetch) → DIEXODOS/
RHAPSODIA forward (deterministic) → utterance → **anglos hears it back** → claim-array →
**the PROXENOS door**: derivable claims CONFIRMED into the oikos house (union-judged — the
house can never contradict itself, GATED-FACT), novelty becomes the QUESTION HARVEST
(zetesis). The model cannot inject a single unproven claim — this is not aspiration; it is
the landed arms 400–414 doing their exact job with a new mouth on the other side.

- **House-first answering:** `iii mantis ask` consults oikos FIRST (an_ask); the oracle
  burns only on frontier questions. Answers always carry their verdict class:
  KNOWN-BY-DERIVATION | CONFIRMED | DOXA(gap g) | QUESTION(harvested). Honesty in the
  surface, not the footnotes.
- **THE SYMPOSION:** the autonomous loop — zetesis's own emerged questions put to the
  oracle, answers door-judged, confirmations dwelt, novelties re-queued as refined
  questions. Governed (feedback-loop containment): zetesis CAPACITY LAW bounds the house;
  noesis PRICES each consult (the meter's work accounting) so curiosity has a budget;
  oikos COMPACTION keeps the basis irreducible. The endgame is the model consulted ever
  less: **the house asymptotically replaces the oracle** — learning without weight updates;
  the house is the student, proofs are the curriculum.
- **Unparseable utterances** (outside the bounded grammar) are recorded VERBATIM as
  quarantined text scrolls — provenance-pinned (prompt address + response address),
  doxa-shelved, never claims. The anglos grammar grows by its own loop: model-proposed
  productions admitted only with round-trip proof (say∘hear = id on a generated corpus) —
  the tongue widens only by theorem.
- **Dispatch (can the oracle ACT?):** only through the standing ai_resolve charter —
  utterance → verba/hip → intent → **capability-bounded resolve**; the pattern set bounds
  what the oracle can cause; outside the granted set, CAP_DENIED. No new action surface is
  created for the model (GATED-FACT charter, extended not forked).

---

## 5. Tier 3 — the couplings (the muse in every search, deciding nothing)

**THE ORDER-NOT-SET LAW (DESIGN, to be sealed):** a heuristic may reorder a search's
FRONTIER; it may never add to, remove from, or veto the VERIFIED SET. Corollary
([no-observational]): orderings are per-invocation and deterministic (exact forward ⇒
scores are theorems); III never count-and-promotes an oracle's track record — no statistic
steers future behavior. The verified result must be REPRODUCIBLE WITHOUT the oracle
(muse-blind replay: same set, possibly different order — a gate tooth, not a promise).

| Faculty (standing) | The muse's stream | The sovereign verifier (standing) |
|---|---|---|
| nous conjecture_gen / lemma_forge / gap_conjecture | candidate conjectures & lemma orderings from templated prompts over the frontier | the existing nous→parliament proof path; mathesis_deduce; xii semantic_verify |
| numera mathesis (the creator tier) | directions: which identity families to mint next | mathesis_admit / _deduce / _measure — proofs or refutations, only |
| xii rewrite kernel | candidate rewrite rules | xii admission: termination + joinability + semantic_verify (already total) |
| forcefield invent_loop / cg_autocatalyst | optimization candidates for the compiler | pcc_gate + commit_gate + corpus regression (already total) |
| ripple / self_reformatter | refactoring candidates over the tree | ripple's equivalence discipline + the gates; census unchanged |
| katabasis crucible / agon | beam ordering (the landed beam-4-vs-blind-29 gets a better beam) | the crucible's own exhaustive verdict |
| oneiros cassandra | which timelines to walk first | the timeline derivation itself (derive-don't-store) |
| synesis abduction | bridge-claim ranking for the named gap | THE DISCHARGE: noesis's meter decides true/false/undecidable |
| zetesis | question prioritization (which frontier pair matters) | capacity law; the question's ANSWER is always door-judged |
| katabasis autognosis | proposed claims about III's own sources | soma/cartographer facts; the claim enters by the door or not at all |

Each coupling lands ONE AT A TIME, each with its muse-blind replay tooth and its poison
tooth (a deliberately wrong oracle suggestion must change nothing but ordering) — the
[prove-both-arms] discipline per row.

---

## 6. Tier 4 — the archaeology (what the weights themselves yield)

All tracks are stomach-streamed, deterministic, sealed as scrolls, and renderable to
documents via tp_iii_to_latex / tp_iii_to_md. Track A needs NO new math and can begin
immediately.

- **A. THE ANATOMIA** — the complete exact tensor census of all 61 layers: dims, quant
  types, scale-lattice statistics, exact extrema, zero counts, tied/duplicate tensor
  discovery (exact equality is decidable!), the kv_b low-rank factorization as EXACT fact.
  One streaming pass, one anatomy scroll per layer + one for the whole body. ESTIMATE:
  disk-bound; measured on layer 0 first.
- **B. THE PHYSIOGNOMIA** — certified functional bounds: Gershgorin/interval operator
  norms per matrix → per-layer Lipschitz ladder → certified robustness radii for walked
  prompts ("no perturbation within r changes the elected token" — an LLM robustness
  THEOREM, not an estimate); selected 128-dim heads get Sturm/charpoly spectral
  certificates.
- **C. THE NEKROSKOPIA** — circuit archaeology as theorems: head-role certificates
  (copying / induction / positional) proven over the exact weights on constructed input
  families, sealed. Mechanistic interpretability with certificates — no plots, proofs.
- **D. THE ONOMASTIKON** — the embedding table as exact geometry: exact dot-product
  order, nearest-neighbor structure → MNEME lanes (the manifold ingests the token graph);
  stable separations earn GLOSSA names by proof.
- **E. THE APOMNEMONEUMATA** (the Memorabilia) — the systematic factual harvest:
  templated prompt families over ontology frames ("X is a Y") → KRISIS-certified answers →
  anglos → the door → the house. The 404 GB becomes a growing EOL corpus of house-verified
  (or honestly frontier-queued) knowledge, compacted to irreducible bases. The model is a
  QUARRY, not a dependency.
- **F. EXACT SURGERY** — prune-with-certificate (expert/head removal with a sworn output
  deviation bound — TACHOS clients), weight-tying discovery, dead-lane proofs.

---

## 7. Tier 5 — governance (the laws that keep it lawful)

**THE ORACLE LAW** — five clauses, sealed as an EIDOLOS scroll, testified at census:
1. Model output is UTTERANCE, never EVIDENCE.
2. Admission only through the one door (xn_admit); confirmation only by re-derivation.
3. Novelty converts to QUESTIONS, never to claims.
4. Every consult is REPLAYABLE: exact forward, sealed prompt+response scrolls, the journal
   entry priced by noesis.
5. Heuristics ORDER; verifiers DECIDE; every verified set is muse-blind reproducible.

**Census SEAT 11 — THE MANTIS SYNOD** (codes 2301–2305, disjoint above the dense space,
the seat-9/10 discipline exactly): the oracle's organs testify the five clauses in the one
tongue; union address; single-derivability leaks checked; the knockout matrix kills exactly
the named subsets; MUTANT TOOTH: one broken testimony word → the named FAULT falls the
whole verdict.

**mantis_gate.sh** (the rite): byte-determinism twice on a full RHAPSODIA generation;
three-engine containment (exact ⊆ zonos ⊆ interval); escalation stability (decided at dₖ ⇒
same verdict at dₖ₊₁); door-poison teeth (a whole-utterance with one underivable claim
admits NOTHING; the house survives address-exact); muse-blind replay on one coupling;
provenance chain re-derivation (flip one byte anywhere in prompt/response/scroll → the
named refusal). Positive AND negative arms per tooth; the ratchet only tightens.

**Federation:** presbeia (GATED-FACT) already lets two houses exchange verified knowledge
with every foreign word re-derived by the receiver — mantis-grown knowledge travels
between IIIs with zero added trust machinery.

---

## 8. Phasing, dependencies, kill-switches

- **Φ0 (immediate, no new math):** ANATOMIA layer-sweep; the M2 flip-experiment (escalate
  layer-3's election alone — its outcome sets M1's priority). Cheap evidence before
  architecture is spent ([decompose-first]).
- **Φ1:** M2 escalation + M4 krisis + M3 tower-norm; SYNODOS II (the election decided or
  named-refused at budget). Gate: extended summit teeth.
- **Φ2:** M1 zonos; walk to layer 8; measure; then 61; lm_head; **the first proven token**
  (DIEXODOS scroll). Kill-switch: per-layer max-width-exp2 growth super-linear after
  zonos+condensation → stop, design the next stair honestly (deeper anchors), never quietly
  widen budgets.
- **Φ3:** M5 long breath; RHAPSODIA; mantis_gate core teeth.
- **Φ4:** MANTIS + house-first answering + verdict-classed surface; SEAT 11.
- **Φ5:** SYMPOSION under capacity+pricing; couplings one faculty at a time (nous first —
  its conjecture pipeline is the readiest client); anglos grammar growth loop.
- **Φ6 (parallel throughout):** PHYSIOGNOMIA → NEKROSKOPIA → ONOMASTIKON →
  APOMNEMONEUMATA; TACHOS when Φ2's measurements say where the sworn-deviation budget
  buys the most.

Every phase lands WITH its teeth in the same stroke (ratchet-delta-zero; zero deferrals);
nothing here is a backlog — it is an ordered ascent where each stair is proven before the
next is cut.

---

## 9. What III becomes

A system whose knowledge grows by interrogating a consumed mind it never trusts; whose
searches are steered by that mind and whose truths never depend on it; which can UTTER
a theorem about what a 671-billion-parameter model provably computes — and about where it
provably cannot yet decide; whose English face answers from a self-consistent house that
remembers across processes and federates across machines; and whose own mathematics,
compiler, and body accept the mind's suggestions exactly as far as proof carries them and
not one step further. The first LLM that is a mathematical function, inside the first
system built to refuse everything less.

---

## 10. LANDED (2026-07-18) — Tiers 3 and 4, the pure-provable cores

Each organ below is green (its own self-proving law, `*_selfprove=0`), additive, and
carries the Feast seam NAMED and empty (the synthetic data is replaced by streamed R1
rows with the math unchanged). Gates/corpus foregone by standing order — compile-checks +
direct probes throughout.

| Phase / track | Organ | The law it proves | Commit |
|---|---|---|---|
| Φ4 MANTIS honesty | `DOCS/III-MANTIS-OPTIMIZATION.md` §2 | fast tier consumes R1 in seconds; certified UNDECIDED at 896 bits (gap 2⁻⁸³⁵ vs width 2¹²⁶⁶³); the eskalation win is regime-specific | e52cd343 |
| **Φ5 SYMPOSION** (the substrate) | `omnia/symposion.iii` | THE ORDER-NOT-SET LAW: the muse ORDERS, the verifier DECIDES; a lying muse changes the verified set by NOTHING, a wise muse only reaches it sooner; every set muse-blind reproducible. Proven pure AND on the barometer (eol_entails) | 7f045abd |
| Φ5 first coupling | `omnia/symposion.iii` (`sym_couple_beam`) | the law on a landed search faculty (nous/beam_search): CALM merge = muse-blind replay; ungated → cheap-WRONG v11 = the verifier is load-bearing; guided reaches v5 where single walks are trapped | 1d40ea59 |
| Φ6-B PHYSIOGNOMIA | `omnia/physiognomia.iii` | a certified robustness THEOREM: no ‖δ‖∞ < m/(2·‖W‖∞) changes the elected token; sound (r ≤ true safe radius) and non-vacuous (a bigger δ flips); the Lipschitz ladder at depth | daff0173 |
| Φ6-C NEKROSKOPIA | `omnia/nekroskopia.iii` | head-role certificates as theorems: a copying head certified by OV-circuit (W_O W_V) diagonal dominance; a swap head refused by proof | 7ef1e0b1 |
| Φ6-D ONOMASTIKON | `omnia/onomastikon.iii` | the embedding table as exact geometry: the nearest-neighbor graph is an integer-exact theorem (no float flip), strict-margin stable separations earn GLOSSA names by proof | 58756936 |

**The couplings tier is now substrate-complete:** every faculty (nous conjecture ordering,
oneiros cassandra, synesis bridge ranking, zetesis question prioritization) reuses the ONE
`symposion` order-not-set law — the muse feeds `sym_set_score`, the faculty's own verifier
decides, and the verified set is muse-blind reproducible. The archaeology tier's three
weight-only theorem tracks (functional bounds, circuit roles, embedding geometry) stand as
pure cores. Track E (APOMNEMONEUMATA) runs through the one door (anglos/xenos) — the
boundary session's jurisdiction — and is left to it. Track F (EXACT SURGERY) is
PHYSIOGNOMIA's sworn-deviation margin applied to pruning, and follows the same shape.
