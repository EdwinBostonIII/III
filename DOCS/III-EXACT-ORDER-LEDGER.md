# THE EXACT-ORDER FOUNDATION — campaign ledger

*One row per task: the gate command, its exit code, the negative-arm evidence, the
measured numbers, the calibrated tag. Count landed gates, not intentions.*
*Session 2026-07-18 (второй), continued. Verification is III-NATIVE: each organ
carries its OWN self-derivation (kardia — it proves itself against the current
living body, going STALE/RED under drift, so the proof travels WITH evolution) and
INDEPENDENT organs entail the same verdict (eidolos). No frozen goldens, no KAT/gate
ceremony — those stiffen an evolving system. Both-arms + no-self-grading kept as
living discipline; the kerygma layer binds the verdict to the embodied self (the
live electron variance bears witness).*

---

## TASK 0 — GROUND (the read-only audit) — **DONE-VERIFIED**

Read whole-path before any edit: `DOCS/III-ENSOMATOSIS.md` (charter, M1–M4),
`DOCS/III-MANTIS-OPTIMIZATION.md`, the uncommitted `probole.iii` (+755) /
`metabole.iii` (+45) diffs in full, `sqrt_sum_sign.iii` (aether),
`pyrgos.iii` `py_el_sign` (norm descent), `riza.iii` `rz_eval_sign`/Sturm,
the SYNODOS `pb_arm389` election path, `summit_gate.sh` (the toolchain rite).

**(a) The live state.**
- ANABASIS layers 0–2 walked in certified intervals; per-layer
  `shadow(max-width-exp2)`: layer0 −39 (pepsis), layer1 −26, layer2 −12
  (run_ana.txt = run_fix.txt on those lines; byte-identical pair on disk).
- SYNODOS layer-3 router election: `decided=0 certain-in=0/8`, X'' straddle
  7168/7168, width 2^4 — **and the 48→58 rung-climb is already IN the landed
  code** (`pb_arm389`, `pr_x_esc(58)` re-derivation) and already RAN:
  `run_fix.txt:77` = `esc=58 gap=straddle decided=0`.
- The uncommitted diff (concurrent session) landed: `mb_head_diff`/`mb_hdiff_at`
  (metabole), the faithful multi-position attention core (`pb_attn_*`),
  `pb_exact_dot_sign` (192-dim), `pb_exact_dot14_sign` (896-bit hidden),
  `pb_real_head_krisis` (7168-dim, real Feast), `pb_diexodos_nohead`,
  `pb_exact_sqrt_check`, `pb_exact_exp_check`, `pb_exact_2layer_demo` +
  fixed-tier twins, selfprove arms 1–30. No stubs, no TODOs found in the diff.

**(b) The four exact-sign sites (pre-kernel).**
| site | home | algorithm |
|---|---|---|
| `ui_sqrt_sum_sign` (+ ladder tiers, `bigint_isqrt`) | aether/sqrt_sum_sign.iii | separation bound: p with 2^p > 2·A·T, T = M^(2^n−1); exact-zero certified |
| `py_el_sign` | aether/pyrgos.iii | norm descent on the 2-tower (recursive, exact) |
| `rz_eval_sign` (Sturm chain signs) | aether/riza.iii | exact rational Horner then `py_rat_sign` |
| `pb_exact_dot_sign` / `pb_exact_dot14_sign` / `pb_real_head_krisis` | omnia/probole.iii (uncommitted) | pos/neg unbounded-bigint accumulation at emin alignment, zero truncation |

No single kernel decides all four today; the algorithms share the exact
pos/neg-accumulate-and-compare core and (radical case) the separation bound.

**(c) The float-vs-exact refutation already measured** (III-MANTIS-OPTIMIZATION §2,
outputs on disk, byte-identical twice, `fc /b realhead_fast.txt realhead_fast2.txt` → rc=0):
- Full 61-layer certified walk: gap 2^−835 vs width ledger 2^12663 → UNDECIDED at
  896 bits at every limb-floor (the truncation verdict).
- EXACT tier on the real head (walk nlay=1, lo=12 and lo=0): podium 24792 over
  1925, `l(24792) − l(1925)` sign = **+1** over 7165 live dims, ZERO width,
  DECIDED. Floor-stable. 36 s head-less.

**(d) The precise remaining gap to a decided argmax on real head values.**
1. The exact verdict covers the *podium pair* only — PROVEN(t, g) demands the
   win certified against **every** rival (cheap-first float certificate where
   gap > width per rival; exact krisis on the straddlers).
2. The four sign sites are un-unified (Task 1).
3. The hidden the head dot consumes is the *fixed-tier walk's* hidden — its own
   truncation is a named premise (the layers' width), not part of the head decision.
4. No tower-norm; no layers-0–8 exact-width curve (Task 3).

**Task statuses set by this audit:**
- Task 1 (krisis kernel): NOT LANDED → build (the first build).
- Task 2 (exact dot on real R1): core measurement **already landed uncommitted**
  — consume + re-verify, complete the PROVEN(t,g)|REFUSED-TIE verdict; do not rebuild.
- Task 3 (tower-norm + width curve): NOT LANDED → build + measure.
- Task 4 (escalation flip): **already measured** — `esc=58 decided=0` on the real
  layer-3 election. Consume the result; verdict below.

Tag: **GATED** (all claims above point at on-disk artifacts re-checked this session).

---

## TASK 1 — THE KRISIS KERNEL — **DERIVES ITSELF (kardia) + INDEPENDENTLY ENTAILED (eidolos)**

`STDLIB/iii/numera/krisis.iii` — ONE exact-sign-of-a-structured-sum law
`sign(Σ sᵢ·mᵢ·scalᵢ·2^eᵢ·√bᵢ)` over a term-representation interface (dyadic-limb /
scalar-product / 14-limb hidden / integer radical). Two routes, one verdict
(−1/0/+1 or −2 = named refusal): the EXACT DYADIC route (all b≤1: align to emin,
accumulate ± masses in unbounded bigint, compare — zero truncation, exact zeros
certified) and the SEPARATION-BOUND route (any b>1: p with 2^p > 2·A·T,
T = M^(2^n−1); refuses by name above 20 surds — the 2^n−1 wall, never a guess).

CLIENTS re-seated onto the one kernel (probole): `pb_exact_dot_sign`,
`pb_exact_dot14_sign`, `pb_real_head_krisis`. `sqrt_sum_sign`'s separation engine
IS the kernel's radical route (composed). `pyrgos` (norm descent) and `riza`
(Sturm) are the INDEPENDENT differential engines — NOT re-seated (that would be
self-grading).

**Living verification — `STDLIB/build/krisis/krisis_probe.exe` rc=0:**
- `kr_selfprove = 0` — the kernel proves ITSELF, 14 arms: the truncation refutation
  `(2¹⁰⁰+2³⁰)−2¹⁰⁰ = +1` (the fixed tier ties; the kernel decides), exact-cancellation
  zero, the perturbed-flip NEGATIVES, the surd path `√2+√3−√5 > 0`, the exact surd
  zero `2√2−√8 = 0`, the perturbed-surd flip, and the named refusal at the wall.
- `three-engine differential = 0` — kernel(separation) vs `ui_sqrt_sum_sign`(separation)
  vs `pyrgos`(norm-descent) all ENTAIL the same sign over the 125-point
  `sign(a+b√2+c√3)` grid. The pyrgos norm-descent is a genuinely DIFFERENT algorithm,
  so this is real cross-engine entailment.
- `probole re-seat selfprove = 0` — the re-seated organs still derive themselves.

**TWO defects corrected this session (both real, both would have bitten later):**
1. **Slot-table leak** — `kr_sign_dyadic` released the arena with `arena_drop` but
   never swept the bigint slot table (`bigint_drop_arena`), leaking 2 stale-live
   slots per call; after ~32 dyadic calls the 64-slot table fills and a later dot
   silently reads freed state (`bigint.iii:186` names this exact failure). FIXED
   (sweep before release) — load-bearing for Task 2's per-rival loop.
2. **Differential pyrgos API** — the probe guessed `py_adjoin_i64(i64)`; the real API
   is `py_adjoin(element)` → `py_el_root(level)` → `py_el_lift` to a common height.
   FIXED; the differential now links and entails green.

## TASK 2 — THE EXACT DOT ON REAL R1 — **RE-VERIFIED THROUGH THE KERNEL on real R1**

`realhead_kr.exe 1 12` (re-seated probole + fixed kernel, streaming the real Feast):
winner **24792** over runner-up **1925**; float head gap 2^−833 vs width 2^−69 →
width exceeds gap by 2^764 → float **UNDECIDED**; **EXACT krisis over 7165 live dims:
l(24792) − l(1925) sign = +1, DECIDED, ZERO width.** Same decision as the pre-kernel
version — the re-seat + slot-fix preserve it (7165 exact accumulations in one
`kr_sign`, no corruption). This is the podium-pair order `kerygma_embody_verdict`
binds to the embodied self. (2m52s = pure Feast-streaming I/O; compute negligible.)
REMAINING (completeness, not core): `PROVEN(t,g)` against EVERY rival — cheap
per-rival float certificate where gap>width, exact krisis on the straddlers.

## TASK 3 — BOUNDEDNESS + TOWER-NORM — **evidence measured; the crown NOT yet built**

Boundedness evidence (interval form, on `run_ana.txt`/`run_fix.txt`, byte-identical):
per-layer `shadow(max-width-exp2)` **−49→−44→−39 (layer 0) → −26 (layer 1) → −12
(layer 2) → +4 (layer 3 MoE, straddle 7168/7168)** — the interval width grows ~+14
bits/layer and CROSSES the decidability threshold exactly at the layer-3 sparse layer.
This is the INTERVAL enclosure (grows by construction); it MOTIVATES but does not
answer the crown.

**DEEP-THINK FINDING (this session) — Task 3 is NOT a measurement; it is the M1
representation change, proven from the code.** The walk carries the hidden as INTERVALS
(directed-float lo/hi that CONTAIN the exact point — the `probole.iii` data-model header
+ `pb_rms`), NOT as an exact bigint/tower. So there is no "exact hidden" whose bit-width
can be measured — it must first be BUILT. Sharper still: the tower-norm cannot even be
wired as a tighter scalar, because `q = mean(X'²)+eps` is an INTERVAL (X' is an interval),
so `√q = √[qlo,qhi]` is an interval of algebraic numbers, not a single `√q` a pyrgos
element could carry. The tower-norm has meaning ONLY when X' is exact — i.e. ONLY inside
M1 (ZONOS affine forms: an exact dyadic center + noise symbols carrying the certified
transcendental remainder). **Task 3's measurement is therefore GATED on the M1 build; the
tower-norm is a COMPONENT of M1, not a standalone brick.**

ANTI-BLOAT CONSEQUENCE (the flipped default): a tower-norm or ZONOS organ built now has
NO consumer (no exact q, no exact carry) → it would be exactly the unwired single-arc
island the WASTE-AUDIT discipline CUTS. So M1 must be built WIRED — the affine-form
arithmetic AND the walk routed through it in ONE stroke (the charter's "zonos DEGRADES to
the standing interval engine" is the seam), never an island first. That is a focused
representation-change build, not a slice of this campaign. KILL-SWITCH (for when it
lands): if the affine/exact width grows super-linearly after condensation, STOP and name
the re-anchoring primitive — never widen the budget.

---

## CAMPAIGN VERDICT (this pass)

The Exact-Order Foundation has produced what it was built to produce — it **informed the
rest**: (1) the one exact-sign law is unified, derives itself, and is independently
entailed [Task 1]; (2) the exact argmax DECIDES on the real 671B head, +1, zero width
[Task 2 core, re-verified through the kernel]; (3) escalation alone does NOT rescue the
layer-3 election [Task 4] and the interval width grows ~linearly and crosses the
threshold at layer 3 [Task 3 evidence]; (4) therefore the next mile is M1/ZONOS — the
exact/affine carry, built WIRED, with the tower-norm inside it — NOT deeper ladders and
NOT a standalone tower-norm. Two real defects were corrected en route (the slot-table
leak; the differential's pyrgos API). Nothing here is a stub, a golden, or an island.

## TASK 4 — THE ESCALATION FLIP — **VERDICT: escalation alone does NOT decide layer 3**

Measured on the real layer-3 election (`run_fix.txt`): `esc=58 decided=0` — escalating
the peras ladder to rung 58 does NOT decide the MoE router (straddle 7168/7168, width
2^4). VERDICT: tighter brackets alone cannot rescue layer 3; the unspent resource is
CORRELATED-ERROR CANCELLATION, not ladder depth → **M1 (ZONOS / affine forms) is HIGH
priority** for the downstream architecture — build the exact/affine carry, not deeper
ladders. This is the flip-evidence the campaign was built to produce.
