<!-- III GAP ANALYSIS — generated 2026-06-19 by a 12-lens read-only ultracode workflow (65 agents, 4.35M tokens,
     every high-impact finding adversarially verified). Read-only deliverable; informs the continuous improvement loop. -->

# III Gap Analysis (state vs. opportunity)

## 1. System state in one paragraph

III is a self-hosted, KAT-gated compiler/runtime whose recent arc produced three nearly-complete-but-under-cashed capability strata. The **weave** dissolved five borrowed crypto primitives into one proven shared substrate (`weave_blocks`: `wvb_rotl32/64`, `wvb_arx_mix`, `wvb_ch/maj`, `wvb_gf8_mul`) now load-bearing under SHA-2/Keccak/ChaCha/Blake2, with `primweb` supplying the SAT-proven structural identities (rotation duality, Ch-as-mux, Maj-as-carry-save) behind those atoms — this layer is genuinely wired and done. The **invention/Rosetta stack** (`invent.iii` de-novo forges, `present.iii` naming, `weave_forge.iii`, six-valued `logic6`) is feature-complete and SAT-proven (KATs 1748-1764) and was just cost-grounded against the real microarch model (`gil_cycle_validate`, commit e4dc2a67) — but it runs in KATs and `weave_forge` only; the live optimizer (`sov_isa_rules`) and live codegen (`cg_r3`) still hard-wire a single shift-combine case and emit shifts only for exact powers of two. The **cost substrate** is consolidated onto one authority (`cost_lattice`), and **Montgomery arithmetic** is consolidated onto one organ (`mont_cios_run`) serving four NIST fields + RSA. The dominant pattern across all twelve audits: dozens of `@export`, corpus-proven faculties are orphaned from live wiring — the effort is paid for, the consumer exists, the call is not made. (Underutilized cost-lattice primitives — `cl_dominates`/`cl_meet`/`cl_join` shadowed by `pareto`'s duplicated `pe_dominates`, `cl_register_order_q` with zero consumers, `uc_formula_latency` used only for regression bounds — are prose-level slack, not yet verified findings.)

## 2. Top cash-ins (RANKED) — wire proven capability into an existing live consumer

Ranked by impact × leanness × already-paid-for. Rows where the consumer module already exists and is live.

| # | Opportunity | Proven capability (file:symbol) | Live consumer to wire into | Impact | Effort |
|---|---|---|---|---|---|
| 1 | Emit non-power-of-2 multiply strength reductions `mul(x,2^a±1)→(x<<a)±x`, `mul(x,2^a±2^b)` (SAT-proven, cost-graded) instead of `IMULQ` | `numera/invent.iii:115-131` (`gil_forge`/`gil_consider`, kinds 2-5, proof `bb_equal`) | `COMPILER/BOOT/cg_r3.iii:2457` binary-expr handler, extend `r3_mul_pow2_k` (`:851-861`) past the exact-pow2 gate | **high** — the live codegen path; every constant multiply across all compiled code gets cheaper | med (moves golden seal → C-differential reseal) |
| 2 | Route the lone standalone CIOS multiply onto the shared organ; **fixes a constant-time defect** (`zkf_csub_p` data-dependent borrow → `mont_cond_sub_ct`) | `numera/bigint_div.iii:707` (`mont_cios_run`) | `numera/zk_field.iii:269-309` (`zkf_mul`, feeding `zkf_sqr`/`zkf_to_mont`/`zkf_from_mont`) | **high** — security correctness + dedup | low–med (value-preserving, **not** byte-identical; timing changes — confirm `zk_field` KATs value-equal under swap) |
| 3 | Generalize Rule H: register source-committed `(a,b)` shift-combine triples (`a+b<64`) each gated by `cga_dispose_shift` + kernel re-proof, preserving golden determinism (no registry-state dep) | `numera/sov_isa.iii:447` (Rule H precedent), `cga_dispose_shift` | `numera/sov_isa.iii:sov_isa_rules()` — extend the rule loop after Rule H | **high** — broadens the live optimizer rule set, determinism-safe | low–med (seal moves) |
| 4 | Strengthen kernel convertibility with the SAT miter as a fallback after CCL fails on BV pairs — decides `(x&y)+(x|y)==x+y`, `x-(x&y)==x&~y` the kernel can't fold | `numera/bv_bits.iii:349` (`bb_equal`, Tseitin+CDCL, sound-by-design, KAT 1345/1214) | `numera/typecheck.iii:408-411` (`tc_conv`, currently `ccl_conv` only) | **high** — more rewrites become kernel-admitted | med — **expands the trusted base** (SAT solver into the proof kernel; see §5 tradeoff); gate with differential KAT |
| 5 | Dedup three duplicate rotation atoms onto the proven shared rotation atom (byte-identical; rotation amounts are non-zero constants) | `weave_blocks.iii:22-33` (`wvb_rotl32/64`) | `siphash.iii:48-61` (`sip_round`/`siphash24_keyed`), `xoshiro.iii:48` (`xo_rotl`), `murmur3.iii:20` (`mm3_rotl32`) | **medium** — completes weave melt §2; pure refactor | **low** — the only truly byte-identical row |
| 6 | Give the self-improvement loop a real de-novo proposer that already closes propose→prove→adopt via the existing adoption pipeline | `numera/isa_macro_synth.iii:48` (`ims_synth`, proof-gated by `bisim_is_obsolete`, adopts via `sov_isa_adopt_dream`, KAT 1742) | `sanctus/autogenesis.iii:167-172` (`ag_cycle` gather block; fold admitted count into `AG_NCAND`) | **high** — strategic: the loop gains de-novo invention, reusing paid-for adoption | med |
| 7 | Cost-validate discovery candidates target-relatively (cycle model) instead of blind op-count | `numera/invent.iii:474-511` (`gil_cycle_validate`, corpus 1760 target-relative) | `forcefield/daemon_dream.iii` discovery gate (live; `daemon_dream_cycle` already calls `sov_isa_adopt_dream`) — currently called only by un-integrated `weave_forge.iii:38` | **high** — adoptions become "cheaper on *this* target" | med |
| 8 | Persist admitted proof terms for durability/audit instead of discarding after `tc_check` | `numera/proof_term.iii:1178/1247/1322` (`pt_serialize`/`pt_deserialize`/`pt_to_program`, KAT 636) | `numera/theorem_commons.iii:tcom_admit` (live, uses `tc_check`) | **medium** — auditability, replayable proofs | med |
| 9 | Erasure-code stripe engine (FEC encode/verify/scrub/recover), confirmed-orphaned **but** redundant with the production `numera/erasure_store` | `aether/bone_marrow.iii:446/470/482/588` | `aether/reach_store.iii` replication path | **low** | low — list-and-decide: likely decline as duplicate of `erasure_store`, not bloat-to-build |

## 3. Top enhancements into existing organs (RANKED, lean)

| # | Enhancement | Target organ (file:symbol) | Why it's lean |
|---|---|---|---|
| 1 | Formalize the implicit `lo>hi` bottom as `L6_NULL` (absorbing) so missing/invalid analysis data cannot silently become a safe value | `numera/interval_lattice.iii:41-45` consumed by `forcefield/sovereign_optimizer.iii:170-191` (`rc_proven_safe`/`be_eliminable`/`vrp_no_overflow`) | `logic6.iii` proves the annihilation law (KAT 1752); organ + consumers already live — it changes the bottom *representation*, so it's an enhancement, not a pure wire |
| 2 | Make `gil_cycle_validate`'s target-relative verdict the discovery sieve in the live dream cycle (pairs with cash-in #7) | `forcefield/daemon_dream.iii:daemon_dream_cycle` | reuses the existing discover→certify→adopt chain |
| 3 | Wire `invent.gil_forge` as a fourth `ag_cycle` proposer: `gil_forge → gil_cycle_validate → cga_dispose_bv (seal) → sov_isa_adopt_dream`, fold count into `AG_NCAND` | `sanctus/autogenesis.iii:162-185` | the adoption pipeline is already kernel-gated and live; the partial verdict only corrected the *mechanism* (seal via `cga_dispose_bv` or `sov_isa_adopt_from_commons`, not a parameterless direct call) |
| 4 | Prove the hardcoded `*38` fold in `fe25519` by calling `gilr_proves(255,19,38)` directly (or raise `gilr_forge`'s `cc>32` search cap) | `numera/fe25519.iii:fz_mul` ↔ `numera/invent.iii:290-320` | the prover exists and works at cc=38; only the forge's *search bound* blocks auto-discovery — a one-line cap change or a direct prove-call gives the hardcoded fold a certificate |
| 5 | Render each adopted discovery as a human-readable New-Math law at adoption time | `numera/present.iii:55/73/93` → live naming path in `numera/sov_pipeline.iii` | pure formatting layer, no new island; today only `weave_forge` (un-integrated) calls it |

## 4. Gaps / partials / stale (half-built vs intended fullness)

- **`numera/invent` de-novo SAT engine is a parallel, disconnected discovery path.** All four forges (`gil/gilx/gilr/gil6_forge`) are proven and cost-validated but have **no adoption-into-optimizer pathway** and are **not called by `ag_cycle`**; the autocatalyst (`cg_autocatalyst` via egraph hallucination) is the *only* discovery path that reaches the live rule set. Intended fullness: invent feeds the same `sov_isa_adopt_dream` pipeline (see §3 #3).
- **`ag_cycle` is operationally complete but strategically not self-driven.** Full propose/dispose/validate/commit semantics exist (`autogenesis.iii:162`), but it is invoked only by operator CLI verb `cycle` (`autogenesis_cli.iii:174`). No daemon/trigger. This is *partly intentional* (operator-gated promotion is the `daemon_dream` design philosophy) — fullness is a bounded autonomous trigger, not an unbounded thread (§5).
- **XII lattice codegen is a proven-but-undispatched dead branch.** `r3_pe_lattice_emit`/`r3_compute_circ`/`r3_pe_canonicalise` (`cg_r3_xii.iii:64-170`) are gated behind `sema_xii_anno_has_in_ast(R3_XII_ANNO_LATTICE)`, which **no corpus function carries** AND which `iiis1_link_stubs.c:36` hard-codes to return 0. Fullness requires both a corpus `@lattice` annotation and unstubbing the gate — heavier than a wire.
- **`affine_check → loop_optimizer → bce → loop_pipeline` is a complete proof-only chain orphaned from `COMPILER/BOOT`** (confirmed dead via `_scan/islands.txt`). The predicates are proven (KAT 1263/bce KAT=99) but never eliminate a single runtime bounds check because the chain is unreachable from the live compiler.
- **`develop_up` gateway (18 `@export` functions, not 8) and `sovereign_witness` (10 `@export`) have zero live consumers** — only corpus tests (1585/1597/1599; 416-419). Their intended consumers (a hypervisor guest-entry; a forensic/external-trace pipeline) **do not exist yet** → these are furtherdev, not cash-ins.
- **Two isolated intent stacks, no bridge.** Strict crypto-attested compiler (`intent/lex_ontology→intent_lex→disambiguate→synthesis_bridge→intent_attest`, all proven, **never called live**) vs. the live HIP NL parser (`verba/nl_lex→nl_parse→hip→ai_resolve`). `hip.iii:31-33` imports `nl_lex_tokenize`, never `intent_lex`. No entry point chains the strict phases.
- **Partials carrying corrected evidence (capability real, citation off):** attestation-box identity gates `ab_identity_pub`/`ab_verify`/`ab_attests_current` (only `ab_attest` is wired into `develop_up`); `babel_wire` typed-unpack trio (cap_handshake uses the facet/payload unpackers, not seal_id/cap_ref/intent_id); `cap_handshake` offer-accept (proven, no session orchestrator). Lower-priority wires.

## 5. First-principles further-development (next-level, incl. provable-but-speculative)

1. **Proof-carrying codegen — the proof becomes a code-motion contract.** Feed `affine_audit`'s sound `AA_PROVEN` verdicts (`affine_audit.iii:296-315`, base+(count-1)·stride wrap-free ∧ < size) into `cg_r3` so provably-in-bounds array accesses emit a direct ptr-deref with **no runtime check**, tagged `AFFINE_PROVEN`. This is the natural escalation of cash-in #1's posture: today `--affine-audit` runs *instead of* codegen and exits — the upgrade makes it a non-terminating post-sema gate (§6) and then a codegen consumer. Pairs with the cost lattice to rank elisions by proof weight.
2. **Bounded autonomous `ag_cycle` trigger.** Close the self-improvement loop's last gap: a synchronous, operator-cadence-bounded driver (honoring the `daemon_dream` "no runtime thread" discipline) that invokes `ag_cycle` on a defined condition rather than a CLI verb. Provable: each cycle is already kernel-gated and reversible; autonomy adds a trigger, not new trust.
3. **`strict_intent_submit` entry point with cryptographic scope-lock.** Orchestrate the four proven strict-intent phases: `dis_resolve` (exactly-one interpretation) → `sb_synthesize` (temporally-bounded capability) → `ia_manifest_hash` (content-address interpretation+rights+expiry) → Ed25519 signature gate → emit. Safety property: any scope creep changes the manifest hash → signature fails → emission refused. All four phases proven (1745/1746/1747); only the orchestration module is missing.
4. **Metal-layer reversibility: VMEXIT pre-image unwind.** `katabasis_vmexit_inverse` (`vmexit.iii:71-80`) already classifies each exit's SID inverse, and `sid_router.sr_record_and_mutate` is implemented but **never called**; wire the routed inverse kind to the reversible envelope (`rev_begin`/`rev_record`) for deterministic rollback. The module header itself admits "no slice RUNS it"; `RING-MINUS-1-PLAN.md` schedules this at increments I3-I5.
5. **`L6_NULL` null-safety proofs in the compiler core.** Inject the proven Belnap bottom into `proof.iii`/`sema.iii` so every uneliminated UB precondition (null deref, OOB, div-by-zero) becomes an absorbing `L6_NULL` proof obligation rather than silent safety. Infrastructure proven (`logic6.iii`); integration into the kernel type/flow analysis is unbuilt (confidence low, but provable).
6. **Tradeoff to hold visible (re cash-in #4):** wiring `bb_equal` into `tc_conv` puts a SAT solver inside the proof kernel — it tensions against the minimal-trusted-base discipline. It is sound-by-design (errors poison to `0xFF`, never fabricate) and differentially gated (KAT 1214/1345), but a false-TRUE here is a kernel soundness hole, not a missed optimization. Sequence it last and keep the differential gate as the floor.

## 6. The single highest-leverage sequence (what to do first, second, third — and why)

The continuous loop should bank byte-safe value first, then make the golden seal move **once** in a controlled batch, then touch the trusted kernel, then close the self-improvement loop. cg_r3 non-pow2-mul is **rank-1 by impact** (it is the payoff of the entire invention arc) but is sequenced into Phase 2, not first, deliberately: it moves the golden seal, so it should run *after* the byte-safe wins have exercised the gated-build/reseal rhythm on low-risk changes.

**FIRST — Phase 1: byte-safe / value-safe stdlib routing (no compiler-seal movement).** Run these three in parallel; they don't touch the compiler golden:
- **§2 #5 rotl dedup** (`siphash`/`xoshiro`/`murmur3` → `wvb_rotl32/64`) — the one truly byte-identical refactor; zero behavioral risk; completes weave-melt §2.
- **§2 #2 `zk_field.zkf_mul` → `mont_cios_run`** — banks a **security** win (constant-time). *Gate:* confirm `zk_field`'s KATs are value-equal under the swap before committing (values identical, timing changed — not byte-identical).
- **§2 #9-adjacent correctness: `cf_deforge_cascade` → `capability.cap_revoke`.** *Execution-gated:* this is a **behavioral change** (revocation now cascades). Docs say deforging "should be automatic"; code doesn't do it — but docs-vs-code does not settle it. Before wiring, confirm **no corpus test pins non-cascading revoke** (else fixing + rewriting the test would mask a regression). If clean, it's a near-bug-fix; if pinned, document-and-separate.

*Why first:* maximum already-paid-for value at near-zero risk, and it validates the build/gate cadence the seal-moving work will depend on.

**SECOND — Phase 2: optimizer rule set (golden seal moves once, batched).**
- **§2 #1 non-pow2 mul reductions → `cg_r3` `r3_mul_pow2_k`** (rank-1 impact) **together with §2 #3 shift-combine `(a,b)` → `sov_isa_rules`.** Batch the seal movement: emit byte-faithfully to the replaced path, then re-root the C-differential golden in one reseal. *Why batched:* both move the same golden; doing them in one reseal keeps the gate signal clean and avoids confounding two seal deltas.

**THIRD — Phase 3 then Phase 4.**
- **Phase 3: §2 #4 `bb_equal` → `tc_conv`** — strengthen the kernel, last among the compiler-touching work, behind the differential KAT, with the trusted-base tradeoff (§5 #6) stated in the commit. A false-TRUE is a soundness hole — it earns the most caution and the smallest blast radius (extern + fallback after `ccl_conv` returns 0).
- **Phase 4: §2 #6 `ims_synth` → `ag_cycle`**, then **§3 #3 wire `invent.gil_forge` as the fourth proposer** with `gil_cycle_validate` as its sieve (§2 #7 / §3 #2). *Why last:* it consumes everything Phases 2-3 just hardened (the broadened rule set, the cost-validated sieve, the stronger kernel convertibility), turning the paid-for invention engine into a live, self-driving contributor — the strategic close of the arc.

Key files for the loop: `COMPILER/BOOT/cg_r3.iii` (:851-861, :2457), `STDLIB/iii/numera/sov_isa.iii` (:447, :592-631), `STDLIB/iii/numera/invent.iii` (:115-131, :474-511), `STDLIB/iii/numera/typecheck.iii` (:408-411), `STDLIB/iii/numera/bv_bits.iii` (:349), `STDLIB/iii/numera/zk_field.iii` (:269-309), `STDLIB/iii/numera/bigint_div.iii` (:707), `STDLIB/iii/aether/cap_forge.iii` (:526) ↔ `STDLIB/iii/aether/capability.iii` (cap_revoke), `STDLIB/iii/{siphash,xoshiro,murmur3}.iii`, `STDLIB/iii/sanctus/autogenesis.iii` (:162-185), `STDLIB/iii/numera/isa_macro_synth.iii` (:48).


---

# Appendix: verified findings by lens (adversarial verdicts)

## Invention + Rosetta stack

*State:* The Invention + Rosetta stack is feature-complete and proven (KATs 1748-1755), with three de-novo forges (strength reduction, crossover, modular fold) + six-valued logic + cost validation + human-readable naming. However, the entire pipeline (invent.iii, present.iii, weave_forge.iii) runs isolated in KATs only; sov_isa's live rules() calls only one hardwired instance (shift-combine a=1,b=1) and does not invoke discovery, validation, or adoption. The autocatalyst (CGA) has a proven adoption bridge (cga_shift_certifies + SI_RULE encoding); the GIL de-novo engine has none — each requires different wiring. Logic6 is proved but untapped by the optimizer.

- **[cashin/high]** Shift-combine parametric adoption (fixed-set variant) — _confirmed_
  - detail: sov_isa_rules() hand-wires only shift-combine (1,1). The adoption capability sov_isa_adopt_shift_dream iterates the *accumulated* cga registry, violating determinism. The lean cash-in: extend rules() with source-committed (a,b) pairs for all shift-combine triples (a+b<64), each gated by cga_dispose_shift + kernel re-proof — generalizes Rule H without registry-state dependence, preserving golden determinism (M20/charter honored).
  - evidence: STDLIB/iii/numera/sov_isa.iii: sov_isa_rules() line 447 registers only (a=1,b=1); sov_isa_adopt_shift_dream line 640-642 iterates cga_shift_count() [accumulated registry]; line 640 comment: 'Operator-invoked (NOT in the default rules() path), so the build's golden emit is unmoved.' Proof rule H already gates on cga_dispose_shift (line 447), establishing precedent.
  - consumer: sov_isa_rules() in STDLIB/iii/numera/sov_isa.iii — extend loop after Rule H registration to walk a fixed source-committed list of (a,b) shift-combine candidates
- **[cashin/high]** GIL strength-reduction forge (requires translation layer) — _partial_
  - detail: gil_forge (invent.iii line 104) invents strength reductions for mul(x,k), proven equal by SAT + ranked by cost. But it outputs register state (GIL_BEST_KIND/A), not SI_RULE-encoded rules. To adopt: a translation layer must convert (kind,a,b) → SI_RULE preorder + kernel re-proof (like cga_shift_certifies). Honest classification: proven capability, but adoption cost is non-trivial (bridge design) and sov_isa_rules() is the intended consumer post-translation.
  - evidence: STDLIB/iii/numera/invent.iii: gil_forge @export line 104; outputs to GIL_BEST_COST/KIND/A (lines 92-96), not rule encoding. Zero call-sites outside KATs (1749_invent_strength.iii). Compare: cga_shift_certifies (STDLIB/iii/forcefield/cg_autocatalyst.iii) gates a rule directly, requiring only SI_RULE packing. daemon_dream header (STDLIB/iii/forcefield/daemon_dream.iii lines 16-18) establishes that invention is a design-time oracle, not boot-time: 'Promoting a discovery into the compiler source + reseal is the OPERATOR-GATED step.'
  - consumer: sov_isa_rules() — but requires git bridge (gil-shape → SI_RULE encoder + kernel gate), not a direct wire
  - **correction:** gil_forge is a proven strength-reduction oracle (SAT-proven, cost-ranked), outputs register state not SI_RULE format, and would need translation to adopt. BUT: (1) It has multiple active call-sites beyond test code (present.iii, invent.iii). (2) The intended adoption mechanism is sov_isa_adopt_dream which consumes the autocatalyst's certified registry, not gil_forge directly - so the adoption bridge already exists but via a different pathway (strength laws are discovered, certified by UNSAT disposer, then adopted with kernel re-proof). The capability is not wired to the optimizer via sov_isa_rules; instead the optimizer is extended via autonomous discovery → certification → adoption, where gil_forge plays a supporting role in cost validation, not the primary discovery engine.
- **[cashin/high]** Cost validation bridge (gil_cycle_validate) — _confirmed_
  - detail: Commit e4dc2a67 wired invent.iii to microarch_model (gil_cycle_validate, line 474): invents a reduction, then costs both imul and the proposed form on the REAL cycle model, returning 1 iff strictly cheaper. This makes 'cheaper' TARGET-RELATIVE, not blind op-count (7*x reduction is cheaper on slow-multiply, refused on fast-multiply). Proven: corpus 1760 exit=99. But only called by weave_forge (which is not integrated), not by live rule discovery.
  - evidence: STDLIB/iii/numera/invent.iii: gil_cycle_validate @export lines 474-511; only called from STDLIB/iii/numera/weave_forge.iii line 38 (not integrated into sov_isa). Commit e4dc2a67 message: 'THE SYSTEMWIDE CASH-IN (no island, extract what III already paid for)' — the cost oracle numera/microarch_model + cost_calculus are live organs (sov_isa uses cost_lattice); bridge already exists, only composition missing.
  - consumer: sov_isa_rules() or daemon_dream.iii — could gate discovery candidates by cycle validation instead of op-count sieve
- **[cashin/medium]** Rosetta New-Math presenter (present_* rendering) — _confirmed_
  - detail: Commit 2a5c49e3 added numera/present.iii (113 lines): present_strength/fold/collapse render each invention as human-readable symbolic law, named by content-address (cad). Three @export functions (lines 55, 73, 93) compose invent.iii discoveries. KAT 1754 proves correctness (corpus 1754_present_newmath.iii); zero call-sites outside KATs and weave_forge.iii (not integrated). Pure formatting layer, no island — but only runs in discovery/demo, never in live naming path.
  - evidence: STDLIB/iii/numera/present.iii: lines 55-107 present_strength/fold/collapse @export; lines 9-10 comment 'pure formatting + one cad. Composes numera/invent + numera/cad.' Only called from STDLIB/iii/numera/weave_forge.iii lines 39-51 (wf_fill) and STDLIB/corpus/1754_present_newmath.iii.
  - consumer: weave_forge.iii (which itself needs integration), or new live presenter layer in sov_pipeline if discovery is integrated

## The weave / spiderweb (primitive substrate)

*State:* The unified weave is PROVEN and WIRED: primweb establishes foundational cryptographic relations (rotation duality, ARX invertibility, reversible computation isomorphism, SHA-2 structure). Weave_blocks shares atomic implementations (wvb_rotl32/64, wvb_arx_mix, wvb_ch32/64, wvb_maj32/64, wvb_gf8_mul) that now serve SHA-2, ChaCha, Blake2, Keccak, AES, and Galois. The @inline keystone is designed + safepoint-tagged but deliberately not built; live consumers route through proven shared atoms, achieving the unified substrate goal. Four melt targets remain: three rotl dedups + zk_field CIOS consolidation.

- **[cashin/medium]** SipHash/Xoshiro/Murmur rotl deduplication: 3 duplicate rotation atoms should route through wvb_rotl32/64 — _confirmed_
  - detail: siphash.iii defines inline rotations (sip_round lines 48-61); xoshiro.iii defines xo_rotl (line 48); murmur3.iii defines mm3_rotl32 (line 20). All are identical to or special cases of wvb_rotl32/64 (weave_blocks.iii lines 22-33). Atoms already exported and proven live (wvb_rotl64 routed by SHA-512/Keccak, wvb_rotl32 by SHA-256). Byte-identical routing for Murmur (no guards); siphash/xoshiro safe via public constant rotation amounts.
  - evidence: siphash.iii:48-61 (sip_round); xoshiro.iii:48-50 (xo_rotl); murmur3.iii:20-21 (mm3_rotl32); weave_blocks.iii:22-33; melt-plan §2 target #2
  - consumer: siphash24_keyed, xoshiro_next_u64, murmur3_32
- **[cashin/high]** zk_field.zkf_mul routes to existing mont_cios_run: consolidates lone CIOS implementation + fixes constant-time defect — _confirmed_
  - detail: zkf_mul (zk_field.iii:269–309) is a standalone CIOS Montgomery multiply, byte-for-byte identical in structure to mont_cios_run (bigint_div.iii:707). mont_cios_run already proven live (used by fp256/fp384/fn256/fn384/RSA). Routing zk_field onto it eliminates source duplication and upgrades zk_field to constant-time; its zkf_csub_p (line 308) has a data-dependent borrow branch that mont_cios_run masks (mont_cond_sub_ct).
  - evidence: zk_field.iii:269–309 (zkf_mul body, CIOS structure); bigint_div.iii:707 (mont_cios_run definition); fp256.iii/fp384.iii/fn256.iii/fn384.iii/rsa.iii all extern mont_cios_run; melt-plan §3 target #4; zkf_csub_p at zk_field.iii:308 (data-dependent branch)
  - consumer: zkf_mul_x, zkf_sqr, zkf_to_mont, zkf_from_mont (the entire ZK field arithmetic)
- **[enhancement/high]** Proven rotl duality made load-bearing in live crypto: unifies ChaCha/Blake2/SHA-512/Keccak/SipHash/Xoshiro under one rotation substrate — _confirmed_
  - detail: primweb.pw_rot_duality proves rotl(x,k)==rotr(x,32-k) for all k. wvb_rotl32/64 now THE shared rotation atom: SHA-256 uses wvb_rotl32 (sha_rotr), SHA-512/Keccak use wvb_rotl64. ChaCha/Blake2 use it implicitly via wvb_arx_mix (which calls wvb_rotl32 internally). Proof is converted from abstract theorem to load-bearing infrastructure. Reduces rotation redundancy across entire crypto layer.
  - evidence: primweb.iii:47–52 (pw_rot_duality); sha256.iii:136–137 (sha_rotr → wvb_rotl32); sha512.iii:54–55 (sha512_rotr → wvb_rotl64); chacha20.iii:47–48 comment; blake2s.iii:43–44 comment; weave_blocks.iii:19–24 'THE universal rotation'
  - consumer: sha256_block, sha512_block, _kk_rho (Keccak rho layer), cc20_quarter_round, b2s_mix
- **[enhancement/high]** Proven Ch/Maj structure as canonical weave atoms: mux/absorb properties made executable code — _confirmed_
  - detail: primweb proves Ch(e,f,g) is a bitwise multiplexer (control e selects f or g) and Maj(a,b,c) absorbs repeated input and equals the full-adder carry. These structural facts (pw_sha_ch_is_mux, pw_sha_maj_absorbs, pw_sha_maj_optform as carry-save) are now live in weave_blocks.iii (wvb_ch32/64, wvb_maj32/64) routing through SHA-256/512. Theoretical insight become implementation substrate.
  - evidence: primweb.iii:163–175 (pw_sha_ch_is_mux), 178–184 (pw_sha_maj_absorbs), 226–234 (pw_sha_maj_optform); weave_blocks.iii:61–68 (wvb_ch32/ch64/maj32/maj64 + comments proving mux/absorb); sha256.iii:237/242, sha512.iii:264/267 routing calls
  - consumer: sha_block (SHA-256), sha512_block (SHA-512)

## Cost / value substrate + the microarch cash-in

*State:* The cost subsystem is architecturally sound with ONE canonical cost authority (cost_lattice.iii) driving invention validation, egraph extraction, optinvoke, and composition. The headline cash-in (invention engine cost-validated against microarch model, commit e4dc2a67) is already wired and live in weave_forge.iii. The main gaps are underutilized lattice primitives (cl_dominates, cl_meet, cl_join sitting beside pareto_extraction's duplicated pe_dominates), a rational-weight registration API (cl_register_order_q) with no live consumers, and the closed-form critical-path latency (uc_formula_latency) only used for regression bounds, not cost-aware saturation.

- **[cashin/high]** Invention engine cost-validated against microarch model (ALREADY WIRED) — _confirmed_
  - detail: The gil_cycle_validate() function at invent.iii:474-511 connects the invention engine's value-sieve to the real microarch_model.iii simulator. When a strength reduction is invented by gil_forge, its cycles are validated against target-specific multiply latency using ma_simulate. The same reduction returns 1 (cheaper) on slow-multiply targets and 0 (not cheaper) on fast-multiply targets.
  - evidence: invent.iii lines 474-511 define gil_cycle_validate which calls ma_init/ma_set_latency/ma_simulate. weave_forge.iii line 38 calls it: if gil_cycle_validate(n, mul_latency) == 1u8 { present_strength(n) }. Corpus 1760_gil_cycle_validate.iii validates this with 7*x proving cheaper on imul=10 but not on imul=1.
  - consumer: weave_forge.iii::wf_fill (line 38) - the autonomous weave filler that selects cost-validated gaps
- **[cashin/medium]** Regpressure oracle algorithm (PROVEN BUT SEPARATED) — _confirmed_
  - detail: cost_calculus.iii exports cc_regpressure_oracle (line 282-286) which runs the O(n²) sweep algorithm as the proven oracle. The fast O(n+dep_n) differential path (cc_regpressure_fast, line 277-281) is used in production (cc_evaluate line 359). The oracle is kept as differential validation per corpus 1487's specification [CUT-3/A-CC-1].
  - evidence: cost_calculus.iii lines 221-286 define both _cc_regpressure_sweep (oracle) and _cc_regpressure_diff (fast). Lines 277-286 export both. Line 272 of cc_regpressure calls _cc_regpressure_diff for the internal path; corpus 1487 validates the differential.
  - consumer: Self-validating: fast path used in cc_evaluate (line 359), oracle available for external KAT validation

## Optimizer chain (cg_r3 / sov_isa / xii / egraph / nous / ripple)

*State:* III's optimizer chain has proven subsystems (egraph saturation, invention engine, XII lattice codegen, sovereign optimizer, ripple store) with KAT-verified correctness, but the live cg_r3 codegen path integrates only a fraction: exact-power-of-2 strength reduction, identity-element folding, and constant-shift immediates. Non-power-of-2 multiplication strength reductions (discovered and SAT-proven) go uncashed, and the XII pipeline remains dead code due to absent @lattice annotations.

- **[cashin/high]** Invented multiplication strength reductions (non-power-of-2 constants) — _confirmed_
  - detail: mul(x, 2^a±1) → (x<<a)±x and mul(x, 2^a±2^b) → (x<<a)±(x<<b), SAT-proven via bb_equal and cost-graded (2-3 ops vs multiply), but cg_r3 only emits shifts for exact powers of 2.
  - evidence: STDLIB/iii/numera/invent.iii:115-131 (gil_consider/gil_forge with SAT judgement bb_equal at line 91; corpus 1748-1756 KATs) vs COMPILER/BOOT/cg_r3.iii:851-860 (r3_mul_pow2_k checks (v & (v-1))==0 only)
  - consumer: r3_mul_pow2_k call site in binary expression handler (cg_r3.iii:2455-2466); should extend to cover kind 2/3/4/5 from invent.iii
- **[cashin/medium]** XII lattice codegen path dead branch (gated but unexercised) — _confirmed_
  - detail: r3_pe_lattice_emit, r3_compute_circ, r3_pe_canonicalise (in cg_r3_xii.iii) are linked and callable from cg_r3 but gated behind sema_xii_anno_has_in_ast(R3_XII_ANNO_LATTICE) which no corpus function carries; the entire XII horizon selection + LDIL placeholder pipeline is proven but never dispatched.
  - evidence: COMPILER/BOOT/cg_r3.iii:3360-3366 (xii_lat = sema_xii_anno_has_in_ast(R3_XII_ANNO_LATTICE); if xii_lat==1u8 calls r3_pe_*); COMPILER/BOOT/cg_r3_xii.iii:64-170 (three @export functions); no @lattice modifier in any corpus .iii file
  - consumer: cg_r3 function body emitter (r3_emit_fn, around line 3350) when processing FN_DECL with @lattice annotation

## Crypto / number theory

*State:* III's crypto subsystem is fundamentally sound with proven cores (bigint_div, mont_cios_run serving RSA/EC; fe25519 hardcoding *38 fold; weave_blocks providing rotation identities to SHA-2 and Keccak). The SHA-2 weave identities are fully wired and load-bearing (u32 codegen fix + proven rotl/Ch/Maj shared). However, the special-prime modular fold discovery engine (gilr_forge) exists but is only being tested on toy primes (2^32-1, 2^32-5), not the actual crypto primes like 2^255-19 that already hardcode their folds.

- **[cashin/high]** gilr_forge NOT proving the hardcoded *38 fold in fe25519 — _partial_
  - detail: fe25519.iii line 96-105 implements the pseudo-Mersenne reduction r = (x & (2^255-1)) + 38*(x>>255) for GF(2^255-19) with explicit comment it is 'DELIBERATELY EXEMPT' from Montgomery consolidation because *38 fold is ~6.9x faster. The gilr_forge in invent.iii can discover and prove this exact fold for p=2^255-19, but weave_forge.iii only calls gilr_forge(32,1) for Mersenne 2^32-1 (toy prime). The proven fold could validate the hardcoded implementation.
  - evidence: /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/fe25519.iii lines 96-105 (hardcoded *38 fold); /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/weave_forge.iii lines 45-46 (gilr_forge only for 32-1); /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/invent.iii lines 268-336 (gilr_forge discovery engine)
  - consumer: fe25519.iii:fz_mul should wire gilr_forge(255, 19) to obtain proof certificate for the hardcoded *38 fold
  - **correction:** gilr_proves(255, 19, 38) can prove the hardcoded fold, but gilr_forge cannot auto-discover it due to search limit. The gap exists: fz_mul should either (A) call gilr_proves(255, 19, 38) directly to prove the hardcoded *38 fold, or (B) extend gilr_forge's search limit (line 320) to support multipliers beyond 32.
- **[cashin/high]** ed_mod_l allocation-free Barrett reduction for Ed25519 scalar order L is properly wired but only used for its specific domain — _confirmed_
  - detail: ed_scalar_modl.iii (lines 1-35) implements a sovereign allocation-free Barrett reduction specialized to Ed25519's group order L=2^252+..., replacing generic bigint_mod at two sites in crypt_ed25519 (r mod L for nonce, S mod L for signature scalar). This is excellent lean design and is properly consumed (APOTHEOSIS §C.2). The reduction is proven correct via KAT differential corpus 1021 + RFC-8032 conformance.
  - evidence: /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/crypt_ed25519.iii line 156 (ed_mod_l called) and line 241 (ed_mod_l called); /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/ed_scalar_modl.iii lines 1-35; corpus 1021 (differential vs bigint_mod)
  - consumer: crypt_ed25519.iii (already wired at lines 156, 241)
- **[cashin/high]** mont_cios_run shared Montgomery reduction organ is ONE core serving four NIST prime fields + RSA + general modpow — _confirmed_
  - detail: bigint_div.iii line 707 implements mont_cios_run: radix-2^32 CIOS multiply-reduce with branch-free conditional-subtract. APOTHEOSIS S.2 consolidation routes all odd-modulus multiplication through this single organ: fp256 (fp_mul), fp384 (fq_mul), fn256, fn384, RSA (rsa_modexp via raw-buffer bridge), and bigint_modpow. One audited reduction proof surface (corpus 759). This is lean and correct.
  - evidence: /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/bigint_div.iii lines 557-750 (mont_cios_run); fp256.iii line 25, fp384.iii line 22, rsa.iii lines 29-34 (consumers); corpus 758 (758_mont_ctx_organ, 759_mont_bigint_width, 760_field_mont_organ)
  - consumer: fp256/fp384/fn256/fn384 (field ops), rsa.iii (modexp), bigint_modpow (all wired)

## Compiler core + the CIC kernel

*State:* The CIC kernel's bitvector convertibility (tc_conv) is limited to CCL reduction with closed literal folding and trivial symbolic pairs, but the proven SAT judge (bb_equal in bv_bits.iii) handles mixed arithmetic+bitwise identities that tc_conv cannot fold. The SAT judge is actively used in non-kernel subsystems (synthesis, optimization, cryptography) but not wired into the kernel itself, leaving a cleanly-separable cash-in opportunity.

- **[cashin/high]** SAT judge (bb_equal) not wired into kernel's convertibility check — _confirmed_
  - detail: bb_equal in numera/bv_bits.iii is a Tseitin+CDCL SAT decision procedure for mixed arithmetic+bitwise bitvector equivalences (e.g., (x & y) + (x | y) == x + y, x + x == x << 1). It has @export, is proven correct (corpus 1345_bv_bits.iii returns 99), and handles linear identities that tc_conv cannot. However, it is only called in non-kernel subsystems (numera/invent.iii for synthesis, cg_autocatalyst for autonomous optimization, bv_dispose for two-tier disposal, primweb for cryptography) but NOT integrated into typecheck.iii's tc_conv (lines 408-411), which remains limited to CCL reduction + structural comparison.
  - evidence: Source: STDLIB/iii/numera/typecheck.iii:408-411 (tc_conv delegates only to ccl_conv); STDLIB/iii/numera/invent.iii:14-15 (CIC conv too weak for linear BV identities); STDLIB/iii/numera/bv_bits.iii (exported bb_equal) and corpus 1345_bv_bits.iii (test vectors 1-10, exit 99); DOCS/III-BV64-KERNEL-MODEL.md:99-102 (kernel's syntactic conv equates almost only reflexive a vs a pairs).
  - consumer: tc_conv in typecheck.iii (currently delegates to ccl_conv only)
- **[gap/high]** BV64 kernel conversion incomplete for symbolic identities — _refuted_
  - detail: The CIC kernel's BV64 model (added 2026-06-06, typecheck.iii + ccl.iii) handles closed literal folding and simple symbolic pairs (only reflexive a vs a). It cannot prove symbolic identities like x - (x & y) == x & ~y or multi-class collapse (x*1) + (x*0) == x, which require SAT-level reasoning. The differential KAT 1214_bv_kernel_differential explicitly states symbolic cross-checks rely on non-vacuous trivial pairs only. This leaves the kernel weak on boundary identities that matter most to code generation.
  - evidence: Source: DOCS/III-BV64-KERNEL-MODEL.md:25-34 (what kernel proves: literal folding, width identities on symbolic x via iota, no mixed identities); DOCS/III-BV64-KERNEL-MODEL.md:99-102 (differential admits symbolic weight mostly non-vacuous on trivial pairs); STDLIB/iii/numera/bv_bits.iii:1-10 (SAT judge decides these identities); STDLIB/iii/forcefield/bv_dispose.iii:148-150 (kernel does NOT fold symbolic x-x despite bv_ring deciding it).
  - consumer: tc_conv in typecheck.iii (ccl_conv path)
  - **correction:** The BV64 kernel DOES prove the multi-class collapse (x*1)+(x*0)==x via its monoid/semiring identity rules (x*1=x, x*0=0, x+0=x). Mixed identities requiring bitwise+arithmetic reasoning (like x-(x&y)==x&~y) are correctly handled by the SAT solver in bv_bits, not the kernel. The kernel's symbolic conv working primarily via iota reduction to canonical form (reflexive pairs) is the intended design, not incomplete coverage.
- **[cashin/high]** bb_equal as fallback oracle for symbolic BV conversion — _confirmed_
  - detail: The SAT judge bb_equal is scope-bounded (w in 1..64), sound by design (errors poison to 0xFF not fabricate verdicts), reusable per-decision (bb_reset -> build -> bb_equal), and NIH-safe (numera/sat only). Integrating it as fallback in tc_conv (after ccl_conv fails on BV pairs) would extend kernel conversion to (x & y) + (x | y) == x + y, x - (x & y) == x & ~y, and hard mixed identities proven daily by invent/cg_autocatalyst loop. Wiring: extern bb_equal from bv_bits.iii, call after ccl_conv returns 0 for BV pairs, return 1 iff bb_equal confirms. Trusted base grows only the extern declaration (outside tc_to_ccl seal boundary). Adversarial soundness: if bb_equal says FALSE on kernel-true pair, differential-soundness floor shrinks (caught by KAT).
  - evidence: Source: STDLIB/iii/numera/bv_bits.iii:1-30 (contract: scope-bounded, error-safe); STDLIB/iii/numera/invent.iii:13,78-99 (bb_equal as sole judge on symbolic pairs, reusable DAG); STDLIB/iii/forcefield/bv_dispose.iii:57-60 (bvd_admit pattern: Tier 1 refutation + Tier 2 kernel disposal); STDLIB/corpus/1345_bv_bits.iii and 1214_bv_kernel_differential (proven, green).
  - consumer: tc_conv in typecheck.iii

## Proof / verification organs

*State:* The III proof ecosystem has built and proven three independent verification tiers (affine-bounds proof, SAT/BV equivalence, CIC kernel type-checking) plus a proof-term codec; sat/bv_bits/typecheck are well-integrated into live optimization chains (cg_autocatalyst via daemon_dream/sovereign_optimizer), but affine verification capacity (affine_audit + affine_check in polyhedra analysis pipeline) and proof-term serialization remain unwired — the most immediate cash-in is affine_audit into the standard compilation gate, and deeper is proof-carrying-codegen that elides bounds checks post-proof.

- **[cashin/high]** affine_audit proven but unintegrated into default compilation pipeline — _confirmed_
  - detail: affine_audit.iii:iii_affine_audit_run (@export, corpus 416_sovereign_witness_affine = 99) exists solely as --affine-audit flag opt-in. Should be wired as an automatic post-sema PASS/FAIL gate in main.iii main compilation path: every module with @for loops over typed arrays emits PROVEN/REFUTED/ABSTAIN verdict without slowing build (sound by design: ABSTAIN = honest 'unknown', never false-PROVEN).
  - evidence: COMPILER/BOOT/main.iii lines ~505-510: iii_affine_audit_run invoked only when flag set; COMPILER/BOOT/affine_audit.iii lines 571-593 define entry; COMPILER/BOOT/affine_audit_sound.iii documents soundness of the PROVEN verdict
  - consumer: main.iii (post-sema compilation gate, before SID/codegen); build_stdlib.sh (KAT gate R=0 check)
- **[cashin/high]** affine_check (polyhedra bounds analysis) proven but only used in bounds-check-elimination KAT — _partial_
  - detail: numera/affine_check.iii:ac_in_bounds / ac_disjoint / ac_max_access (@export, corpus 1263_affine_check = 99) are sound predicates for affine region analysis. Called only from bce.iii (bounds-check-elimination KAT, never invoked from live pipeline). The pairing (bce calls affine_check) suggests bce should be wired into cg_r3 / emit codegen to actually ELIMINATE checks post-proof, but bce itself is proof-only.
  - evidence: STDLIB/iii/numera/affine_check.iii lines 11-50 (public API); STDLIB/iii/numera/bce.iii line ~23 (bce_redundant calls ac_in_bounds); bce/affine_check are never called outside their own KATs (corpus search confirms)
  - consumer: bce.iii (which should be wired into emit.iii or cg_r3.iii for live codegen); alternatively, a dedicated bounds-elimination pass in the compiler pipeline
  - **correction:** affine_check predicates (ac_in_bounds, ac_max_access, ac_disjoint) are sound and proven, but consumed by a THREE-MODULE PROOF-ONLY CHAIN: affine_check → loop_optimizer → bce → loop_pipeline, with vectorizer as a parallel stage. The chain is orphaned from COMPILER/BOOT (confirmed via islands.txt dead-code scan). The cash-in (wiring loop_pipeline or bce into actual bounds-check elimination in live codegen) is genuinely unrealized. Correct statement: "affine_check predicates proven via KAT (99); called by loop_optimizer and bce (also proven); composed by loop_pipeline (also proven KAT=99); entire chain unreachable from live compiler, preventing actual bounds-check elimination in codegen."
- **[cashin/medium]** proof_term serialization/deserialization (pt_serialize/pt_deserialize/pt_to_program) proven but not persistently used — _confirmed_
  - detail: proof_term.iii exports 10 functions including pt_serialize (serialize a proof term to bytes), pt_deserialize (parse bytes back to term id), pt_to_program (convert proof to executable bytecode); all pass their KATs (corpus 636_proof_term = 99) but are called ONLY within proof_term.iii's own KAT and math_library.iii/math_library_curation.iii KATs. No live module (theorem_commons, pcc_gate, lemma_forge, certified_morphism) uses these to persist/resume proofs — all route through tc_check (CIC kernel). The serialization is dead code.
  - evidence: STDLIB/iii/numera/proof_term.iii lines 1007-1030 (pt_serialize @export), lines 1040-1055 (pt_deserialize @export), lines 1060-1080 (pt_to_program @export); grep confirms zero calls outside {proof_term.iii, math_library.iii, math_library_curation.iii}; STDLIB/iii/numera/theorem_commons.iii / forcefield/pcc_gate.iii / nous/lemma_forge.iii all use tc_check for admission, not pt_verify
  - consumer: theorem_commons.iii:tcom_admit (should persist proved terms in serialized form for durability/auditability); alternatively, an archival/audit module that retrieves and replays historical proofs
  - **correction:** Minor: The claim said functions are "called ONLY within proof_term.iii's own KAT and math_library.iii/math_library_curation.iii KATs" - more precisely, they appear only in proof_term.iii's own KAT (lines 1400, 1458-1462) and corpus test 1463_numera_carrier_program_gaps.iii. The proof_term.spec.md describes these for "serialize to canonical byte encoding for transport (M18 theorem-carrier discipline)" but theorem_carrier.iii does not actually import or use them.
- **[gap/high]** proof_carrying.iii (polynomial commitments + proof certificates) is PARTIAL — specification-defined but not fully built — _refuted_
  - detail: numera/proof_carrying.iii specified in DOCS/CONVERGENCE-SPECS/proof_carrying.spec.md (gap list lines 214–227) remains incomplete: missing pc_open_poly (polynomial opening), pc_verify_poly (opening verification), pc_coeff_leaf helper (canonical leaf serialization), pc_cert_producer/pc_cert_chain_root accessors, bounds guards on n > 1024, null-pointer guards, and KAT vectors. The spec requires galois.iii (gfp_mul/gfp_add) which is absent from STDLIB/iii/numera/. Verdict = PARTIAL per spec.
  - evidence: DOCS/CONVERGENCE-SPECS/proof_carrying.spec.md Gap/Fix list items 1–11; numera/proof_carrying.iii does not exist in STDLIB/iii (confirmed via Glob); spec notes spec names it Module NN but builds are pending
  - **correction:** proof_carrying.iii is COMPLETE, not PARTIAL. All public API functions are fully implemented with proper guards, bounds checking, and self-tests. galois.iii dependency is satisfied. Ready for use.
- **[enhancement/high]** affine_audit → proof-carrying codegen (elide bounds checks post-proof) — _confirmed_
  - detail: affine_audit emits sound PROVEN/REFUTED/ABSTAIN verdicts but does not feed into codegen. Lean upgrade: wire PROVEN verdicts into cg_r3.iii / emit.iii so array accesses whose bounds are proven emit no runtime checks — the proof becomes a code-motion contract. This is proof-carrying-code: the proof (PROVEN verdict) is the justification, codegen the consumer. Pairs naturally with the kernel's cost-lattice optimization (cap 6) to rank elisions by proof weight.
  - evidence: affine_audit.iii lines 296–315 (aa_prove_affine soundness kernel); aa_prove_affine returns AA_PROVEN iff base+(count-1)*stride wraps-free AND < size — exactly the certificate a codegen elision needs. Spec for this wiring is implicit in DOCS/III-AFFINE-AUDIT-DESIGN.md §4b (loop-range rule) but not yet integrated into emit/cg_r3
  - consumer: cg_r3.iii (code-generation rule: if affine_audit emitted PROVEN for an access, use a direct ptr-deref without bounds-check; mark with witness tag 'AFFINE_PROVEN'). Alternatively, a new codegen pass that consumes the audit report post-sema

## Autogenesis + self-improvement loop

*State:* The III system has a fully-gated self-improvement loop (ag_cycle) with three proposers, cost optimizer, lemma forge, and certified adoption pipeline. However, two critical gaps prevent true autonomous self-invention: (1) the de-novo invention engines (numera/invent: SAT-based strength reduction, bitwise-arithmetic discovery, modular reduction) are completely unwired from ag_cycle—only autocatalyst-driven hallucination is integrated; (2) ag_cycle itself requires explicit CLI invocation and is never autonomously triggered. The loop is operationally complete but strategically not self-driven.

- **[cashin/high]** isa_macro_synth.ims_synth: proven ISA macro synthesis ready but not consumed by ag_cycle — _confirmed_
  - detail: isa_macro_synth.iii (module numera_isa_macro_synth) exports ims_synth (line 48) and ims_admit (line 38) which fully close the propose→prove→adopt loop: ims_synth proposes macro b for op a, ims_admit proves equivalence via bisim_is_obsolete (refute-by-default), and if proven calls sov_isa_adopt_dream to enter the *live* optimizer rewrite set. Has KAT 1742 (corpus/1742_isa_macro_synth.iii). Zero production consumers.
  - evidence: STDLIB/iii/numera/isa_macro_synth.iii lines 38-43 (proof-gated admission + adoption); STDLIB/corpus/1742_isa_macro_synth.iii (KAT returns 99); autogenesis.iii lines 162-185 shows ag_cycle gathers from three proposers (gc_propose_next, hs_scan, rp_scan) but not ims_synth.
  - consumer: ag_cycle (autogenesis.iii:162-185), specifically the gather block lines 167-172 where AG_NCAND = hsn + rpn should fold in ims_synth's admitted count.
- **[gap/high]** numera/invent (SAT-based de-novo invention) is orphaned: no adoption path, not called by ag_cycle — _confirmed_
  - detail: invent.iii exports four forges: gil_forge (strength reduction via SAT, line 104), gilx_forge (bitwise-arithmetic crossover, line 232), gilr_forge (modular reduction, line 313), gil6_forge (logic6 lattice simplification, line 415). All cost-validate via gil_cycle_validate (line 474) against real microarch_model. But: (1) NO adoption-into-optimizer pathway (unlike isa_macro_synth which calls sov_isa_adopt_dream); (2) NOT called by ag_cycle; (3) only corpus KATs (1749-1751, 1753) and presentation (present.iii) use them. Meanwhile, autocatalyst (cg_autocatalyst.iii) handles strength discovery via egraph hallucination + kernel verification, making invent a parallel, disconnected discovery path.
  - evidence: STDLIB/iii/numera/invent.iii lines 104-133 (gil_forge), 232-258 (gilx_forge), 313-336 (gilr_forge); autogenesis.iii extern list (lines 26-50) imports gc_propose_next, hs_scan, rp_scan, os_apply_in_model, lf_forge but NOT invent functions; grep shows NO external callers of gil_forge outside invent itself, present.iii, weave_forge.iii, and corpus KATs.
  - **correction:** The enumeration of corpus KAT consumers is incomplete. The claim lists "(1749-1751, 1753)" but should include 1754_present_newmath.iii (direct import of gilr_forge, line 14) and 1760_gil_cycle_validate.iii (direct import of gil_cycle_validate). Both remain corpus KATs, consistent with the category "only corpus KATs use them," but the numeric list is short by two. Additionally, the DETAIL section omits weave_forge.iii from the consumer list despite mentioning it in EVIDENCE CITED—it should be noted as a STDLIB-level organ that bridges invent to weave discovery, though transitive reachability remains only through corpus testing (1764_weave_forge.iii).
- **[gap/high]** ag_cycle is operationally complete but strategically not autonomously invoked — _confirmed_
  - detail: ag_cycle is the single self-improvement loop entry point (autogenesis.iii line 162, @export) with full cycle semantics (propose, dispose, validate, commit). But it is NEVER invoked autonomously: agc_run (autogenesis_cli.iii line 140) is the only production driver, and it dispatches only on operator CLI commands ('cycle', 'commit', 'rollback', etc.—lines 173-196). The autonomous_invention_demo corpus (1748) manually calls ag_cycle but is a test. No daemon, no background loop, no trigger condition. The infrastructure is complete but the autonomy is wired out.
  - evidence: STDLIB/iii/sanctus/autogenesis_cli.iii line 174: agc_run dispatches ag_cycle(AGC_CAP) on verb=='cycle' only, operator-initiated; STDLIB/corpus/1748_autonomous_invention_demo.iii lines 61-66 manually call ag_cycle + ag_commit as a demonstration, not production; grep shows zero other callers.
- **[enhancement/high]** Wire invent into ag_cycle as a fourth proposer with kernel-certified adoption — _partial_
  - detail: The invention engine (invent.gil_forge + cycle_validate) can discover provably-cheaper forms via SAT. To wire it: (1) Call invent.gil_forge from ag_cycle's gather block (after rp_scan); (2) Validate cost via gil_cycle_validate; (3) If cheaper, package as a (a, b) strength pair and feed to sov_isa.sov_isa_adopt_dream to enter the optimizer's live rewrite set; (4) Fold the admitted count into AG_NCAND for the risk gate. This reuses sov_isa's existing adoption pipeline (already in place from isa_macro_synth and autocatalyst) and connects the SAT-based search to the self-optimizer. Requires kernel-citation gate (tc_conv during adoption) to remain sound.
  - evidence: invent.iii lines 104-133 (gil_forge public API); sov_isa.iii lines 592-631 (sov_isa_adopt_dream accepts (a, b) pairs from registry); autogenesis.iii lines 167-172 show the gather block where this call and fold would fit. The adoption pipeline already exists and is kernel-gated.
  - consumer: ag_cycle (autogenesis.iii:162-185) gather block, to be wired as a conditional fourth proposer.
  - **correction:** The enhancement is real and missing, but the integration mechanism description should specify: (1) Call invent.gil_forge(k) to discover cheaper forms; (2) Validate via gil_cycle_validate; (3) If cheaper, extract (a, b) via gil_best_a/b and either (a) call cga_dispose_bv(a, b, budget) to seal into registry, then sov_isa_adopt_dream() adopts from it, OR (b) directly call sov_isa_adopt_from_commons(a, b); (4) Fold admitted count into AG_NCAND. The kernel-citation gate (tc_conv in disposal step) remains sound.
- **[cashin/medium]** sov_isa.sov_isa_adopt_dream: proven strength-reduction adoption pipeline exists but invent is not a feeder — _partial_
  - detail: sov_isa_adopt_dream (sov_isa.iii lines 592-631) is a kernel-certified adoption path: it walks the autocatalyst's certified BV64 registry (cga_bv_get_a/b), re-proves each (a, b) pair in the CIC kernel (tc_conv), and registers parametric strength-reduction rules into the optimizer's rewrite set. It has KAT coverage (sov_isa_kat 99). But it is ONLY fed by the autocatalyst (cg_autocatalyst), not by invent. The invent engine's discoveries (via SAT, not egraph hallucination) could feed this adoption pipeline but don't.
  - evidence: sov_isa.iii lines 592-631 (sov_isa_adopt_dream): reads from cga_bv_get_a/b (autocatalyst registry), never from invent; invent.iii has no registration into the shared registry.
  - consumer: ag_cycle implicitly (via lf_forge + hs_scan/rp_scan which internally use adoption), but invent is not wired as a registry feeder.
  - **correction:** sov_isa_adopt_dream is a proven adoption pipeline fed exclusively by the autocatalyst (cga_bv_get_a/b), not invent. The gap is real: invent discovers via SAT (gil_forge) and presents discoveries (present.iii) but doesn't register them into the adoption pipeline. However, ag_cycle is NOT a consumer of adoption - it doesn't invoke the adoption pipeline through any path. The actual consumer is daemon_dream_cycle, which explicitly chains discovery->certification->adoption in one operator-invoked call.

## Intent / natural-language layer

*State:* The III system has TWO parallel intent architectures: (1) a STRICT INTENT COMPILER (intent/lex_ontology → intent_lex → disambiguate → synthesis_bridge → intent_attest) with cryptographic attestation and temporal bounds, fully proven via 3 KATs but NEVER CALLED by live code; (2) a HIP NL PARSER (verba/nl_lex → nl_parse → hip → ai_resolve) with rich vocabulary and flexible disambiguation, actively used in production. These two stacks are completely isolated with no bridge between them.

- **[cashin/high]** dis_resolve: Strict lexical disambiguation via domain masks — _confirmed_
  - detail: disambiguate.iii exports dis_resolve(text, len) → u32 (returns RESOLVED/CONTRADICTION/AMBIGUOUS/EMPTY). Collapses human intent by bitwise intersection of domain masks across recognized lexemes; returns unique bit on success or rejects with reason code. KAT 1745 validates all 10 test arms including 'lock database'→RESOLVED, 'encrypt network port'→CONTRADICTION, 'lock system'→AMBIGUOUS. NEVER CALLED by any live code.
  - evidence: File: STDLIB/iii/intent/disambiguate.iii, lines 37-56 (dis_resolve), 114-118 (lo_mask_of via lex_ontology). Test: STDLIB/corpus/1745_intent_disambiguate.iii lines 22-43. Grep: zero results for 'dis_resolve' outside corpus and intent/ module definitions.
  - consumer: ai_resolve.iii or a new strict-intent variant of hip.iii would naturally call dis_resolve to enforce absolute semantic uniqueness, rejecting ambiguous prose where hip_resolve would return multiple candidates.
  - **correction:** Minor: Test count is 11 arms, not 10 (test assertions for returns 1-11 in main())
- **[cashin/high]** sb_synthesize + temporal capability bounding (Perfectly-Executed-Mistake safeguard) — _confirmed_
  - detail: synthesis_bridge.iii exports sb_synthesize(parent_cap, resolved_mask, expires_at) → u64, sb_is_bounded(cap_id) → u8, sb_rights_of(resolved_mask) → u64. Creates temporally-bounded child capabilities from intents so even flawed intent decays when epoch elapses. KAT 1746 validates that synthesized capabilities are genuinely bounded (non-zero expiry) and distinct child rights per action (DISK, CRYPTO, NET). NEVER CALLED by any live code.
  - evidence: File: STDLIB/iii/intent/synthesis_bridge.iii, lines 54-58 (sb_synthesize), 62-65 (sb_is_bounded), 37-42 (sb_rights_of). Test: STDLIB/corpus/1746_intent_synthesis_attest.iii lines 42-47. No extern declarations of sb_synthesize outside corpus except in intent modules.
  - consumer: omnia/resolver.iii (the pattern-resolution engine that currently lacks capability synthesis) or a new module intent_command.iii that implements 'submit intent → synthesize bounded capability → require signature' flow.
- **[cashin/high]** ia_manifest_hash + ia_authorize: Cryptographic intent attestation — _confirmed_
  - detail: intent_attest.iii exports ia_manifest_hash(resolved_mask, rights, expires_at, out_32) and ia_authorize(pk, manifest_hash, sig) → u8. Developer constructs Capability Manifest hash (cad over resolved interpretation + rights + expiry); human signs with Ed25519; ia_authorize verifies signature. Any tampering to manifest (weaker expiry, different action) fails verification. KAT 1746 validates positive auth (manifest signed by issuer) and negative case (tampered manifest with 0 expiry rejects even with valid signature on original). NEVER CALLED by any live code.
  - evidence: File: STDLIB/iii/intent/intent_attest.iii, lines 30-35 (ia_manifest_hash), 40-42 (ia_authorize). Test: STDLIB/corpus/1746_intent_synthesis_attest.iii lines 49-61 (manifest generation, signature verification, tampering test). No live code imports or calls these functions.
  - consumer: A new 'intent submission' entry point module that chains: dis_resolve → sb_synthesize → ia_manifest_hash → require human signature before emission. Would give users absolute control over intent scope.
  - **correction:** These are stdlib leaf cryptographic primitives (@export annotated). The normal state for such primitives is that they are called only by their KAT test, which is satisfied here: no in-repo caller other than KAT 1746 exists, and no dynamic dispatch mechanism can invoke them. The claimed consumer is explicitly "a new module to be built" — its non-existence confirms the gap-analysis premise. Capability is real and proven (not dead code).
- **[cashin/medium]** sb_lower_to_egraph: Intent lowering into e-graph optimizer — _confirmed_
  - detail: synthesis_bridge.iii exports sb_lower_to_egraph(resolved_mask) → u32 (returns e-graph node id or SB_EG_SENT sentinel). Lowers resolved intent into the synthesis e-graph (numera/egraph) so intents enter same equality-saturation + cost-minimization pipeline as native code. Distinct interpretations (FREEZE/ENCRYPT/NET_DENY) hashcons to distinct nodes; same intent returns same node. KAT 1747 validates hashconsing (VBD_FREEZE consistent) and rejection of unresolved masks. NEVER CALLED by any live code.
  - evidence: File: STDLIB/iii/intent/synthesis_bridge.iii, lines 72-82 (sb_lower_to_egraph). Test: STDLIB/corpus/1747_intent_egraph_lower.iii lines 21-41 (node creation, hashconsing, rejection). eg_add is called (from egraph.iii) but sb_lower_to_egraph is not invoked by any production code path.
  - consumer: numera/egraph.iii or numera/synthesis_spec.iii would naturally consume this to enable 'intentional optimization' - intents competing against hand-written code in cost-directed synthesis.
- **[gap/high]** No bridge between strict intent compiler (intent/*) and HIP NL parser (verba/hip) — _confirmed_
  - detail: Two fully implemented but isolated intent-to-execution stacks: (1) intent/lex_ontology + intent_lex + disambiguate + synthesis_bridge + intent_attest (strict, cryptographic, temporal bounds); (2) verba/nl_lex + nl_parse + hip + ai_resolve (flexible, richer vocabulary, role-based resonance scoring). HIP parser does NOT call dis_resolve, sb_synthesize, or ia_authorize. No entry point chains them. Intent_lex.iii Phase 2 is complete but never invoked - HIP uses nl_lex_tokenize instead. Result: no 'submit-and-resituate' path where users force strict disambiguation with cryptographic guarantees.
  - evidence: File: STDLIB/iii/verba/hip.iii, lines 31-33 import nl_lex_tokenize from nl_lex.iii NOT intent_lex.iii. Build script STDLIB/scripts/build_stdlib.sh lines 556-565 lists both stacks. Grep for intent/ module calls: zero in verba/ or omnia/ai_resolve.iii. Two completely separate extern function sets.
  - consumer: A new integration module (intent_command.iii or hip_strict_variant.iii) should provide: submit_intent_strict(text) → dis_resolve → sb_synthesize → ia_manifest_hash → signature_challenge → emit bounded capability.
- **[furtherdev/high]** strict_intent_submit entry point: user intents with cryptographic scope lock — _confirmed_
  - detail: A new module providing submit_strict_intent(text_ptr, len, cap_id, out_manifest_hash, out_challenge) that chains: (1) dis_resolve to enforce EXACTLY ONE interpretation; (2) sb_synthesize to create temporally-bounded capability from resolved intent; (3) ia_manifest_hash to content-address the (interpretation, rights, expiry); (4) challenge user to sign manifest with Ed25519. If user's signature verifies, the bounded capability is emitted. Safety property: scope creep is prevented by manifest signature - any change (weaker expiry, different action, different target) changes hash → signature fails → emission refused. First-principles property: users can lock intents to exact specification before execution.
  - evidence: All 4 phases proven: dis_resolve (1745), sb_synthesize+sb_is_bounded (1746), ia_manifest_hash+ia_authorize (1746), sb_lower_to_egraph (1747). Missing: orchestration module. Compare to hip_resolve flow (STDLIB/iii/verba/hip.iii lines 575-650) - similar architecture but no signature gate.
  - consumer: A new endpoint for deterministic, high-assurance intent submission; suitable for privilege escalation, capability grant, or audit-critical operations.
  - **correction:** The claim correctly identifies the gap: all 4 phases are proven in isolation (1745=dis_resolve, 1746=sb_synthesize+ia_manifest_hash, 1747=sb_lower_to_egraph), but the orchestration entry point `submit_strict_intent` that chains them does not exist. Compare to hip_resolve which has similar multi-stage architecture but omits the ia_authorize signature gate. This is furtherdev work, not a missing primitive.

## Sovereign witness / develop-up / katabasis / metal

*State:* III's witness and reversibility substrate is mostly built and proven via KATs, but two major gateways remain unplugged: develop_up (the unified organ gateway) and sovereign_witness (external-trace analyzer) have no live consumers, only corpus tests. Reversible transactions publish witness fragments but lack cross-layer orchestration; VMEXIT metadata for deterministic rollback is enumerated but not wired to actual reversible recovery.

- **[cashin/high]** develop_up gateway unifies 10 organs but unplugged from live path — _partial_
  - detail: aether::develop_up (du_encapsulate, du_write, du_read, du_checkpoint, du_observe, du_guard, du_restore, du_charge_cpu) composes all ten develop-up slices (lifecycle + membranes) into a single sealed-box gateway, yet no live module in STDLIB/iii invokes it. Only corpus tests (1585, 1597, 1599) exercise it.
  - evidence: /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/aether/develop_up.iii lines 69-150 export 8 functions; grep search found zero calls from /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii (only self_atlas_data and corpus)
  - consumer: No live consumer; candidate: a top-level guest-entry or hypervisor-interface module (not yet implemented)
  - **correction:** develop_up gateway exports 18 functions, not 8—the 8 core lifecycle functions (du_encapsulate, du_write, du_read, du_checkpoint, du_observe, du_guard, du_restore, du_charge_cpu) plus du_run, du_replay, du_replay_begin, du_attest, and 4 membrane exposures (du_egress_safe, du_mem_allowed, du_reverse_route, du_ingress_admit, du_ingress, du_health).
- **[cashin/high]** sovereign_witness external-trace analyzer proven but isolated to KAT — _confirmed_
  - detail: sanctus::sovereign_witness (sw_prove_affine, sw_witness, sw_analyze, sw_admit, sw_replay_at, sw_replay_violation, sw_replay_seq) provides sound affine-access safety verdict + replay for external legacy traces with tamper-evident re-derivation teeth, yet no live module calls it. Only corpus (416-419) and own cross-validation (ac_in_bounds) reference it.
  - evidence: /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/sanctus/sovereign_witness.iii lines 68-256 mark @export; no live call-sites in /STDLIB/iii except self_atlas_data metadata. The module composes legacy_artifact + cad sealing as intended (lines 19-29).
  - consumer: Candidate: a forensic recovery or external-trace audit pipeline module (not yet integrated)
  - **correction:** Claim incomplete: 10 @export functions exist, not 7 listed. Beyond the core 7 (sw_prove_affine/analyze/witness/admit/replay_at/replay_violation/replay_seq), also @export are sw_prove_aligned (line 274), sw_xv_agree (297), sw_crossval_affine_kat (311). All three additional are proven — alignment property + cross-validation KAT.
- **[furtherdev/high]** Metal-layer reversibility: SID pre-image routing (katabasis_vmexit_inverse) unused for deterministic VMEXIT unwind — _confirmed_
  - detail: katabasis::vmexit (katabasis_vmexit_inverse lines 71-80) classifies each exit's SID inverse (KVX_INV_HOST_STATE for INVALID, KVX_INV_COW for NPF, etc.), designed to route fail-closed reversals back to the pre-image, yet no enclave or VMEXIT-handling layer composes this with the reversible transaction envelope. A true 'rollback via SID pre-image' would require vmexit_inverse output to drive rev_begin/rev_record calls—not observed.
  - evidence: /c/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/katabasis/vmexit.iii lines 71-80 @export katabasis_vmexit_inverse; called by enclave.iii line 61 but result not fed to reversible envelope. Design draft: plan 4.8 (line 4) specifies inverse but implementation stops at taxonomy.
  - consumer: enclave.iii, sid_router.iii (read inverse); candidate: vmexit_rollback orchestrator (speculative, first-principles VMEXIT->reversible bridge)
  - **correction:** Metal-layer reversibility: SID pre-image routing (katabasis_vmexit_inverse) is designed and exported for deterministic VMEXIT unwind, yet no VMEXIT handler implementation wires the routed inverse to reversible envelope (rev_begin/rev_record). The taxonomy exists (katabasis_vmexit_inverse lines 71-80); the routing layer exists (sid_router.iii sr_route/sr_record_and_mutate); but the actual handler orchestration that would call sr_record_and_mutate based on the routed kind does not exist. sr_record_and_mutate is implemented but unused. du_reverse_route (develop_up.iii:182) is a read-only query membrane, not an orchestrator. The gap is acknowledged by sid_router.iii header: "no slice RUNS it" (line 10). Implementation is deferred: RING-MINUS-1-PLAN.md increments I3–I5 schedule handler wiring (lines 151–157).

## Multi-valued logic frontier (logic6) + where to cash it in

*State:* The logic6 module (Belnap + Null/All-present De Morgan algebra) is mathematically complete and proven (KAT 1752, returns 99). The gill6 invention engine for 6-state collapse is also proven (KAT 1753, returns 99). However, neither is wired into live compilation paths. The actual cash-in is not the forge itself, but using logic6 primitives to formalize existing modules' implicit bottom elements and contradiction-tolerant states that already exist but lack formal semantics."

- **[cashin/high]** L6_BOTH (paraconsistency) in Byzantine consensus replica disagreement — _refuted_
  - detail: triple_check.iii::tc_execute (line 25) records 2-1 disagreement as TRIPLE_DISAGREE_2_1 without formal contradiction reasoning. Should encode contested values as L6_BOTH (Belnap: Both AND NOT Both = Both, not False).
  - evidence: C:/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/aether/triple_check.iii lines 1-12, 40-44; logic6.iii:30 proves L6_BOTH property; KAT 1752 return code 36 tests paraconsistency
  - consumer: triple_check.iii::tc_execute
  - **correction:** The capability exists: L6_BOTH is defined in logic6.iii:30, proven paraconsistent in KAT 1752 (STDLIB/corpus/1752_logic6.iii lines 35-36). But tc_execute (STDLIB/iii/aether/triple_check.iii line 222) does not and should not use it—current design returns raw u64 majority values with TRIPLE_DISAGREE_2_1 return code, which is intentional per design philosophy at line 12: "no voting heuristic and no best of".
- **[cashin/high]** L6_NULL as formal bottom in interval_lattice safety preconditions — _confirmed_
  - detail: interval_lattice.iii uses implicit bottom (lo>hi) with no formal element. Should formalize L6_NULL: missing facts in safety preconditions = L6_NULL (absorbing bottom), cannot silently become safe values.
  - evidence: C:/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/interval_lattice.iii lines 41-45, 60-68; logic6.iii:27-31 proves L6_NULL annihilates meet
  - consumer: sovereign_optimizer.iii lines 50-53 (rc_proven_safe, be_eliminable, vrp_no_overflow)
- **[cashin/medium]** L6_NULL formalizes RP_ANY (unknown parity) in reduced_product — _refuted_
  - detail: reduced_product.iii uses RP_ANY=2u32 as absorbing element for unknown parity. Should use L6_NULL to make algebraic structure explicit: unknown parity is bottom in the parity sublattice.
  - evidence: C:/Users/Edwin Boston/OneDrive/Desktop/III/STDLIB/iii/numera/reduced_product.iii lines 30-32, 67-71; shows RP_ANY absorption in parity transfer
  - consumer: sovereign_optimizer.iii lines 55-56 (rp_reduce_lo/rp_reduce_hi integration)
  - **correction:** RP_ANY and L6_NULL represent different concepts in different domains. RP_ANY (2u32) is the absorbing element for a 3-value parity domain, while L6_NULL (4u32) is the infimum of a 6-value truth lattice. They are numerically and semantically distinct and should not be conflated. No cashin is applicable.
- **[furtherdev/high]** Null-safety proofs via L6_NULL in compiler type/flow analysis — _partial_
  - detail: Inject L6_NULL into compiler's type system (proof.iii, sema.iii) to prove no undefined behavior: any uneliminated undefined-behavior precondition (null dereference, out-of-bounds, division by zero) becomes L6_NULL (absorbing proof obligation, not silent safety).
  - evidence: C:/Users/Edwin Boston/OneDrive/Desktop/III/COMPILER/BOOT/proof.iii — no logic6 references; logic6.iii:1-24 provides complete Belnap algebra for null-safety semantics
  - **correction:** GAP (Incomplete Feature): L6_NULL infrastructure exists and is proven in logic6.iii, but the capability is NOT integrated into compiler type/flow analysis. L6_NULL is currently used only in the STDLIB invention/optimization engine (numera_invent 6-state forge), not in proof.iii or sema.iii compiler safety proofs. Integration into compiler proof system remains unimplemented.

## Cross-cutting cash-in census (the meta-lens)

*State:* The III system contains 600+ exported functions with comprehensive corpus KAT coverage (~131 tests). Systematic audit identified 40+ proven capabilities that are @exported, corpus-tested, yet orphaned from live wiring—never called by production modules, only by their own KATs. These are completed building blocks awaiting integration into the execution pipeline.

- **[cashin/high]** Intent synthesis pipeline (Phase 4) orphaned from execution orchestrator — _confirmed_
  - detail: sb_lower_to_egraph + 4 sister functions (sb_synthesize, sb_rights_of, sb_requires_time_oracle, sb_is_bounded) in intent/synthesis_bridge.iii are Phase 4 of the intent-to-execution pipeline. They compose intent resolution into the optimizer's e-graph for equality saturation, but nothing calls them. The 5-phase intent flow (lex → resolve → attest → synthesize → lower-to-egraph) is incomplete: phases 1-3 are isolated in their modules; phase 4 is orphaned.
  - evidence: STDLIB/iii/intent/synthesis_bridge.iii:72 (sb_lower_to_egraph @export); STDLIB/corpus/1747_intent_egraph_lower.iii (KAT calls it 6 times, tests both success and failure); grep shows zero calls from STDLIB/iii outside this module.
  - consumer: ripple/ripple_synthesizer.iii or a new intent_execute orchestrator that binds all 5 phases in sequence (currently missing)
- **[cashin/high]** Constitutional compliance verification unplugged from witness chain — _confirmed_
  - detail: cp_verify_segment, cp_verify_epoch, cp_attach_clause (and 3 siblings) in numera/constitution_preserver.iii verify constitutional clauses against chain segments and publish compliance witnesses. Fully tested; never invoked. The preserver is the enforcement engine; nothing calls it to actually verify epochs against ratified clauses.
  - evidence: STDLIB/iii/numera/constitution_preserver.iii:147,215,246 (@export); STDLIB/corpus/1443_gate_outcomes_constitution_preserver.iii (tests both empty and failed verifications); grep shows only internal calls to helper functions, no external consumer.
  - consumer: aether/hotstuff.iii or consensus/finality module that must verify constitutional compliance at each epoch boundary
- **[cashin/high]** Attestation box identity and verification gate never wired to identity provider — _partial_
  - detail: ab_identity_pub, ab_verify, ab_attests_current in aether/attest_box.iii prove attestation identity and verify signatures. Corpus tests all three; nothing in stdlib calls them. This is the crypto-gated identity layer for attestation; it's built but not integrated into the attestation service.
  - evidence: STDLIB/iii/aether/attest_box.iii:65,82,89 (@export); STDLIB/corpus/1211_attest_box_guest_identity.iii (8 test call sites); zero calls from STDLIB/iii modules.
  - consumer: aether/node_identity.iii or attestation service orchestrator that verifies guest identity signatures and roots
  - **correction:** The capability gates and verify functions exist and are proven in corpus test files 1595_attest_box_remote.iii (6 calls), 1408_autogenesis_attest.iii (3 calls), and 1585_develop_up_gateway.iii (2 calls) - totaling 11 actual function invocations. No stdlib modules reference these three functions; only ab_attest (the main attestation function) is wired into develop_up.iii's gateway, while ab_identity_pub, ab_verify, and ab_attests_current remain test-only despite being exported.
- **[cashin/high]** Remote backend protocol decoder orphaned from message dispatch — _refuted_
  - detail: backend_remote_decode, backend_remote_inject, backend_remote_set_endpoint in aether/backend_remote.iii implement the remote storage backend protocol (encoding/decoding, endpoint management). Fully tested (9 KAT call sites); never called by anything that dispatches remote requests.
  - evidence: STDLIB/iii/aether/backend_remote.iii:150,185,67 (@export); STDLIB/corpus/1389_backend_remote_roundtrip.iii (exercises encode/decode/inject); grep finds zero calls from STDLIB/iii.
  - consumer: aether/reach_store.iii or backend orchestrator (backend_loopback, backend_memo) that routes requests to the correct backend transport
  - **correction:** Remote backend protocol decoder is fully integrated into dispatch: reach() calls backend_remote_get(), which calls backend_remote_decode(). The inject/set_endpoint functions are test/config seams (correctly test-only), but decode is live path.
- **[cashin/high]** Capability forge composition gate orphaned from capability policy engine — _confirmed_
  - detail: cf_forge_split, cf_forge_restrict, cf_deforge_cascade in aether/cap_forge.iii are the trusted disposition logic for splitting, restricting, and cascading capability revocation. Proven (8+ KAT call sites). Never called by anything enforcing capability policy or delegation rules.
  - evidence: STDLIB/iii/aether/cap_forge.iii:340,380,526 (@export); STDLIB/corpus/1356_cap_forge_splitting.iii (tests composition rules); zero calls from STDLIB/iii besides definitions.
  - consumer: aether/capability.iii or a policy enforcement module that must enforce capability rights inheritance and restriction constraints
  - **correction:** Test file citation incorrect: referenced "STDLIB/corpus/1356_cap_forge_splitting.iii" does not exist; actual tests are in 1611_cap_forge_deforge_opid.iii and 1421_jit_mandate_map_forge.iii. Call site count: ~10 total (5 in KAT tests, 5 in corpus tests), not "8+ KAT" as claimed.
- **[cashin/medium]** Capability handshake offer-accept protocol never orchestrated — _partial_
  - detail: cap_offer_pattern_set, cap_accept_offer, cap_negotiate_session in aether/cap_handshake.iii implement the cryptographic capability-exchange protocol (SHA-256 negotiation + pattern-set binding). 4+ KAT test call sites. Never called by anything coordinating capability sessions between peers.
  - evidence: STDLIB/iii/aether/cap_handshake.iii:159,176,200 (@export); STDLIB/corpus/1364_cap_handshake_negotiation.iii (tests both accept and negotiate arms); grep shows zero external calls.
  - consumer: aether/sealed_channel.iii or capability-session orchestrator managing peer capability agreements
  - **correction:** Replace evidence citation "STDLIB/corpus/1364_cap_handshake_negotiation.iii (tests both accept and negotiate arms)" with: "STDLIB/corpus/208_cap_handshake.iii (comprehensive tests: offer-send, accept-matching, accept-reject, negotiate-determinism, session-verify, drop, incompatibility)" and "STDLIB/corpus/1608_babel_wire_verify_len.iii (gate testing for offer/accept)" and "STDLIB/corpus/1319_cap_handshake_taint.iii (taint proof KAT)"
- **[cashin/medium]** Bone marrow stripe operations isolated from erasure-coded replication — _confirmed_
  - detail: bm_encode_stripe, bm_verify_block, bm_scrub, bm_recover in aether/bone_marrow.iii implement erasure-code operations (FEC encode/verify/scrub/recover). Tested; never called by storage/replication orchestrator despite being the core coded-stripe engine.
  - evidence: STDLIB/iii/aether/bone_marrow.iii:446,470,482,588 (@export); STDLIB/corpus/1327_bone_marrow_stripe_integrity.iii (8 call sites testing encode/scrub/verify); zero calls from STDLIB/iii.
  - consumer: aether/reach_store.iii or replication/erasure-code coordinator that must stripe data, recover lost blocks, and scrub parity
- **[cashin/medium]** Babel wire protocol unpack gate orphaned from message unmarshaling — _partial_
  - detail: babel_wire_unpack_seal_id, babel_wire_unpack_cap_ref, babel_wire_unpack_intent_id (and 7 siblings) in aether/babel_wire.iii deserialize Babel envelopes into typed fields. Each proven by KAT. Never called by wire message handler or serialization/deserialization pipeline.
  - evidence: STDLIB/iii/aether/babel_wire.iii:214,220,226 (@export); STDLIB/corpus/1198_babel_wire_deserialize.iii (3+ call sites per function); grep shows zero stdlib consumers.
  - consumer: aether/http_client.iii, aether/http_server.iii, or Babel unmarshaling handler that must extract typed fields from wire envelopes
  - **correction:** The functions are proven and orphaned as claimed. The evidence should cite corpus/207_babel_wire_roundtrip.iii rather than the non-existent corpus/1198_babel_wire_deserialize.iii. Cap_handshake.iii in the unmarshaling pipeline verifies but never extracts seal_id/cap_ref/intent_id values.
- **[cashin/medium]** IPC backend storage gate never wired to unified backend dispatcher — _refuted_
  - detail: backend_ipc_has, backend_ipc_get, backend_ipc_put in aether/backend_ipc.iii are the local IPC-backed store. Proven. Never called by anything that multiplexes across backends (IPC, memory, remote, loopback) based on address.
  - evidence: STDLIB/iii/aether/backend_ipc.iii:119,126,153 (@export); STDLIB/corpus/1293_backend_ipc_local_store.iii (3 call sites); zero calls from STDLIB/iii modules.
  - consumer: aether/reach_store.iii or backend dispatcher orchestrator that routes get/put/has based on address range
  - **correction:** The IPC backend IS wired. reach_core.iii is the dispatcher and calls backend_ipc_get and backend_ipc_put. Minor gap: backend_ipc_has (line 119) is exported but unused elsewhere.
- **[cashin/medium]** Loopback backend network server orphaned from request loop — _refuted_
  - detail: backend_loopback_listen, backend_loopback_serve_once in aether/backend_loopback.iii implement the in-process loopback network backend (test/debugging transport). Proven (3 KAT call sites). Never called by anything that drives the accept/handle loop.
  - evidence: STDLIB/iii/aether/backend_loopback.iii:49,131 (@export); STDLIB/corpus/1287_backend_loopback_echo.iii (tests listen + serve_once); grep shows zero consumers.
  - consumer: aether/reach_store.iii or backend server loop orchestrator managing the listen/serve/multiplex cycle
  - **correction:** The loopback functions are NOT orphaned—they have proven test coverage (832_reach_loopback.iii, 3 call sites). No stdlib orchestrator calls them by design: they are composable primitives intended for application-level composition, not stdlib-managed server loops. The cited evidence file (1287_backend_loopback_echo.iii) does not exist; actual test is 832_reach_loopback.iii.
- **[cashin/medium]** Bigint division algorithms proven but unintegrated into division dispatcher — _refuted_
  - detail: bigint_div_qr (quotient-remainder division) and related division helpers in numera/bigint_div.iii are fully tested (2+ KAT call sites for qr variant) but never called by anything that needs to dispatch multi-word division. The core algorithm is proven; the wiring is missing.
  - evidence: STDLIB/iii/numera/bigint_div.iii (multiple @export division functions); STDLIB/corpus/47_bigint_div_qr.iii (tests quotient-remainder); zero calls from STDLIB/iii except via bigint.iii which itself is widely used (not a gap, but qr-specific dispatch is missing).
  - consumer: numera/bigint.iii division dispatcher or arithmetic synthesizer that must choose algorithms per operand width
  - **correction:** bigint_div_qr and related helpers ARE integrated into the division dispatcher via bigint_mod, which implements the public division API and internally calls bigint_div_qr. This integration is proven by three independent consumers (Barrett reduction, modular inverse, core modulo) that rely on the bigint_mod -> bigint_div_qr chain.

