# III — E-II: The Exact Loschmidt Echo · E-III: The 3D Photonic Crystal Cavity

Gates **2482, 2483**, runner `STDLIB/scripts/run_sqrtsum_kats.sh`. Organs: `gas.iii` (+`gs_kick`,
the momentum-impulse verb), `crystal3.iii` (new). Companions to the Turing-substrate charter
(2167–2198) and DOCS/III-TRIVARIATE-BREP.md (E-I).

## E-II — the exact Loschmidt echo (gate 2482)

The Loschmidt gedanken, run exactly: `F^n · R · F^n · R` from the post-event-1 origin (the
2433-gated involution protocol — R re-applies the last response, so its fixed point is a
post-event state).

- **Perfect echo**: defect 0, bit for bit (theorem arm).
- **Imperfect reversal** = R followed by ONE momentum quantum (`gs_kick`, its own inverse under
  composition — the kick-unkick arm returns defect 0, also a theorem). At n = 7 the echo defect
  is an EXACT integer observable: 3 velocity slots, Σ|Δv| = 17, 4 position slots, and an energy
  shift of exactly 7 = 2v+1 with v = 3 — the kick's energy identity.
- **The depth table** (n = 5, 7, 9 → defects (2,7), (3,17), (1,1)): the machine CONFIRMS the
  2189 adjudication live — the n = 9 defect is SMALLER than the n = 5 defect. Separation in a
  bounded exact gas FLUCTUATES; the gate pins values and would redden on any smuggled exponent.
- **The KS adjudication**: Kolmogorov-Sinai entropy is an asymptotic, measure-theoretic limit —
  no finite exact run certifies it, and the substrate refuses to fake it. What IS delivered:
  the event skeleton of a 2500-step exact trajectory (gas_big under the den-bits-48 law) and its
  EXACT distinct-word counts — W(1..8) = 22, 265, 2124, 2471, 2493 (near-maximal diversity) —
  against the integrable one-particle control (W ≡ 2, flat in L). "Chaotic-looking vs periodic"
  as integer combinatorics; the limit object honestly refused.

## E-III — the 3D photonic crystal cavity (gate 2483)

`crystal3.iii`: the 2194 crystal closed into 3D — x-walls at 0/W AND y-walls at 0/V (the new
mirror family), z-strata of alternating L/D classes (√2 / √6 optical speeds), end-caps closing
the cavity or an aperture opening it. State: each axis a rank-4 ℚ(√2,√3) slot vector over one
den; every face-ordering decision one `q23_sign` query; certificates consumed from photon3's
closure constants (TIR margins +2/+6, Snell residues 0).

Machine-found results, all pinned over 300 certified events:

- **The axis-stratified rank bound** (the 3D escalation of 2194's subring theorem): the
  TRANSVERSE axes never form √3 — their reachable ring is rank-3 {1, √2, √6} — while the
  OPTICAL axis reaches full rank-4 (√6-legs over √2-carrying distances deposit 2√3 into z
  alone). Census: 56 x-mirrors, 76 y-mirrors, 112 interfaces, 56 caps.
- **The crystallographic den bound**: the denominator's prime support is EXACTLY {2,3}
  (trial-divided to 1) — mirrors never scale den; interfaces scale by k ∈ {2,6}.
- **Certified trapping**: exact six-face membership (q23_sign) at every event; every event
  carries its class certificate; 300 states pairwise distinct (non-recurrence); the aperture
  control escapes at event 2 — trapping is the cap's doing, witnessed by its absence.
- **Exact interferometric phase**: the traversal clock is a rank-4 field element. The x↔y
  mirror symmetry's clock difference is an EXACT ZERO certified by q23_sign (constructive
  interference as an algebraic identity), with the event skeletons mirroring event-for-event;
  the inequivalent-lattice pair differs by exactly ΔT = −24√2 − 22√6 — rational slot ZERO, so
  a surd-blind comparator declares the cavities phase-equal while q23_sign decides −1 (the
  float-blindness witness at the phase level).
- **The tie discovery**: equal transverse speeds make exact corner re-alignment STRUCTURAL
  (u−v is invariant under the shared clock — every config tied within ≤46 events, certified and
  refused). Incommensurate integer speeds (|sx| ≠ |sy| from {1,2,3}, which preserves the {2,3}
  den support) dissolve it: 400+ events tie-free. The machine found the wall; the geometry
  answered it.

Envelopes: gas echo within the i64 ensemble (~12 events/leg); crystal3 i64 slots under guards,
2048-event log; charted v2 = the bigint crystal and deeper echoes on gas_big with a g2 kick verb.

Calibration: every claim is a gated-fact (2482/2483); the one refusal-by-design (KS entropy from
finite data) is the adjudication itself.
