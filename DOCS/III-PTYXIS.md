# PTYXIS — THE FOLD

*The de-exponentiation faculty: the one law beneath seven organs, named, recognised, dispatched, composed.*
*Session 2026-07-19.*

---

## 0. The one sentence

**Exponential explosion is an artifact of representation.** Wherever a problem carries a *generating
structure*, the exact answer is DERIVABLE and DECIDABLE from a small generating set, without ever
materialising the exponential. III has proven this, gated, in six domains — each an organ that never
knew it was the same organ. PTYXIS is the seventh: the meta-organ that names the law, recognises which
generating structure a problem has, dispatches the de-exponentiation, refuses the shadow where no
structure exists, and composes folds into what none does alone.

## 1. The law, seven masks

Every one of these organs makes the *same move* — hold the generators, derive the instance on demand,
decide by an exact predicate, never enumerate:

| Fold | generating structure | exponential → | the exact decision | organ |
|------|---------------------|---------------|--------------------|-------|
| **GF2** | GF(2) linear span (stabilizer) | 2^K timelines → K colors | GF(2) solve / linear collapse | `cassandra` / `aether_lens` |
| **SYMMETRY** | torus / equivariant action | ∞ integral → finite fixed points | localization → closed-form product | `meris` (Nekrasov) |
| **CONGRUENCE** | an equivalence with congruence | expo forms → one e-class | `eg_union` collapse, congruence propagate | e-graph (`2117_field_superpos`) |
| **FUNCTION** | bounded codomain | ∞ circuits → 256 functions | canonical signature (zero SAT) | `weave_explosion` / Toffoli group |
| **CORRELATION** | shared error sources | interval blowup → bounded affine | noise-symbol cancellation | `zonos` |
| **TILT** | perfectoid / Frobenius tower | ∞ tower → finite shadow | the tilt correspondence, exact | `klisi` (perfectoid) |
| **ORDER** | discrete order over exact values | enumerate-argmax → decide-by-sign | `sign(Σaᵢ√bᵢ)` / exact bigint | krisis / `sqrt_sum_sign` |

They are not analogies. They share *primitives*: `sqrt_sum_sign` decides `aether_lens`'s depth *and* the
R1 krisis; GF(2) is `cassandra`'s basis *and* the stabilizer fragment of `kyma`; `pyrgos` exact rationals
underwrite `kyma`, `meris`, `klisi`. **One exact substrate that refuses to materialise the exponential,
pointed at six kinds of explosion in turn.**

## 2. What PTYXIS realises (BUILT + GATED — `STDLIB/iii/omnia/ptyxis.iii`, `ptyxis_selfprove()=0`)

- **The GF(2) fold, the cleanest exact instance (the Gottesman–Knill shape).** K generators held in an
  echelon basis; `pt_derive(mask)` materialises any of the 2^K instances by XOR; `pt_member` DECIDES span
  membership by GF(2) solve without enumeration; `pt_collapse` names the surviving subset at observation.
  **Live: 20 generators hold 2²⁰ ≈ 1.05 M instances in 20 words.**
- **The recogniser + the refusal.** `pt_add_gen` admits a direction IFF it grows the rank; a *dependent*
  direction is REFUSED and counted — the fold never inflates 2^K by admitting a redundant generator.
- **No self-grading.** Every de-exponentiation is cross-checked against the brute exponential it replaces
  (`pt_member` == `pt_brute_member`; distinct-count == 2^K; the fold argmax == the brute 2^K argmax).
- **The cross-composition FOLD_GF2 ⊗ FOLD_ORDER — the crown.** For a separable exact score, the argmax over
  the 2^K candidate set is `{i : wᵢ > 0}` — O(K) exact-sign decisions, the exponential NEVER enumerated,
  and it agrees with the brute 2^K argmax. **This is certified beam over an exact order with no exponential
  memory** — the R1-beam-decoding vision in miniature, gated.
- **The dispatcher.** `pt_dispatch(tag)` routes each generating structure to its organ, and returns
  `FOLD_NONE` — the HONEST WALL — for a problem with no generating structure (genuinely exponential, never
  faked). `pt_recognise_gf2` folds n directions to their rank live.

## 3. The cross-compositions the fold unlocks (the campaign)

The organs have never been composed *against each other*. PTYXIS is the socket; these are the plugs — each
a capability no single organ has:

1. **Certified beam over R1 (GF2 ⊗ ORDER, realised in miniature).** Hold 2^K candidate continuations as K
   GF(2) colors (`cassandra`), collapse each token by the exact-order krisis. No exponential memory; every
   adoption certified.
2. **A stabilizer-rank quantum simulator, exact (GF2 ⊗ KYMA).** `cassandra` holds the Clifford part as GF(2)
   colors (free); `kyma` pays exact full-amplitude only for the Toffoli/T "magic," priced by its 7-T
   decomposition — an exact explorer of the stabilizer/magic frontier, no float anywhere.
3. **The softmax is a partition function (SYMMETRY ⊗ neural).** `meris`/Nekrasov localizes partition
   functions to fixed points and closed forms; where a symmetry exists in the pass, the sum need not be summed.
4. **Perfectoid tilt as certified precision reduction (TILT ⊗ ORDER).** Hold the char-0 exact hidden as a
   char-p finite shadow chosen so the argmax *order* is preserved — a decidability-preserving compression.

## 4. Calibrated

- **GATED**: the fold (GF2 de-exp sound vs brute), the recogniser + refusal, the GF2⊗ORDER cross-composition,
  the dispatcher's route-or-refuse — `ptyxis_selfprove()=0`, byte-deterministic twice. Probe `build/mantis/ptyxis_probe.exe`.
- **NAMED + ROUTABLE**: the full seven-fold taxonomy, each pointing at its landed organ.
- **CHARTERED (the campaign)**: the four cross-compositions of §3 — the plugs for the socket now built.

The seven organs are one fold. Hold the generators, decide by exact predicate, refuse the shadow.
