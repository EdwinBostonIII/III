# III — REUNIFICATION DIAGNOSIS & PLAN
### "Splinters into a cabin": the honest current-state map + the path back to ONE source of truth
> **Date:** 2026-06-20 · **Author pass:** /deep-think · /receiving-code-review (verify-before-assume)
> **Status:** DIAGNOSIS COMPLETE · PLAN PROPOSED — destructive steps gated on user direction (§7 forks)
> **Method note:** Every claim below was read from git history + the live tree this session. Where the
> user's recalled mental model and the repo disagree, the repo state is stated and the divergence flagged.

---

## 0. THE NORTH STAR (restated in your terms)

ONE and only one **source of truth** that defines the source base, upon which all logic is created,
derived, and balanced. That source is **XII** — the woven systemwide math (rewrite / canonicalize /
lower / critical-pairs / confluence / morphism / lattice). Beneath it, **KATABASIS** is the deep
driver/hypervisor descent (Ring 3 → −1 → −2). The "rival" primitives (cg3 / cgiii / iiis-2 / gc / the
C seed) get their good parts **harvested and phase-incremented into XII** until no borrowed,
unoriginal math sits on a "different playing field" for different parts of the system.

---

## 1. CURRENT STATE — WHAT IS ACTUALLY TRUE (verified)

- **XII already IS the one substrate, and it is alive and central.** `STDLIB/iii/omnia/xii_*.iii`
  is a ~40-module family: `xii_canonicalise`, `xii_rewrite`, `xii_lower_{compose,decide,iterate,then,
  with,under,program}`, `xii_rule_{patterns,overlap,verify}`, `xii_critpair_enum`, `xii_discharge`,
  `xii_fusion_verify`, `xii_morphism`, `xii_lattice`, `xii_hj`, `xii_circ`, `xii_chd`, `xii_kernel_emit`,
  `xii_horizon{,_reach}`, `xii_basis`, `xii_savings`. **The destination of the reunification exists.**
- **The compiler "rivals" are LARGELY already unified.** Per the self-host state, `cg_r3.iii` + `sema.iii`
  ARE the active compiler (`iiis-2`); the C data-model accessors were deleted; `iiis-2 == iiis-3` fixpoint.
  The C seed (`iiis-0`) survives only as a cold-bootstrap, generated by `COMPILER/BOOT/*.py`.
- **The parity attack did NOT crack parity — and that is the documented win.** It proved the *wall*
  formally (DOCS `*-WALL-FORMAL*` + grail KATs `1838–1881`), then generalized the template to 10 walls
  (SAT, confluence, GI, lattice, primality, comm-complexity, Ramsey, constructibility, Goodstein).
- **The grail research genuinely advanced parity solvers** — VJ / LRC strategy-improvement, small-progress-
  measures, bounded-dominion, tri-solver cross-val, Groebner-parity — preserved as corpus KATs `1839–1847`.
- **The "walls cash-in" faculties were BUILT, then CUT.** Commit `65783306` removed 10 `numera::*`
  faculties (parity_game, sat_tractable, confluence, graph_refine, diophantine, con_lattice, constructible,
  ramsey, comm_lb, goodstein) as **duplicates/demos** of existing organs (sat_tractable≈`sat`,
  confluence≈`xii`, con_lattice≈`congruence`, diophantine≈`crt`), each referenced only by its own gate.
  **The grail RESEARCH (docs + KATs) was explicitly NOT cut.**
- **The Python gate was already de-infected** in the latest commit `32a8d275` (removed `gate_dead_imports.py`
  + `_weave_*` scratch). The correction has begun.

> **Reconciliation of your memory vs. the repo:** the cuts you're uneasy about were mostly correct *as
> deletions of duplicates* — but the **method** (python scripts, word-matching) and the **framing**
> (delete-the-unused vs. harvest-the-unused) violated the ethos. Both are fixable without resurrecting
> duplicate islands.

---

## 2. THE MESS — VERIFIED INVENTORY OF CLUTTER & DEBT

| # | Item | Evidence | Kind |
|---|------|----------|------|
| M1 | 4 stale worktrees (`agent-…`, `wf_…×3`), all `prunable` | `git worktree list` | hygiene |
| M2 | Claude Python scratch in tree: root `pg_*.py` + `softmu_*.py` (7, dated today), `STDLIB/build/corpus/statmech_parity_probe.py` | parity-attack oracles | sovereignty |
| M3 | Python that **generates/gates III**: `gen_self_atlas.py`, `emergence_{discover,cycle}.py`, `self_refactor.py`, `cartographer.py` (legacy build gate) | `build_stdlib.sh` refs; `carto.c`/`self_atlas` already exist in-III | sovereignty |
| M4 | Borrowed reference python: `fips205_slhdsa_{sha2,shake}_ref.py` | crypto KAT vector oracles | borderline |
| M5 | `COMPILER/BOOT/*.py` (7) | cold-bootstrap C-seed generators | legacy / load-bearing? |
| M6 | `_audit_scratch/` — **74 tracked files** (.iii + `parse_findings.py`, `_apply_backlog_1728.py`) | `git ls-files` | clutter |
| M7 | Untracked regenerable build artifacts (`*.clog`, `*.o.s`) littering `git status` | session-start status | hygiene |
| M8 | Grail/wall research = **live-but-uncalled** genuine math (KATs `1829–1881`), not harvested into XII | §1 | **the unspent asset** |
| M9 | Borrowed unoriginal math primitives on a "different playing field" (the pieces from another core) | your acknowledged debt | woven-math debt |

---

## 3. THE PHILOSOPHICAL WOUND (root cause)

The debloat session's instinct was **AMPUTATE** (unused ⇒ delete) decided by **python word-matching**.
The III ethos is **INTEGRATE** (unused-but-genuine ⇒ harvest into the one source) decided by **III's own
organs**. The cuts were largely right *about the duplicates* and wrong *about the method and the asset*.

---

## 4. THE RECONCILING INSIGHT — "unused" is two different things

- **(a) Duplicate wrappers** of an organ that already exists (sat_tractable vs `sat`, …) → **correctly cut.**
  Resurrecting them re-violates *no-islands / one-substrate*.
- **(b) Genuine novel math** sitting unharvested (grail solvers, wall theorems) → **the asset.** The fix is
  neither delete nor leave-dead: **harvest into XII so it is actually used.** That is the cabin.

---

## 5. PROPOSED PLAN (phased; destructive phases gated on §7)

- **Phase 0 — Hygiene (no decisions):** `git worktree prune` the 4 stale trees (M1); `.gitignore` + drop
  untracked regenerable build artifacts (M7).
- **Phase 1 — Sovereign python purge:** delete Claude scratch (M2); verify the in-III replacements
  (`carto.c`, `self_atlas`) cover M3, then retire the python generators/gates; settle M4/M5 with the user.
- **Phase 2 — De-clutter scratch:** retire `_audit_scratch/` (M6) — promote any valuable `.iii` into the
  corpus proper, cut the rest.
- **Phase 3 — HARVEST (the hard deliverable, M8):** for each genuine grail/wall advance, exactly one of:
  (i) **wire** it into a live XII/numera consumer; (ii) **prove subsumed** by an existing organ and retire
  the research KAT; (iii) **re-derive** it in woven-XII math. **No standalone dead faculties left behind.**
- **Phase 4 — Foreign-primitive replacement (M9):** locate + tag the borrowed primitives; schedule the
  woven-XII re-derivation (your acknowledged "replace later").
- **Phase 5 — Unification capstone:** write the single source-of-truth law (all math derives from XII;
  KATABASIS is the deep driver) and add a **sovereign (in-III)** gate that keeps it that way.

---

## 6. RISKS

| Risk | Mitigation |
|------|-----------|
| Re-amputating a genuine asset under time pressure | Phase 3 forbids delete-without-harvest; every retire needs a "subsumed-by-X" proof |
| Removing load-bearing bootstrap python (M5) and breaking cold-start | Verify the self-host fixpoint path before touching BOOT; treat as user fork F2 |
| Repeating the unilateral-direction mistake | §7 forks settled with you before any destructive step |
| Harvest creating NEW islands instead of wiring into XII | Each harvest must land in an existing live consumer, KAT RED-before/GREEN-after |

---

## 7. FORKS I NEED YOU TO SETTLE (before I touch anything destructive)

- **F1 — The cuts:** KEEP the duplicate-faculty cuts and harvest the *research* forward (my recommendation),
  vs. AUDIT/REVERT each of the 10 cut faculties first. I can show you the exact diff of all 10 to veto any.
- **F2 — The python line:** full sovereign purge (replace/retire *all* tree python incl. BOOT + crypto
  oracles with in-III equivalents) vs. pragmatic (purge scratch + generators now; keep cold-bootstrap and
  crypto-vector python as explicitly out-of-tree tooling).
- **F3 — Cadence:** drive the whole arc autonomously with ultracode workflows, reporting at phase gates,
  vs. approve each phase before it runs.
