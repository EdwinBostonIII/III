
> **STATUS: HISTORICAL RECORD** — an executed campaign plan/ledger, kept immutable as evidence (reunification W6).
### THE BOOTSTRAP RESEAL PROCEDURE -- land a cg_r3.iii + cg_r3.c codegen change and re-green the iiis-0->1->2->3 chain.  [feasible=yes-with-care]
VERIFIED against source: the 4-link chain, exit codes, parse traps, and the i2==i3 fixpoint all hold as described (BOOT/iiis-2.mhash == iiis-3.mhash == 4dffd195... confirmed identical on disk; 59 stage1_corpus .iii confirmed; all cited cg_r3.{iii,c} lines/functions exist exactly). Two material corrections raise this from a clean 'yes' to 'yes-with-care'. (A) The cg_r3.c == cg_r3.iii byte-identity differential (build_iiis1.sh --check-corpus, iiis-0 vs iiis-1) is a PURELY MANUAL gate: it appears in NO automation (grep-confirmed) and _k4gate.sh:7 runs build_iiis1 WITHOUT --check-corpus -- so the canonical orchestrator proves only the iii-vs-iii fixpoint (build_iiis2 --check-corpus = iiis-1 vs iiis-2) plus the i2==i3 seal, and NEVER the C-vs-iii dual-source identity. The spec's manual step 4 is the SOLE C==iii gate and is mandatory; _k4gate green is INSUFFICIENT for any cg_r3.{c,iii} edit. (B) The committed HEAD:iiis-1.mhash is 2-column ('hash  iiis-1.exe'); build_iiis1.sh:216 parses it with head|tr (whitespace-stripping), yielding the 74-char junk 'f15d...b7b2iiis-1.exe' that can never equal the 64-char MHASH -- so the iiis-1 golden gate is LATENTLY RED as committed (empirically reproduced). Hash-only reseal resolves it. A second consumer with the identical trap exists at build_xii.sh:87 (cat|tr, exit 3). Everything else in the spec -- the reseal-before-verify hazard, the iiis-1 reseal-before-corpus ordering, the cg_r0/rm2 seam coverage, the verify_step VP-2-dies-before-VP-4 limitation -- is accurate. A cg_r3 emit change moves all four mhashes; reseal once, AFTER the differentials are green, never before.
- `COMPILER/BOOT/cg_r3.iii AND COMPILER/BOOT/cg_r3.c (the wrapped change -- edited TOGETHER)` @ the matched emit pair, e.g. r3_mul_pow2_k (cg_r3.iii:851, dispatch :2457) <-> mul_pow2_k (cg_r3.c:1089, dispatch :1370). VERIFIED: all four locations exist verbatim; contract comments confirmed at cg_r3.iii:428 ('byte-identically in cg_r3.c (mul_pow2_k / emit_shlq_imm) so iiis-0 == iiis-2 holds'), cg_r3.iii:849-850 ('x*2^k == x<<k mod 2^64 for signed + unsigned'), cg_r3.c:1085 ('IDENTICAL logic so the emitted ... byte-for-byte'), and the u32-truncate movl at cg_r3.iii:2464 (if r3_expr_is_u32(lhs)==1u32 { emit MOVL_EE }).: Apply the SAME emission logic in both sources so they produce byte-identical machine code for every input. The .c version is the frozen seed gcc compiles into iiis-0; the .iii version is what iiis-1/2/3 carry. If they emit even one differing byte on any corpus program, build_iiis1.sh --check-corpus reddens (exit 5). Watch the iiis traps: u32 truncation must match the imul path the reduction replaces (the r3_expr_is_u32->MOVL_EE emit at cg_r3.iii:2464); signedness (mul mod 2^64 identical signed/unsigned, cg_r3.iii:850); the scratch/temp register choice must match byte-for-byte; never gate on registry/accumulated state (determinism). No .def edit unless the change touches iii_compositions (cg_r3.c #includes iii_compositions.h at line 566, regenerated by gen_compositions.sh on every build_iiis0 -- build_iiis0.sh:137-138, confirmed).
- `COMPILER/BOOT/stage1_corpus/NN_<feat>.iii  (NEW -- REQUIRED, not optional)` @ new stage1_corpus program that TRIGGERS the new emit (the dir holds 59 .iii today; VERIFIED count=59): The 59-program stage1_corpus IS the witness set for the C==iii differential. If no corpus program exercises the new codegen path, build_iiis1 --check-corpus (and the iii-vs-iii build_iiis2/3 --check-corpus) never emit the new bytes and the byte-identity gate is VACUOUS for that path -- a cg_r3.c/.iii divergence would be invisible. Add a program whose emission hits the new path. NOTE: because the C==iii gate (build_iiis1 --check-corpus) is run by NO automation (grep-confirmed) -- only by a human in step 4 -- this corpus program is the only durable record that the path was ever differentially covered; name it descriptively.
- `COMPILER/BOOT/iiis-0.mhash` @ reseal target #1: After the chain greens with the NEW consistent hash: cp COMPILED/iiis-0.exe.mhash COMPILER/BOOT/iiis-0.mhash. VERIFIED SAFE: build_iiis0.sh:303 parses the golden with awk '{print $1; exit}', tolerant of the 2-column 'hash  iiis-0.exe' form; and the sidecar is written at build_iiis0.sh:298 BEFORE the golden die at :304-305 (exit 5), so the new hash is recoverable even though build_iiis0 exits 5 on the first post-edit run. verify_step.sh:96-97 also awk-parses both sides, so verify_step's VP-4 reseal (cp at :100) is consistent for iiis-0.
- `COMPILER/BOOT/iiis-1.mhash` @ reseal target #2 -- FORMAT-CRITICAL: MUST be HASH-ONLY: cut -d' ' -f1 COMPILED/iiis-1.exe.mhash > COMPILER/BOOT/iiis-1.mhash. Do NOT cp the 2-column sidecar: build_iiis1.sh:216 parses with `head -n1 | tr -d '[:space:]'` which concatenates the filename to 'hash...iiis-1.exe' and then NEVER matches MHASH -> spurious exit 5. CONFIRMED EMPIRICALLY: the committed HEAD:iiis-1.mhash IS 2-column ('f15d16d1...b7b2  iiis-1.exe'); tr-parse yields the 74-char 'f15d16d1...b7b2iiis-1.exe' != the 64-char digest, so the iiis-1 golden gate is currently LATENTLY RED on any correct build. Hash-only resolves it. Build_iiis1.sh:213's own comment states the golden 'contains just the hex digest (no filename suffix)' -- the committed file violates that contract.
- `COMPILER/BOOT/iiis-2.mhash  +  COMPILER/BOOT/iiis-3.mhash` @ reseal targets #3/#4 -- reference snapshots only: cp COMPILED/iiis-2.exe.mhash COMPILER/BOOT/iiis-2.mhash ; cp COMPILED/iiis-3.exe.mhash COMPILER/BOOT/iiis-3.mhash. VERIFIED: a repo-wide grep for 'iiis-[23].mhash' (excluding *.exe.mhash) finds ZERO consumers -- no build script machine-compares these; they are the human-readable record. The enforced fixpoint is that the two SIDECARS are equal (COMPILED/iiis-2.exe.mhash == COMPILED/iiis-3.exe.mhash), asserted via the spec's step-6 diff and visible in the K4 DONE echo. Optionally re-root the output-preservation baseline: cp COMPILED/iiis-2.exe COMPILED/iiis-2.exe.emitgen-baseline (arg-1 input to emit_gen_diff.sh). IMPORTANT (see risk): for a stdlib-affecting codegen change, re-snapshot ONLY after the stdlib<->compiler convergence loop has settled, else these snapshots (and the git-TRACKED COMPILED/iiis-2.exe) record a non-steady-state build.
- `COMPILER/BOOT/build_iiis1.sh:216  AND  COMPILER/BOOT/build_xii.sh:87  (RECOMMENDED robustness fix, moves nothing)` @ the two iiis-1.mhash whitespace-stripping golden parsers: BOTH places parse iiis-1.mhash with a whitespace-stripping form that mangles the 2-column sidecar: build_iiis1.sh:216 `head -n1 "$GOLDEN_FILE" | tr -d '[:space:]'`, and build_xii.sh:87 `cat "$BOOT/iiis-1.mhash" | tr -d '[:space:]'` (mismatch -> exit 3 at build_xii.sh:91). The spec originally named only build_iiis1.sh:216 -- build_xii.sh:87 is a SECOND consumer with the identical trap. Change BOTH to `awk '{print $1; exit}' "$file"` so iiis-1.mhash is parsed like iiis-0.mhash (tolerant of the 2-column form). Hash-only reseal already makes both pass, but this awk fix removes the recurring foot-gun so a cp-style reseal can't silently red either gate. Optional but eliminates the latent class entirely.
- VERIFY: RESEAL SEQUENCE (run from III root; prerequisite: stdlib built for the iiis-2/3 link). INVARIANT: VERIFY THE DIFFERENTIALS GREEN ON THE NEW BINARIES BEFORE RESEALING -- the reseal only updates the EXPECTED hash, it does not re-check correctness. CRITICAL: the C==iii differential is run by NO automation; step 4 is mandatory and _k4gate (step 9) does NOT substitute for it.\n\n# 0. stdlib must be current (iiis-2/3 link libiii_native.a; build_stdlib uses default-ring/R3 cg_r3). Record its mhash for the convergence check.\nIIIS=\"$PWD/COMPILED/iiis-2.exe\" bash STDLIB/scripts/build_stdlib.sh   # require: FAIL = 0\ncp STDLIB/build/iii/libiii_native.a.mhash /tmp/stdlib_pre.mhash       # baseline (build_stdlib.sh:1478 emits this)\n\n# 1. iiis-0 from cg_r3.c (writes sidecar at :298 then DIES exit 5 on the seal move -- EXPECTED)\nbash COMPILER/BOOT/build_iiis0.sh || true\nbash COMPILER/BOOT/build_iiis0.sh --check-deterministic   # exit 6 => nondeterministic codegen -> STOP, do not reseal\n\n# 2. PROVE C==iii GATE-INDEPENDENTLY *before* any reseal (preferred: fully respects verify-before-reseal).\n#    build_iiis1.sh writes iiis-1.exe at :200 BEFORE it dies at the golden (:215), so the binary exists:\nbash COMPILER/BOOT/build_iiis1.sh || true                 # builds NEW iiis-1.exe, writes sidecar, dies exit5 at golden\nbash COMPILER/BOOT/emit_gen_diff.sh COMPILED/iiis-0.exe /tmp/eg0\nbash COMPILER/BOOT/emit_gen_diff.sh COMPILED/iiis-1.exe /tmp/eg1\ndiff -r /tmp/eg0/r3 /tmp/eg1/r3   # MUST be empty == cg_r3.c emits byte-identically to cg_r3.iii on all 59. If non-empty: revert.\n\n# 3. reseal iiis-0 golden, reconfirm green\ncp COMPILED/iiis-0.exe.mhash COMPILER/BOOT/iiis-0.mhash\nbash COMPILER/BOOT/build_iiis0.sh                          # now exit 0\n\n# 4. iiis-1 golden (HASH-ONLY) + the C-vs-iii corpus differential (the SOLE automated form of this gate).\n#    build_iiis1's golden assertion runs BEFORE its corpus phase, so reseal must precede --check-corpus.\ncut -d' ' -f1 COMPILED/iiis-1.exe.mhash > COMPILER/BOOT/iiis-1.mhash   # HASH-ONLY (NOT cp of the 2-col sidecar)\nbash COMPILER/BOOT/build_iiis1.sh --check-corpus           # REQUIRE exit 0 AND \"corpus equivalence: 59 passed, 0 failed\".\n#   If this FAILS, the step-2 diff should already have caught it; revert cg_r3.{c,iii} + iiis-0/iiis-1 goldens via git.\n\n# 5. iiis-2 fixpoint + Ring-2 + Ring-0 gates (build_iiis2 has NO in-script golden)\nbash COMPILER/BOOT/build_iiis2.sh --check-corpus\n#   REQUIRE: corpus equivalence 59/0 (iiis-1 vs iiis-2) ; check_rm2 do_thing(7)=21 ; [cg_r0-gate] GATE PASS (PASS=N FAIL=0) ; [cg_r0-wgate] GATE PASS\n\n# 6. iiis-3 second fixpoint + the QUINE SEAL assertion (build_iiis3 --check-corpus = iiis-2 vs iiis-3)\nbash COMPILER/BOOT/build_iiis3.sh --check-corpus\ndiff <(cut -d' ' -f1 COMPILED/iiis-2.exe.mhash) <(cut -d' ' -f1 COMPILED/iiis-3.exe.mhash)   # MUST be empty (i2==i3 fixpoint)\n\n# 7. reseal the reference goldens (NO machine consumer -- record only) + emitgen baseline\ncp COMPILED/iiis-2.exe.mhash COMPILER/BOOT/iiis-2.mhash ; cp COMPILED/iiis-3.exe.mhash COMPILER/BOOT/iiis-3.mhash\ncp COMPILED/iiis-2.exe COMPILED/iiis-2.exe.emitgen-baseline\n\n# 8. seam + whole-system regression + STDLIB<->COMPILER CONVERGENCE LOOP (the committed iiis-2.exe is git-tracked)\nIIIS=\"$PWD/COMPILED/iiis-2.exe\" bash COMPILER/BOOT/cg_seam_gate.sh          # REQUIRE [cg-seam] PASS=N FAIL=0\nIIIS=\"$PWD/COMPILED/iiis-2.exe\" bash STDLIB/scripts/build_stdlib.sh         # rebuild stdlib w/ NEW iiis-2; FAIL = 0\nIIIS=\"$PWD/COMPILED/iiis-2.exe\" bash STDLIB/scripts/run_corpus.sh           # PASS unchanged + new 99-oracle KAT=99 (semantic correctness on stdlib)\n#   CONVERGENCE: if STDLIB/build/iii/libiii_native.a.mhash now differs from /tmp/stdlib_pre.mhash, the codegen change\n#   altered stdlib .o -> the iiis-2/3 you just sealed embed the OLD-stdlib link. Re-run steps 5-7 (build_iiis2 --check-corpus,\n#   build_iiis3, re-snapshot) so iiis-2/3 link the NEW stdlib, then rebuild stdlib once more and re-compare its mhash.\n#   Repeat until libiii_native.a.mhash is stable (converges in ONE extra cycle -- stdlib *semantics* are unchanged).\n\n# 9. one-shot end-to-end gate (orchestrator) -- read every rc + the K4 DONE i2==i3 echo. NOTE: _k4gate runs build_iiis1\n#    WITHOUT --check-corpus, so it does NOT prove C==iii; it is a fixpoint+seam smoke test, not a substitute for step 4.\nbash STDLIB/_k4gate.sh\n#   REQUIRE: i0 rc=0, i1 rc=0 (built+golden matched -- NOT a byte-identity proof), i2 rc=0 (corpus equivalence 59/0,\n#            do_thing=21, cg_r0 PASS), i3 rc=0, stdlib FAIL=0, corpus PASS unchanged, seam FAIL=0, and K4 DONE shows i2 == i3.\n\nNOTE on OneDrive: if a relink reports 'ld returned 1' with zero undefined refs, Defender/OneDrive holds the .exe open -- rm the target .exe before relinking (per the relink-lock memory).
- NEW KAT: A NEW stage1_corpus/NN_<feat>.iii is MANDATORY for any codegen emit change -- it is the witness set that makes build_iiis1/2/3 --check-corpus non-vacuous for the new path. (Extra weight here: the C==iii form of that gate runs in NO automation, so this corpus program is the only durable artifact recording that the path was differentially covered.) PLUS a STDLIB/corpus 99-oracle KAT that RUNS the emitted code and FAILS if the emission is wrong: e.g. for pow2-mul strength reduction, compute x*K via the reduced shift/lea sequence and assert == a reference x*K computed a different way (return 99 only on match; a wrong shift count or missing u32 truncate -> !=99). The stage1_corpus program proves cg_r3.c==cg_r3.iii byte-for-byte; the STDLIB 99-KAT (run under step 8's run_corpus, compiled by the NEW iiis-2) proves the bytes are SEMANTICALLY correct, not merely identical to a wrong sibling -- this is also what catches a stdlib-affecting emit error during the convergence loop.
- RISK: (1) RESEAL-BEFORE-VERIFY = blessing a broken seal: reseal only rewrites EXPECTED hashes, no correctness check. Mitigation: gate every differential on the NEW binaries BEFORE any golden cp -- step-2 emit_gen_diff (gate-independent, no reseal), step-4 build_iiis1 --check-corpus (C==iii), step-5 build_iiis2 fixpoint+rm2+cg_r0, step-6 i2==i3, step-8 cg_seam; revert cg_r3.{c,iii}+goldens via git on any red. (2) THE C==iii GATE IS MANUAL-ONLY: grep confirms 'build_iiis1.sh --check-corpus' is in no automation and _k4gate.sh:7 omits --check-corpus -- so _k4gate proves only iii-vs-iii (build_iiis2 --check-corpus) + i2==i3, NEVER cg_r3.c-vs-cg_r3.iii. Step 4 is the sole C==iii gate and is non-optional; treating _k4gate green as sufficient lets a .c/.iii divergence ship. (3) iiis-1.mhash FORMAT TRAP (two consumers): build_iiis1.sh:216 (head|tr, exit 5) AND build_xii.sh:87 (cat|tr, exit 3) whitespace-strip the golden; a 2-column cp yields 'hash...iiis-1.exe' that never matches -> spurious die. The committed HEAD:iiis-1.mhash IS 2-column, so the iiis-1 golden gate is latently RED today; write hash-only (and optionally awk-fix both parsers, edit #6). (4) build_iiis1 dies at its golden BEFORE its corpus phase, forcing reseal-before-corpus for the gated path; mitigated by step-2 emit_gen_diff which verifies C==iii on the pre-reseal iiis-1.exe (written at :200, before the :215 die) with NO reseal. (5) VACUOUS DIFFERENTIAL: without a stage1_corpus program triggering the new emit, the byte-identity gate never witnesses it. (6) UN-CORPUS'D RINGS: the 59-prog diff is R3-only; cg_r0/rm1/rm2 are covered ONLY by cg_seam_gate.sh + check_rm2.sh + emit_gen_diff -- a shared-helper edit can break cg_r0 invisibly to the R3 corpus (the 2026-06-04 u32-width class), so cg_seam (PASS=N FAIL=0) and the cg_r0 crypto/width gates MUST be green. (7) NONDETERMINISM -> build_iiis0.sh --check-deterministic exit 6; run before reseal. (8) STALE libiii_native.a -> iiis-2 links old stdlib -> false green; rebuild stdlib first (step 0). (9) STDLIB<->COMPILER CONVERGENCE (NEW, reproducibility not correctness): build_stdlib compiles stdlib with default-ring cg_r3 (build_stdlib.sh:1358) and iiis-2/3 link libiii_native.a, but iiis-1 is stdlib-FREE (so the C==iii differential is stdlib-independent -- good). In the reseal flow iiis-2/3 link stdlib built by the OLD iiis-2 (step 0); a stdlib-affecting emit change (the pow2-mul example IS one) means a from-scratch rebuild (stdlib compiled by the NEW iiis-2) would yield a different iiis-2 mhash. Not gated (no golden on iiis-2/3; i2==i3 holds within a run) and semantic errors are caught by step-8 run_corpus -- BUT COMPILED/iiis-2.exe is git-TRACKED, so committing without iterating leaves an inconsistent artifact set (iiis-2.exe embeds stdlib_v1 while libiii_native.a is stdlib_v2). Mitigation: the step-8 convergence loop (compare libiii_native.a.mhash; re-run steps 5-7 until stable). (10) verify_step.sh CANNOT reseal iiis-0 on a seal move: its VP-2 (lines 68-72) runs build_iiis0.sh which dies exit 5 and aborts (fail 2) before VP-4's reseal (lines 99-101) is reached -- use the manual cp reseal, not verify_step, for seal-MOVING changes.
- DEPENDS: FOUNDATIONAL -- must PRECEDE every compiler-touching (seal-moving) cash-in: e.g. a pow2/non-pow2 mul strength-reduction in cg_r3 r3_mul_pow2_k (NOTE: r3_mul_pow2_k at cg_r3.iii:851 is specifically the POW2-mul->shift path; a non-pow2 mul cash-in would add a sibling, not reuse it -- the dependsOn label is a forward reference to an external gap-analysis doc's item numbering, not verifiable from this repo), a shift-combine in sov_isa_rules (if it moves the golden), and any kernel-trust change. Per the gap-analysis ordering: run the byte-safe Phase-1 cash-ins (rotl dedup, zkf_mul->mont_cios, cap_revoke) FIRST to exercise this build/reseal rhythm on ZERO-seal-movement changes (their build_iiis1 --check-corpus must show 59/0 and all four mhashes must be UNCHANGED -- a moved mhash on a 'byte-safe' change is itself a finding), THEN batch the seal-moving codegen changes into ONE reseal (so the gate signal isn't confounded by two seal deltas), THEN the kernel-trust changes last.

### cg_r3 non-power-of-two multiply strength reduction (gil_forge kinds 2-5) -- VERIFIED with 3 mandatory corrections folded in (the spec AS SUBMITTED ships a miscompiler and an unattested parse idiom; CORRECTED it holds up).  [feasible=yes-with-care]
The locations, string constants, identities, and reseal mechanics all check out against the real source -- BUT the spec as written has a real soundness bug and an unattested iii idiom, so it does NOT hold up as literally submitted. (1) Both emit blocks store the shift amounts in GLOBALS (g_sr_a/g_sr_b in C, R3_SR_A/R3_SR_B in iii) and read them AFTER the recursive emit_expr(lhs); a nested strength-reducible multiply (e.g. (x*3)*7) re-runs the detector and overwrites the globals, so the outer emit reads the inner's shift amount and emits x*9 instead of x*21. The existing pow2/shift/identity folds are immune precisely because srk/shk/idn are LOCALS holding the return value. Both C and iii are bugged identically, so the byte-identity gate (--check-corpus) still passes while the value is wrong, and 1762-as-written never nests so the bug is dormant. FIX: capture into locals before the recursion; the spec's 'Soundness: NONE added' is false. (2) The iii detector uses double-space same-line statement separators where the whole codebase (incl. the dual r3_mul_pow2_k line 859) uses ';' -- convert to ';'. (3) Add a nested-multiply RED/GREEN case to 1762. With these three folded in the core (kinds 2-5 exact BV64 identities, byte-identical duals, safe rcx) is correct.
- `COMPILER/BOOT/cg_r3.c` @ detector: after arith_identity() (ends 1132), before emit_expr (1134) -- VERIFIED line 1133 blank, 1134 = emit_expr. arith_identity at 1122-1132, mul_pow2_k 1089-1100, shift_const_k 1107-1115 all confirmed present: CORRECT AS-IS: add `static int g_sr_a, g_sr_b;` + mul_strength_form detector (signature `static int mul_strength_form(iii_cg_r3_state_t *cg, const iii_ast_node_t *n)`, reads n->u.binary.op/rhs, rhs->u.int_.value -- same accessors as the proven mul_pow2_k dual). Detector math VERIFIED for k2(2^a-1)/k3(2^a+1)/k4(2^a+2^b)/k5(2^a-2^b): pow2 reject early, ordering k2->k3->k4->k5, a,b<=12 bound, conflicting-representation values (e.g. v=3 is both 2^2-1 and 2^1+1) resolve to the SAME kind in C and iii by identical check order. III_BIN_MUL==3 confirmed.
- `COMPILER/BOOT/cg_r3.c` @ emit_expr BINARY case, after the idn (arith_identity) block -- VERIFIED: insert after line 1412 (`}` ending idn block), before 1413 (general `if (emit_expr(cg, n->u.binary.lhs)...`): CORRECTED (soundness fix REQUIRED): capture the globals into locals BEFORE the recursion. `int srf = mul_strength_form(cg, n); if (srf != 0) { int sa = g_sr_a, sb = g_sr_b; if (emit_expr(cg, n->u.binary.lhs) != 0) return -1; stack_pop_reg(cg, "rax"); emit_line(cg, "    movq %%rax, %%rcx"); emit_line(cg, "    shlq $%d, %%rax", sa); if (srf==2) emit_line(cg,"    subq %%rcx, %%rax"); else if (srf==3) emit_line(cg,"    addq %%rcx, %%rax"); else { emit_line(cg,"    shlq $%d, %%rcx", sb); if (srf==4) emit_line(cg,"    addq %%rcx, %%rax"); else emit_line(cg,"    subq %%rcx, %%rax"); } if (expr_is_u32(cg, n->u.binary.lhs)) emit_line(cg,"    movl %%eax, %%eax"); stack_push_reg(cg,"rax"); return 0; }`  The spec's version reads g_sr_a/g_sr_b AFTER emit_expr(lhs) -> clobber bug. emit_line VERIFIED to append exactly one '\n' (cg_r3.c:243-254), so the 6 emit strings are byte-identical to the iii arrays. rcx-clobber is SAFE (rhs is a constant literal, never emitted; rcx free after lhs materialized). Stack depth balanced (+1, same as pow2 fold).
- `COMPILER/BOOT/cg_r3.iii` @ module-state vars, after R3_G_LAST_ERROR -- VERIFIED line 257 = R3_G_LAST_ERROR; surrounding are `var R3_G_*: u32`. R3_SR_ prefix is novel (no collision).: CORRECT AS-IS: add `var R3_SR_A: u32 = 0u32` and `var R3_SR_B: u32 = 0u32`. (Module globals are the iii idiom for a multi-value return; the clobber risk is neutralized by capturing into locals at the emit site -- see the emit edit below.)
- `COMPILER/BOOT/cg_r3.iii` @ detector: after r3_arith_identity() (ends 888), before r3_emit_load_slot (889) -- VERIFIED both lines. r3_mul_pow2_k 851-861, r3_shift_const_k 867-873, r3_arith_identity 881-888 all present; signature (op:u32,rhs:u32) matches.: CORRECTED (iii idiom REQUIRED): add r3_mul_strength_form(op:u32,rhs:u32)->u32 with IDENTICAL decomposition to the C dual, BUT replace every double-space same-line statement separator with ';' (the attested idiom -- r3_mul_pow2_k line 859 uses `t = t >> 1u64; k = k + 1u32`; line 2456 uses ';' for multi-let). E.g. `let mut a: u32 = 0u32; let mut t: u64 = w2`, `while t > 1u64 { t = t >> 1u64; a = a + 1u32 }`, `if a >= 2u32 { if a <= 12u32 { R3_SR_A = a; R3_SR_B = 0u32; return 2u32 } }`. Uses iii_ast_node_kind/R3_K_EXPR_INT/iii_ast_expr_int_u64 (same as r3_mul_pow2_k -- proven byte-identical to C). The block loop (parse.iii 2612-2628) needs no mandatory separator so double-space MIGHT parse for these non-ambiguous cases, but it is unattested and fragile; ';' guarantees it and matches the dual.
- `COMPILER/BOOT/cg_r3.iii` @ r3_emit_expr BINARY case, after the idn block -- VERIFIED: insert after line 2491 (`}` ending idn), before 2492 (`let signed`). op/rhs locals in scope from line 2456.: CORRECTED (soundness fix REQUIRED): `let srf: u32 = r3_mul_strength_form(op, rhs); if srf != 0u32 { let sa: u32 = R3_SR_A; let sb: u32 = R3_SR_B; r3_emit_expr(lhs); r3_pop_rax(); r3_emit_arr(&R3_STR_MOVQ_RAX_RCX as u64, R3_STR_MOVQ_RAX_RCX_LEN); r3_emit_arr(&R3_STR_SHLQ_DOLLAR as u64, R3_STR_SHLQ_DOLLAR_LEN); r3_emit_dec(sa as u64); r3_emit_arr(&R3_STR_CM_RAX_NL as u64, R3_STR_CM_RAX_NL_LEN); if srf == 2u32 { r3_emit_arr(&R3_STR_SUBQ as u64, R3_STR_SUBQ_LEN) } if srf == 3u32 { r3_emit_arr(&R3_STR_ADDQ as u64, R3_STR_ADDQ_LEN) } if srf == 4u32 { r3_emit_arr(&R3_STR_SHLQ_DOLLAR as u64, R3_STR_SHLQ_DOLLAR_LEN); r3_emit_dec(sb as u64); r3_emit_arr(&R3_STR_COMMA_RCX_NL as u64, R3_STR_COMMA_RCX_NL_LEN); r3_emit_arr(&R3_STR_ADDQ as u64, R3_STR_ADDQ_LEN) } if srf == 5u32 { r3_emit_arr(&R3_STR_SHLQ_DOLLAR as u64, R3_STR_SHLQ_DOLLAR_LEN); r3_emit_dec(sb as u64); r3_emit_arr(&R3_STR_COMMA_RCX_NL as u64, R3_STR_COMMA_RCX_NL_LEN); r3_emit_arr(&R3_STR_SUBQ as u64, R3_STR_SUBQ_LEN) } if r3_expr_is_u32(lhs) == 1u32 { r3_emit_arr(&R3_STR_MOVL_EE as u64, R3_STR_MOVL_EE_LEN) } r3_push_rax(); return R3_OK }`  Spec reads R3_SR_A/R3_SR_B AFTER r3_emit_expr(lhs) -> SAME clobber bug. String bytes VERIFIED: R3_STR_MOVQ_RAX_RCX[20] line 613='    movq %rax, %rcx\n'; R3_STR_SHLQ_DOLLAR[10] 429='    shlq $'; R3_STR_CM_RAX_NL[7] 434=', %rax\n'; R3_STR_COMMA_RCX_NL[7] 343=', %rcx\n'; R3_STR_ADDQ[20] 2109='    addq %rcx, %rax\n'; R3_STR_SUBQ[20] 2111='    subq %rcx, %rax\n'; R3_STR_MOVL_EE[20] 2145='    movl %eax, %eax\n' -- all byte-match the C emit_line output.
- `COMPILER/BOOT/stage1_corpus/60_mul_strength.iii` @ NEW FILE -- VERIFIED no existing stage1 file contains a multiply (all `*` hits are pointer types/derefs), so this genuinely gives --check-corpus teeth for the new path.: CREATE AS-IS (params+mul supported by iiis-0, cf 02_arithmetic.iii/05_param.iii). CAVEAT: --check-corpus is --compile-only + sha256 (build_iiis2.sh:240-252) -- it does NOT RUN the program, so the inner `if f2(10)!=70` asserts are NOT executed; this file proves only C/iii BYTE-IDENTITY, not result-correctness. It also will NOT expose the clobber bug (byte-identical-wrong passes). Result-correctness teeth live only in the run-oracle 1762.
- `STDLIB/corpus/1762_r3_mul_strength.iii` @ NEW FILE (modeled on 1215_r3_identity_fold.iii -- VERIFIED present; 1207_shift_fold_certified.iii present): CREATE, but ADD a NESTED case so the no-clobber property is actually proven (RED on the global/unfixed build, GREEN after local-capture): e.g. `if (x * 3u64) * 7u64 != x * 21u64 { return 11u64 }` with nonzero x. Reference 21 is non-reducible (popcount 3, odd -> imul), a clean differential. The spec's body tests only `x*K` vs shift-forms (no nesting) -> dormant to the bug. CAVEAT to confirm before relying on it: ensure no upstream sema/XII pass reassociates (x*3)*7 -> x*21 before cg_r3, or the nested case loses its teeth.
- VERIFY: 1) Apply all 5 edits with the THREE corrections (local-capture in both emit blocks; ';' separators in the iii detector; nested case in 1762). 2) RED/GREEN proof of the soundness fix (this project's gate idiom): build the corpus with the GLOBAL (unfixed) emit and confirm 1762's nested case returns non-99 (RED); then with the LOCAL-capture fix confirm 99 (GREEN). Without this RED step there is NO proof the nested path is exercised. 3) Byte-identity gate: cd COMPILER/BOOT && bash build_iiis0.sh && bash build_iiis1.sh && bash build_iiis2.sh --check-corpus && bash build_iiis3.sh. --check-corpus (VERIFIED build_iiis2.sh:230-258) compiles every stage1_corpus/*.iii (incl. 60_mul_strength) with iiis-0 AND iiis-2 and asserts sha256-identical .o ('corpus equivalence: N passed, 0 failed'); a C/iii decomposition or string mismatch fails it. NOTE this gate proves C==iii, NOT nested-correctness (both bugged identically would still pass) -- that is why step 2 is mandatory and separate. 4) Reseal: codegen legitimately changes -> build_iiis1.sh dies on mhash drift; after gate-green re-seal COMPILER/BOOT/iiis-1.mhash, iiis-2.mhash, iiis-3.mhash (all three VERIFIED present); re-run build_stdlib if it pins a compiler mhash. 5) Result-correctness: bash STDLIB/scripts/build_stdlib.sh (grep 'FAIL = 0') + run corpus; 1762 (incl. the new nested case), 1215, 1207 must return 99. 6) Spot-check emit: iiis-2 --emit-asm on `fn f(x:u64)->u64{return x*7u64}` -> `movq %rax, %rcx ; shlq $3, %rax ; subq %rcx, %rax`.
- NEW KAT: STDLIB/corpus/1762_r3_mul_strength.iii (99-oracle), the spec's body PLUS a nested-multiply clause that turns the dormant clobber bug into a hard RED/GREEN discriminator. Add to mul_u64 (nonzero x reaches it via the main() probes): `if (x * 3u64) * 7u64 != x * 21u64 { return 11u64 }` and `if (x * 5u64) * 9u64 != x * 45u64 { return 12u64 }` (45 = popcount 4 -> imul reference). These RED on the global-emit (unfixed) build -- (x*3)*7 emits x*9, (x*5)*9 emits x*25 -- and GREEN once g_sr_a/g_sr_b (R3_SR_A/R3_SR_B) are captured into locals. Keep the spec's existing non-nested clauses (k2..k5 + u32 boundary 0xFFFFFFFF + u64 overflow-wrap 0xFFFFFFFFFFFFFFFF) -- those are sound and prove the exact-product property per kind. Confirm sema/XII does not pre-reassociate (x*3)*7 -> x*21; if it does, replace the LHS with an opaque-via-param form so codegen still sees two literal multiplies.
- RISK: PRIMARY (spec's 'Soundness: NONE added' is FALSE as written): the detector writes GLOBALS (g_sr_a/g_sr_b, R3_SR_A/R3_SR_B) and the emit reads them AFTER the recursive emit_expr(lhs); any nested strength-reducible multiply re-runs the detector and clobbers them, so the outer multiply emits the inner's shift amounts -> wrong product (e.g. (x*3)*7 -> x*9). Both C and iii clobber identically, so the byte-identity gate PASSES while miscompiling -- a latent landmine that could corrupt the bootstrap if such a pattern exists in cg_r3.iii/stdlib sources. MITIGATION (mandatory, not optional): capture into locals (sa/sb) immediately after the detector call, before the recursion -- this mirrors the proven pow2/shift/identity folds (srk/shk/idn locals) and preserves byte-identity (both sides capture-then-emit the same bytes). With the fix, soundness IS clean: each kind is an exact 2's-complement identity mul(x,K)==(x<<a)+/-(x<<b) mod 2^64 (signed i32/i64 sign-extend, unsigned u32/u64 zero-extend; u32 uses the same movl %eax,%eax truncation as the imul path, keyed on expr_is_u32(lhs)). SECONDARY: iii detector's double-space same-line separators are unattested (whole tree, incl. its own dual r3_mul_pow2_k:859, uses ';') -> convert to ';'. rcx-clobber: SAFE (rhs is a constant literal, never emitted; movq %rax,%rcx is after lhs is materialized and no further sub-expr runs). Determinism/byte-identity: detector logic, ordering (pow2-in-handler then k2->k3->k4->k5), and a,b<=12 bound are identical in C and iii. SEAL: real codegen change for small-constant multiplies across stdlib+compiler -> iiis-1/2/3.mhash MUST be re-sealed; caught by --check-corpus (byte) + the stdlib corpus (result, now incl. the nested clause).
- DEPENDS: Independent/self-contained. Per DOCS/III-GAP-ANALYSIS-2026-06-19.md (cited by the spec) BATCH the reseal with any concurrent cg_r3/codegen-touching cash-in (esp. the shift-combine '(a,b)->sov_isa_rules' item) so the iiis-1/2/3.mhash golden moves once. No code dependency; if landed alone it is a single standalone reseal. Caveat: confirm no concurrent writer is mid-edit on cg_r3.c/cg_r3.iii (spine files) before applying -- the dual edit must land in lockstep or the --check-corpus gate reddens.

### sov_isa shift-combine rule generalization (gap-analysis §2 #3)  [feasible=yes]
Generalize Rule H (the lone hand-wired (1,1) shift-combine) by appending, inside sov_isa_rules() right after Rule H, a SOURCE-COMMITTED grid of (a,b) in 1..SI_SHC_MAX with a+b<64 that registers shl(shl(?0,cnum_a),cnum_b) -> shl(?0,cnum_{a+b}) in the parametric SI_CNUM vocabulary. Each (a,b) is re-earned by cga_shift_certifies(2,a,b) — the NON-mutating count-mask UNSAT re-proof (it save/restores CGA_SH_N), so the rule set is a compile-time fixed list with ZERO dependence on the accumulated cga registry: golden determinism preserved. It is correct because (1) the gate is the same soundness basis as Rule H (the shift-combine law has no CIC iota; its proof IS the lattice UNSAT — confirmed by the source comment at lines 653-656), and (2) the new rules live in the SI_CNUM vocabulary, DISJOINT from the c0/c1/c2 tags every existing sov_isa KAT term is built from, so no existing extraction changes. This does NOT move the codegen golden: cg_r3 cites sov_isa only in comments (the live fold is the independent r3_mul_pow2_k) and the compiler bootstrap never compiles STDLIB/sov_isa.iii.
- `STDLIB/iii/numera/sov_isa.iii` @ module consts block, insert near line 135 (beside SI_SENT / CL_BALANCED): Add: `const SI_SHC_MAX : u32 = 8u32`  -- source-committed shift-combine grid bound. With MAX=8 every pair has a+b<=16, so a+b<64 (count-mask wall) AND a+b fits the cnum byte (<=255); both hold.
- `STDLIB/iii/numera/sov_isa.iii` @ fn sov_isa_rules() (:350), insert AFTER Rule H's closing brace (line 459) and BEFORE Rule I (line 461): Insert the fixed-grid loop (reuses in-scope `s_shl` from line 352 and `rulep` from line 389; extern cga_shift_certifies already declared at line 44; si_slot_cnum already at :199):

    let mut shc_a : u32 = 1u32
    while shc_a <= SI_SHC_MAX {
        let mut shc_b : u32 = 1u32
        while shc_b <= SI_SHC_MAX {
            if (shc_a + shc_b) < 64u32 {
                if cga_shift_certifies(2u32, shc_a, shc_b) == 1u8 {
                    let s_ca  : u32 = si_slot_cnum(shc_a as u8)
                    let s_cb  : u32 = si_slot_cnum(shc_b as u8)
                    let s_cab : u32 = si_slot_cnum((shc_a + shc_b) as u8)
                    if s_ca != SI_SENT {
                        if s_cb != SI_SENT {
                            if s_cab != SI_SENT {
                                SI_RULE[0u32] = 5u32
                                SI_RULE[1u32] = 3u32
                                SI_RULE[2u32] = si_enc_sym(s_shl, 2u32)
                                SI_RULE[3u32] = si_enc_sym(s_shl, 2u32)
                                SI_RULE[4u32] = si_enc_var(0u32)
                                SI_RULE[5u32] = si_enc_sym(s_ca, 0u32)
                                SI_RULE[6u32] = si_enc_sym(s_cb, 0u32)
                                SI_RULE[7u32] = si_enc_sym(s_shl, 2u32)
                                SI_RULE[8u32] = si_enc_var(0u32)
                                SI_RULE[9u32] = si_enc_sym(s_cab, 0u32)
                                if eg_register_rule(rulep) == SI_SENT { return 12u32 }
                            }
                        }
                    }
                }
            }
            shc_b = shc_b + 1u32
        }
        shc_a = shc_a + 1u32
    }

Note: cga_shift_certifies (not cga_dispose_shift) is chosen deliberately over the literal Rule-H call so the new loop NEVER grows CGA_SH_N — this is what makes the rule set independent of registry state (the cash-in's core requirement). It still satisfies 'gated by cga_dispose_shift' since cga_shift_certifies wraps cga_dispose_shift then restores the count. Return code 12u32 (next free after Rule I's 11) fails LOUD on table overflow; a lattice refusal SKIPS the pair (sound-but-weaker), exactly Rule H's contract. rules() still returns 0 in practice (capacity is ample; cga always certifies a+b<64 shape-2).
- `STDLIB/iii/numera/sov_isa.iii` @ new @export fn, insert after sov_isa_shiftcombine_kat() (ends line 840): Add the generalization KAT:

fn sov_isa_shiftcombine_list_kat() -> u64 @export {
    /* (a) a NON-(1,1) LISTED pair combines in cnum vocab: shl(shl(x,2),3) -> shl(x,5) */
    eg_init() ; cl_init()
    let cx : u32 = si_leaf(SI_X)
    let ca2 : u32 = si_leaf_cnum(2u8)
    let cb3 : u32 = si_leaf_cnum(3u8)
    let inner : u32 = si_node2(SI_SHL, cx, ca2)
    let root  : u32 = si_node2(SI_SHL, inner, cb3)
    if root == SI_SENT { return 1u64 }
    let cab5 : u32 = si_leaf_cnum(5u8)
    let cseed : u32 = si_node2(SI_SHL, cx, cab5)        /* seed shl(x,cnum5) RHS slot+cost */
    if cseed == SI_SENT { return 2u64 }
    if sov_isa_rules() != 0u32 { return 3u64 }
    let otp : *u32 = (&SI_OUT) as u64 as *u32
    let onp : *u32 = (&SI_OUTN) as u64 as *u32
    if sov_isa_descend(root, otp, onp) != 0i32 { return 4u64 }
    let s_shl : u32 = si_slot(SI_SHL)
    let s_c5  : u32 = si_slot_cnum(5u8)
    if SI_OUTN[0u32] != 3u32 { return 5u64 }            /* combined: shl(x,cnum5) = 3 entries */
    if SI_OUT[0u32] != s_shl { return 6u64 }
    if SI_OUT[2u32] != s_c5 { return 7u64 }             /* count cnum5 = 2+3: genuinely combined */
    /* (b) BOUND falsifier: (9,9) is lattice-CERTIFIED (a+b=18<64) yet NOT source-listed (MAX=8),
     *     so it does NOT combine -- proving the SOURCE LIST governs, not the accumulated registry. */
    if cga_shift_certifies(2u32, 9u32, 9u32) != 1u8 { return 8u64 }   /* lattice does certify it */
    eg_init() ; cl_init()
    let dx : u32 = si_leaf(SI_X)
    let d9a : u32 = si_leaf_cnum(9u8)
    let d9b : u32 = si_leaf_cnum(9u8)
    let din : u32 = si_node2(SI_SHL, dx, d9a)
    let dro : u32 = si_node2(SI_SHL, din, d9b)
    let dse : u32 = si_node2(SI_SHL, dx, si_leaf_cnum(18u8))
    if dse == SI_SENT { return 9u64 }
    if sov_isa_rules() != 0u32 { return 10u64 }
    if sov_isa_descend(dro, otp, onp) != 0i32 { return 11u64 }
    if SI_OUTN[0u32] != 5u32 { return 12u64 }           /* NOT combined: nested 5-entry form stays */
    /* (c) faithfulness: with NO rules even the listed (2,3) does not combine */
    eg_init() ; cl_init()
    let fx : u32 = si_leaf(SI_X)
    let f2 : u32 = si_leaf_cnum(2u8)
    let f3 : u32 = si_leaf_cnum(3u8)
    let fin : u32 = si_node2(SI_SHL, fx, f2)
    let fro : u32 = si_node2(SI_SHL, fin, f3)
    if fro == SI_SENT { return 13u64 }
    if sov_isa_descend(fro, otp, onp) != 0i32 { return 14u64 }
    if SI_OUTN[0u32] != 5u32 { return 15u64 }           /* no rules => stays nested */
    return 99u64
}

Note: si_leaf_cnum/si_slot_cnum already @export at :191/:199. The cnum_9 LHS never matches any grid rule (constants only go to cnum_8), and SI_C1/C2-tagged terms used by 1362 never match the cnum rules -- both non-regression facts the KAT pins.
- `STDLIB/corpus/1765_shiftcombine_list.iii` @ new corpus file (next free id after 1764): Create:

/* 1765_shiftcombine_list.iii -- THE GENERALIZED LATTICE-CITED SHIFT-COMBINE (cash-in §2#3).
 * sov_isa_rules() now registers a SOURCE-COMMITTED (a,b) grid (1..8, a+b<64) of shift-combine
 * rules shl(shl(?0,cnum_a),cnum_b) -> shl(?0,cnum_{a+b}), each re-earned by the non-mutating
 * count-mask UNSAT gate (cga_shift_certifies) -- generalizing the single Rule H WITHOUT any
 * dependence on the accumulated cga registry (golden determinism preserved). 99 = never hard-coded. */
module corpus_1765

extern @abi(c-msvc-x64) fn sov_isa_shiftcombine_list_kat() -> u64 from "sov_isa.iii"

fn main() -> u64 {
    let r : u64 = sov_isa_shiftcombine_list_kat()
    if r != 99u64 { return r }
    return 99u64
}
- `STDLIB/scripts/run_corpus.sh` @ the `declare -A EXPECTED=( ... )` map, beside the other 17xx entries (e.g. after [1737_shift_dream_adoption]): Add map entry: `[1765_shiftcombine_list]=99`  -- WITHOUT it, run_corpus.sh line 1528-1530 FATALs ('no EXPECTED entry for conformance test'). Value 99 = the KAT's pass code.
- VERIFY: 1) Primary gate (STDLIB corpus regression): `bash STDLIB/scripts/build_stdlib.sh` then confirm 'FAIL = 0' / 'FAIL=0' and PASS count rose by exactly 1; specifically every sov_isa KAT stays green (corpus 874,876,963,1207,1362,1364,1365,1737) and the new 1765 returns 99. The disjoint-vocabulary argument predicts ALL pre-existing sov_isa results are byte-unchanged. 2) Single-test focus (optional faster loop): build+run via `IIIS=COMPILED/iiis-2.exe bash STDLIB/scripts/run_corpus.sh` and check 1765_shiftcombine_list exit==99 and 1362 still 99. 3) Codegen-golden CONFIRM (expected NO-OP, not a reseal): run the seal-gated bootstrap `bash COMPILER/BOOT/build_iiis2.sh` (and/or build_iiis3.sh) -- the iiis-0==iiis-2 byte-identity gate must stay GREEN because sov_isa.iii is compiler-unreferenced. If it reddens, STOP: that would mean an unexpected coupling, do NOT reseal -- investigate. No reseal step is required for this cash-in.
- NEW KAT: NEW KAT REQUIRED: STDLIB/corpus/1765_shiftcombine_list.iii + sov_isa_shiftcombine_list_kat() (99-oracle). Existing 1362 only proves the single (1,1) Rule H instance and cannot prove (a) a non-(1,1) listed pair combines, nor (b) the decisive determinism property: a lattice-certified-but-unlisted pair (9,9) does NOT combine -- i.e. the SOURCE LIST governs the live rule set, not the accumulated registry. Arm (b) is the teeth that pin the cash-in's whole point. The EXPECTED entry in run_corpus.sh is mandatory (a missing one is FATAL).
- RISK: LOW. (1) Soundness: each rule re-earned by cga_shift_certifies (count-mask UNSAT) -- identical basis to Rule H; the shift-combine law has NO CIC iota (source comment :653-656), so adding a tc_conv would be impossible AND unnecessary -- the cash-in's '+kernel re-proof' wording does not apply to this family (honest correction; we follow Rule H's gate exactly). No new trusted base. (2) Non-regression: new rules use SI_CNUM constants, DISJOINT from the c0/c1/c2 tags all existing sov_isa terms build, so they never match those terms; symbol IDs are append-only/idempotent and interned AFTER each term, so prior extractions are byte-stable (the descend invariant at :516-519). (3) Determinism: cga_shift_certifies is non-mutating (save/restore CGA_SH_N) and the grid is a compile-time fixed bound -> rule set identical every boot regardless of any discovery run -- exactly 'NO accumulated-registry dependence.' (4) Capacity: ~8 core + 64 grid = 72 rules << EGRAPH_MAX_RULES 256; ~576 arena words << EGRAPH_RULE_AREA 65536; ~25 syms << EGRAPH_MAX_SYMS 4096; eg_init resets the table each call; SI_RULE[64] holds the 10-entry rule; SI_REQ[256] indexed by slot+2 stays in-bounds. Mitigation = the full corpus run plus the seal-gated bootstrap CONFIRM (step 3) proving the codegen golden is unmoved. Traps avoided: no function-local runtime-indexed array (the grid computes a,b directly -> dodges the local-array segfault trap); a,b<=8 keep (a+b) within u8 for the `as u8` casts.
- DEPENDS: - (independent; NOT seal-moving). sov_isa is not in the live emit path, so this does not move the codegen golden and needs no reseal and no batching. NOTE the gap-analysis Phase-2 advice to 'batch §2#3 with §2#1 in one reseal' applies to §2#1 (non-pow2 mul -> cg_r3 r3_mul_pow2_k), which DOES touch the live emit path and IS seal-moving; this cash-in (§2#3) is gated purely by the STDLIB corpus and can land alone before, after, or independent of §2#1. Only hard prerequisite: the in-tree COMPILED/iiis-2.exe must be the pinned compiler for the corpus run (run_corpus.sh already pins it).

### bb_equal SAT miter as tc_conv kernel fallback (trusted-base tradeoff)  [feasible=yes-with-care]
The mechanism is sound and almost every concrete claim checks out against source: the 12 bb_* externs exist with the exact signatures stated, bb_reset clamps width into 1..64 (so bb_reset(64u64) is the kernel's Z/2^64), bb_equal is genuinely tri-state (1=UNSAT, 0=SAT, 0xFF=poison) and any solver code != SAT_UNSAT(2)/SAT_SAT(1) leaves verdict at 0xFF, SAT_E_TOO_BIG=-2 already exists, sat_solve (652-686) truly has no conflict budget, sat.iii is a leaf so typecheck->bv_bits->sat adds no cycle, bv_bits is built (build_stdlib.sh:790), and tc_conv is at 408-411 with 1214 arm B at 203-206 as stated. The translator/whitelist is fail-closed and monotone (only flips 0->1 on bb_equal==1), width-64 is correct, and the soundness argument (bb universal-over-2^64 equality => model equality on the kernel's BV fragment) holds modulo solver correctness. HOWEVER the spec as literally written FAILS its own verification: (1) the KAT id 1765 is already taken (1765_weave_oneeval + 1766_weave_invent exist; true max is 1766 -> use 1767); (2) the risk-5 audit is too narrow -- besides 1214 arm B, 1355_bv_selflaws_lshr.iii:59 DETERMINISTICALLY reds (it pins tc_conv((x+1)-(1+x), 0)==0, an extensional equality the fallback flips to 1), so an additional companion edit to 1355 is required in the same commit. With the 1767 id, the 1355 edit, and the full-corpus + timing checks below, the change holds; absent them it breaks the build.
- `STDLIB/iii/numera/typecheck.iii` @ extern block, after the ccl externs (the ccl block runs lines 54-101, so insert ~line 101, NOT line 55): CORRECT as proposed. Add 12 externs from "bv_bits.iii": bb_reset(w:u64)->i32, bb_var(v:u32)->u32, bb_const(c:u64)->u32, bb_add/bb_sub/bb_mul/bb_and/bb_or/bb_xor(a:u32,b:u32)->u32, bb_shl(a:u32,k:u64)->u32, bb_shr(a:u32,k:u64)->u32, bb_equal(a:u32,b:u32)->u8. All 12 verified to exist with these exact signatures + @export in bv_bits.iii. Dep typecheck->bv_bits->sat introduces no cycle (sat.iii is a leaf with zero externs; bv_bits imports only sat at lines 33-35). Only correction: the spec says 'near line 55' but the ccl externs occupy 54-101, so place these after ~101.
- `STDLIB/iii/numera/typecheck.iii` @ new fn tc_bv_word_head(t:u32)->u8, just above tc_conv (line 408): CORRECT. Return 1u8 iff TC_TAG[t] in {TC_VAR(1), TC_BVLIT(38), TC_BVADD(39), TC_BVSUB(40), TC_BVMUL(41), TC_BVAND(42), TC_BVOR(43), TC_BVXOR(44), TC_BVSHL(45), TC_BVLSHR(48)}, else 0u8. Note: TC_VAR is intentionally included, so this also fires on var-vs-var conversions of ANY type; that is sound (distinct free vars are bb-SAT, never UNSAT, so no false accept) but means non-BV variable comparisons also pay a trivial bb round-trip -- minor perf, not a soundness issue.
- `STDLIB/iii/numera/typecheck.iii` @ new fn tc_to_bb(t:u32)->u32, just above tc_conv (line 408): CORRECT. Recursive whitelist translator, refuse-sentinel 0xFFFFFFFFu32. TC_VAR k(=TC_A[t]): if k>=8u32 refuse else bb_var(k) (BB_MAX_INPUTS=8 verified line 41; pre-check avoids poisoning BB_ERR). TC_BVLIT: bb_const(((TC_B[t] as u64)<<32)|(TC_A[t] as u64)) -- matches A=lo32,B=hi32 (line 146). TC_BVADD/SUB/MUL/AND/OR/XOR: recurse both children, propagate refuse, else bb_add/sub/mul/and/or/xor. TC_BVSHL/TC_BVLSHR: require TC_TAG[TC_B[t]]==TC_BVLIT else refuse; k=((hi<<32)|lo)&63u64 (kernel masks &63, corroborated by 1214 arm A passing); bb_shl/bb_shr(a,k). All other tags (BVUDIV(49) -- bb has no div; BVADDOVF(46)/BVMULOVF(47) -- Bool-typed; everything else) refuse. The refuse-sentinel 0xFFFFFFFF intentionally coincides with bv_bits' BB_SENT, so a bb error (node overflow >128) also propagates as refuse -> fail-closed. Whitelist tag set is consistent with tc_bv_word_head.
- `STDLIB/iii/numera/typecheck.iii` @ tc_conv, lines 408-411 (replace body, keep signature/@export): CORRECT. ccl_reset(); let r:u8 = ccl_conv(tc_to_ccl(x), tc_to_ccl(y)); if r==1u8 return 1u8; if tc_bv_word_head(x)==0u8 return r; if tc_bv_word_head(y)==0u8 return r; bb_reset(64u64); let bx:u32=tc_to_bb(x); if bx==0xFFFFFFFFu32 return r; let by:u32=tc_to_bb(y); if by==0xFFFFFFFFu32 return r; if bb_equal(bx,by)==1u8 return 1u8; return r. Verified: width MUST be 64 (kernel BV = Z/2^64; narrower would over-accept = false TRUE). Monotone (only 0->1, only on bb_equal==1); SAT(0) and poison(0xFF) both fall through to r. ccl_conv(tc_to_ccl(x),tc_to_ccl(y)) is byte-identical to the current body, so all non-BV and all ==1 outcomes are PRESERVED exactly (tc_to_ccl is read-only on the TC arena, so x/y node structure survives for tc_to_bb).
- `STDLIB/corpus/1214_bv_kernel_differential.iii` @ bv_kernel_differential(), arm B, lines 203-206: DEFENSIVE companion (downgrade 'REQUIRED' -> 'recommended, verify empirically'). The premise (kernel-eq subset poly-eq) IS invalidated by the fallback (now decides extensional Z/2^64 = a superset of polynomial), so the bare kc==1=>bc==1 is no longer logically valid. BUT for the FIXED seed, two INDEPENDENT random arithmetic trees being extensionally-equal-yet-poly-unequal is astronomically unlikely, and hard multiplier miters poison out (0xFF) under the budget -> kc stays ccl=0 -> the kc==1 block is skipped; so arm B most likely stays green even unedited. Apply the proposed probe hardening anyway (snapshot sv=RNG[0]; on kc==1 && bc!=1, probe ~64 random (px,py) via tc_eval(a_tc,px,py)/tc_eval(b_tc,px,py) and red only on a real separation er1==0&&er2==0&&v1!=v2; restore RNG[0]=sv). The restore IS necessary: arm_c() reads RNG[0] as its initial seed_save, so an unrestored probe would perturb arm C. Verdict: sound and correct, but its necessity for THIS seed should be confirmed by actually running 1214, not assumed.
- `STDLIB/corpus/1355_bv_selflaws_lshr.iii` @ line 58-59 (the 'commuted-twin refusal'): onepx=tc_bvadd(one,x); if tc_conv(tc_bvsub(xp1a,onepx), z) != 0u8 { return 8u64 }: NEW, REQUIRED -- SPEC MISSED THIS, IT IS A DETERMINISTIC BUILD BREAK. xp1a=(x+1), onepx=(1+x), z=bvlit(0). (x+1)-(1+x)==0 for all x in Z/2^64. Root is TC_BVSUB (whitelisted); both operands translate cleanly; the ripple-sub-vs-const-0 miter is a small easy UNSAT (~7 bb nodes, not poisoned) -> bb_equal=1 -> tc_conv=1 -> the `!= 0u8` assertion fires -> 1355 returns 8 (not 99). The current kernel returns 0 here (ccl refuses commutativity-derived equality), which is why the test passes today. FIX (different in kind from 1214's probe): the kernel now SOUNDLY accepts this extensional equality, so flip the assertion `if tc_conv(tc_bvsub(xp1a,onepx), z) != 1u8 { return 8u64 }` and update the comment to note the SAT fallback consciously subsumes the commuted-twin case (retiring that incompleteness pin). Do NOT use the separation-probe pattern here -- the terms are genuinely ext-equal, so a probe would correctly never separate and the test must record acceptance. Must land in the SAME commit as the tc_conv edit.
- `STDLIB/iii/numera/sat.iii` @ sat_solve(), while going==1u8 loop at 652-686 (RECOMMENDED): CORRECT and well-targeted. Verified there is no conflict bound (going never set to 0; loop exits only on SAT_SAT/SAT_UNSAT or SAT_ALLOC_FAIL->SAT_E_TOO_BIG). Add a max-conflicts budget; on exhaustion return a non-UNSAT code (SAT_E_TOO_BIG=-2 already exists, line 48). Verified bb_equal leaves verdict=0xFF for any sv not in {1,2} (lines 429,454-455) -> budget-exhaust => poison => tc_conv falls through to r => sound + fail-closed. This is the only thing standing between the kernel and a hang on a hard variable*variable multiplier miter, which arm B of 1214 can now generate ~480 times per run.
- VERIFY: CORRECTED. 1) Build: bash STDLIB/scripts/build_stdlib.sh ; confirm 'FAIL = 0' and PASS unchanged (bv_bits/sat already in archive). 2) Corpus: bash STDLIB/scripts/run_corpus.sh ; ALL KATs exit 99 -- NOTE the spec's claim 'every KAT exits 99 including new 1765 and modified 1214' is FALSE as written: the new KAT is 1767 (1765/1766 are taken by weave tests), and 1355 reds (returns 8) UNLESS its line-59 companion edit lands. Must-stay-99 set includes the new 1767, modified 1214, modified 1355, AND the transitive internal-tc_conv-caller KATs: 1213, 1363, 1444 (bvd_admit Tier-2 tc_conv on BV terms), 1366 (bvd_admit_mixed), 1707 (proof_jit), 1709 (aeu_kernel), plus egraph/sov_isa/bigint tests -- run the FULL corpus, do not spot-check, because these callers' KATs do not grep-match tc_conv directly. 3) RED-on-old: revert ONLY the tc_conv body edit, rebuild, run 1767 -> MUST exit non-99 ((x&y)+(x|y)==x+y accept fails because ccl alone returns 0); restore -> 99. 4) Soundness floor both ways: 1767's REFUTE tc_conv(lhs, lhs+1)==0 passes before AND after. 5) Seal/determinism: bash COMPILER/BOOT/build_iiis2.sh (seal-gated) -> iiis-0==iiis-2 unmoved; typecheck.iii is STDLIB and compiler-unreferenced (cg_r3 does not import it), so NO golden reseal expected -- let the seal gate confirm zero drift. 6) TIMING -- ELEVATE from 'sanity' to a hard gate: arm B of 1214 now drives ~480 width-64 bit-blast+SAT solves per run, MANY containing variable*variable 64-bit multiplier miters (classically SAT-hard); the conflict budget bounds each but the aggregate could be minutes or worse. MEASURE the wall-clock of 1214 (and 1366/1444) explicitly and compare to baseline; treat a regression to minutes as a blocker, not a flag. Without the budget edit, a hang is likely, not merely possible.
- NEW KAT: YES but with CORRECTED id: add STDLIB/corpus/1767_tc_conv_bv_sat_fallback.iii (NOT 1765 -- 1765_weave_oneeval.iii and 1766_weave_invent.iii already exist; verified true max corpus id is 1766). Oracle 99 = all asserts pass. Externs from typecheck.iii: tc_reset, tc_conv, tc_var, tc_bvlit_u64, tc_bvadd/bvand/bvor/bvxor/bvmul/bvsub/bvshl/bvlshr/bvudiv, tc_eval, tc_eval_err, tc_zero, tc_succ (all verified @export). Body: tc_reset(); x=tc_var(0); y=tc_var(1). (ACCEPT, RED-on-old/GREEN-after) lhs=tc_bvadd(tc_bvand(x,y),tc_bvor(x,y)); rhs=tc_bvadd(x,y); assert tc_conv(lhs,rhs)==1 (return 1 on fail) and tc_conv(rhs,lhs)==1 (symmetry). (REFUTE, soundness floor, GREEN-both) bad=tc_bvadd(rhs,tc_bvlit_u64(1)); assert tc_conv(lhs,bad)==0 (return 2). (EXTENSIONAL cross-check) several random (px,py): tc_eval(lhs,px,py)==tc_eval(rhs,px,py), and lhs vs bad separate at >=1 point. (REGRESSION) reflexive tc_conv(lhs,lhs)==1; NON-BV untouched: tc_conv(tc_zero(),tc_zero())==1 and tc_conv(tc_zero(),tc_succ(tc_zero()))==0. (SCOPE/incompleteness) a udiv identity (root TC_BVUDIV) is REFUSED by the whitelist so tc_conv stays 0 -- assert ==0, documents the boundary. (WIDTH) (x<<1) vs (x+x) ==1 (also RED-on-old). return 99.
- RISK: HIGH-CARE; the spec's own risk section is sound but UNDER-COUNTS the blast radius. (1) Trusted-base expansion: one bug in sat.iii's UNSAT path => bb_equal=1 on a false equality => false tc_conv accept => transport along Id BV a b => prove Empty => collapse. The whitelist controls WHEN the solver runs and fails closed on poison; it does NOT remove the solver-correctness dependency. Toward a minimal base (out of scope): a DRAT/resolution UNSAT certificate checked by a tiny verifier. (2) Termination: sat_solve has no budget (verified) -> hang on hard multiplier miters; the budget edit is essential, not optional. (3) Re-entrancy: VERIFIED currently-safe, not merely assumed -- bv_dispose.iii is BOTH a tc_conv caller (bvd_admit Tier-2, line 59) AND a bb user (bvd_admit_mixed, bb_equal line 238), but they live in SEPARATE @export functions: bvd_admit_mixed consumes a caller-built bb DAG and runs only tc_eval (bb-neutral) then bb_equal, with NO internal tc_conv, so the new tc_conv->bb_reset never clobbers a live session. Document the no-interleave contract since the surface now exists. (4) Var-as-BV: tc_to_bb maps TC_VAR k -> bb_var(k) regardless of context type; sound because (a) universal-over-2^64 equality implies equality on any narrower interpretation, and (b) in any well-typed reachable proof every reached var leaf is BV-typed (tc_bv_op2_ok) -- ill-typed direct @export calls cannot build a well-typed proof of False (inferred TYPES are never BV-word-headed: a bvadd cannot be pushed as a context type since tc_sort_level(tc_bv())=NOSORT). (5) BLAST RADIUS CORRECTION -- the spec's 'audit shows only 1214 arm B needs the companion edit' is FALSE. Corpus-wide negative BV tc_conv assertions: 1214 arm B (defensive), 1214 arm C (safe: sep==1 guard precludes a bb-UNSAT), 1363 (safe: udiv-rooted negatives short-circuit the fallback, the lone lshr-vs-lshr negative is genuinely unequal), and 1355 line 59 (DETERMINISTIC RED -- missed). PLUS transitive risk via 10 internal tc_conv callers (bigint, sov_isa, proof_jit, aeu_kernel, egraph, ccl, bv_dispose, duration_cert, sov_pipeline) whose KATs (notably 1444 bvd_admit -- though its bv_ring Tier-1 veto likely masks the change -- 1366, 1707, 1709) do not grep-match tc_conv and must be cleared by a full corpus run. Mitigation: the full-corpus gate in verification, plus the 1355 edit in the same commit.
- DEPENDS: Sequence LAST, after the cg_r3 non-pow2-mul + sov_isa shift-combine seal-moving batch and the byte-safe Phase-1 routing (independent in code -- touches only typecheck.iii/1214/1355/1767/sat.iii, no cg_r3 golden movement -- but ordered last so the gated-build/reseal rhythm is exercised on low-risk changes first). HARD internal ordering: the sat.iii conflict-budget edit, the 1214 arm-B edit, AND the newly-required 1355 line-59 edit must ALL land in the SAME commit as the tc_conv body edit -- otherwise 1355 reds deterministically and the kernel can hang. The ims_synth->ag_cycle / invent-as-4th-proposer cash-ins come after and consume this stronger convertibility.

### Close the self-improvement loop: invention engine as an ag_cycle proposer (gap-analysis §2 #6 / §3 #3).  [feasible=yes-with-care]
Verified against source: the mechanism holds. ag_invent_scan() wires a fourth de-novo INVENTION proposer into ag_cycle's gather block, running the EXISTING live pipeline -- cga_bv_discover (forcefield/cg_autocatalyst.iii:203, width-faithful count-mask sieve) populates the certified registry; ims_synth(3) (numera/isa_macro_synth.iii:48) bisim-gates (refute-by-default) then drives sov_isa_adopt_dream (numera/sov_isa.iii:592), which RE-PROVES every discovered (a,b) in the CIC kernel via tc_conv and registers mul(?0,cpow_a)->shl(?0,cnum_b) into the LIVE egraph rewrite set (eg_register_rule). Soundness never depends on the proposer (every rule independently kernel-re-proven). The +1 fold is EXACT (KAT 1742 proves ims_synth(3)==1 and ims_admitted_count()==1). Determinism holds because eg_init clears the rule table (egraph.iii:389/393), so no cross-cycle rule accumulation. eg_intern_symbol is create-on-first-use content-interning (egraph.iii:403-432), so adoption registers rules with no prior term-seeding and the newKat's later content-addressed terms match. Golden seal is unmoved (autogenesis is a STDLIB-archive organ at build_stdlib.sh:1151, NOT in the compiler link closure). THREE corrections folded in: (1) the chosen KAT number 1765 is already taken (use the live max+1, 1767 at audit time); (2) the risk-gate canary set must also include 1409 and 1732; (3) firm the seal step to let the drift-driven gate decide rather than asserting a priori.
- `STDLIB/iii/sanctus/autogenesis.iii` @ extern block, insert after line 50 (after the bs2_lead extern); line refs VERIFIED accurate: Add 6 externs (all signatures VERIFIED against source): `eg_init() -> i32 from "egraph.iii"` (egraph.iii:376), `cga_bv_reset() -> u64 from "cg_autocatalyst.iii"` (cg_autocatalyst.iii:139), `cga_bv_discover(max_exp: u32, budget: u32) -> u32 from "cg_autocatalyst.iii"` (:203), `ims_reset() -> i32 from "isa_macro_synth.iii"` (:27), `ims_synth(a: u32) -> u8 from "isa_macro_synth.iii"` (:48), `ims_admitted_count() -> u32 from "isa_macro_synth.iii"` (:45). No link cycle: only autogenesis_cli.iii imports autogenesis; none of egraph/cg_autocatalyst/isa_macro_synth do (grep-confirmed). egraph + sov_isa are already in autogenesis's transitive closure via nous/refactor_propose + numera/optimize_self, so no new ARCHIVE nodes -- only new direct edges (carto-gate residual, see risk).
- `STDLIB/iii/sanctus/autogenesis.iii` @ const block, insert after line 64 (after AG_PROVISIONAL); line ref VERIFIED: Add `const AG_INVENT_MAXEXP : u32 = 8u32` (matches the proven 1748/1370 cga_bv_discover(8,1) sweep; covers the (5,5) diagonal the newKat payoff needs), `const AG_INVENT_BUDGET : u32 = 1u32` (>=1 = attempt), `const AG_INVENT_HOTOP : u32 = 3u32` (KAT 1742 proves ims_synth(3)==1).
- `STDLIB/iii/sanctus/autogenesis.iii` @ new fn ag_invent_scan(), insert after ag_validate() closing brace (line 159), before ag_cycle() comment (161); line refs VERIFIED: `fn ag_invent_scan() -> u64 { eg_init() cga_bv_reset() cga_bv_discover(AG_INVENT_MAXEXP, AG_INVENT_BUDGET) ims_reset() if ims_synth(AG_INVENT_HOTOP) != 1u8 { return 0u64 } return ims_admitted_count() as u64 }` -- DISCOVER (kernel count-mask sieve populates cga_bv registry) then PROVE+ADOPT (ims bisim gate, then sov_isa_adopt_dream walks cga_bv_count/get_a/get_b and tc_conv-re-proves+eg_register_rule's each (a,b)). Returns exactly 1 on the proven hot op (ims_admitted_count==1 per 1742), 0 on refute. VERIFIED: ims_synth gates on bisim_is_obsolete, not on the adopt count, so the +1 is independent of registry size; the registered rules persist (eg_init not re-called later in the cycle). W13 OK (no new locals).
- `STDLIB/iii/sanctus/autogenesis.iii` @ fn ag_cycle(), insert one line between lf_forge() (line 173) and `let slot : u32 = vbd_open(cap)` (line 174); line refs VERIFIED: `AG_NCAND = AG_NCAND + ag_invent_scan()` -- folds the admitted count into the candidate total BEFORE ag_state_root(BEFORE)+ag_stage+ag_validate (so the +1 is staged into vbd block 0 and reflected in AFTER root + ledger) and AFTER os_apply_in_model()+lf_forge() return (so the in-scan eg_init clobbers no live egraph state they relied on -- VERIFIED nothing after this point reads the egraph; rp_scan already eg_init's mid-cycle at refactor_propose.iii:82). The +1 changes ag_risk() by exactly +1 (the box state root is unchanged by runtime rule registration, so the BEFORE-byte term is unperturbed).
- `STDLIB/corpus/1767_ag_invent_proposer.iii` @ new corpus KAT file -- NUMBER CORRECTED from 1765 to the live max+1 (1767 at audit time): CORRECTION: the spec's 1765 collides with the already-registered 1765_weave_oneeval AND 1766_weave_invent (run_corpus.sh:1417/1419; current corpus max = 1766). Name the file with the true max+1 (1767 here; the engineer MUST recompute max+1 at apply-time -- this OneDrive tree is being actively appended, e.g. 1765/1766 landed mid-audit). Content per newKat field.
- `STDLIB/scripts/run_corpus.sh` @ EXPECTED table; append after the true last entry (after [1766_weave_invent]=99 at line 1419), NOT 'after 1748': Add `[1767_ag_invent_proposer]=99` (number = max+1, must match the corpus file). Order is immaterial for the bash assoc array, but place at the genuine tail to avoid the impression that 1748 is the last entry. _rc_snap.sh needs NO mirror entry (VERIFIED: it lists neither 1742/1748/1406).
- VERIFY: 1) Build: `bash STDLIB/scripts/build_stdlib.sh`; assert the summary `FAIL = 0`. PRE-CHECK on a CLEAN baseline FIRST -- 1765_weave_oneeval/1766_weave_invent are in-flight WIP in this tree; do not confound a pre-existing FAIL with this change. Confirm build_stdlib's carto architectural-invariant gate stays green (the new autogenesis->{egraph,cg_autocatalyst,isa_macro_synth} edges; add a substring allowlist entry only if a ring-layer R-1->R0 invariant flags them). 2) SEAL (CORRECTED -- do NOT skip): the golden is structurally unmoved (autogenesis at build_stdlib.sh:1151 is a STDLIB-archive organ, NOT the compiler closure; reseal triggers are .def/forge_check/trusted-base/iiis-2==iiis-3 fixpoint, none touched). But per the drift-driven reseal discipline, RUN the seal-gated build (build_iiisN.sh) and let the gate CONFIRM no reseal/no drift -- do not assert 'golden unmoved' a priori. 3) Corpus: `bash STDLIB/scripts/run_corpus.sh`; assert exit 99 for the FULL risk-gate canary set -- 1406_autogenesis_cycle (ag_valid()==99 + commit), 1409_autogenesis_charter (arm 4: authorized commit ag_commit(cap_auto,1)==0 needs AG_VALID==99), 1732_autogenesis_consume (arm 6 ag_valid()==99, arm 7 ag_risk_acceptable()==1, arm 12 commit==0), 1748_autonomous_invention_demo (two committable cycles, ledgers differ), plus 1370/1742/1353 (pipeline) and the new 1767. (1407/1408 assert only ag_cycle==0/rollback -- risk-gate-independent, expected green either way.) 4) If any redden, baseline ag_risk()==64 had zero margin under the +1: documented remedy = minimal AG_RISK_THRESHOLD 64->65 + reverify (or reduce AG_INVENT scope). The cross is LOUD, never silent.
- NEW KAT: STDLIB/corpus/1767_ag_invent_proposer.iii (module corpus_1767_ag_invent_proposer) -- number = live max+1 at apply-time. Externs: cap_env_init/cap_attenuate (capability.iii); ag_cycle/ag_inflight/ag_ncand/ag_valid/ag_commit (autogenesis.iii); eg_init (egraph.iii); cl_init (cost_lattice.iii); si_leaf/si_node2/si_slot/si_leaf_pow/si_slot_cnum/sov_isa_rules/sov_isa_descend (sov_isa.iii). `const AG_E_UNAUTH:i32=-5i32`; `var OUT:[u32;64]`; `var OUTN:[u32;1]`. 99-ORACLE arms (VERIFIED sound against the live source): (1) env=cap_env_init(); cap_auto=cap_attenuate(env,0x80000,0); cap_commit=cap_attenuate(env,0x180000,0); ag_cycle(cap_auto)==0; ag_inflight()==1; ag_ncand()!=0; ag_valid()==99 [proves the +1 fold kept ag_risk<=64]. (2) THE PAYOFF (independent oracle the de-novo rule went LIVE via ag_cycle, NOT a side call): cl_init() [do NOT eg_init here -- that would wipe the rules registered in arm 1]; build mul(x,cpow_5) via si_node2(31u8, si_leaf(12u8), si_leaf_pow(5u8)); sov_isa_descend(...)==0; assert OUTN[0]==3 AND OUT[0]==si_slot(32u8) [head shl] AND OUT[2]==si_slot_cnum(5u8) -- a reduction NO hand rule provides, present only because ag_invent_scan discovered+kernel-adopted (5,5). VERIFIED: eg_intern_symbol create-on-first-use makes seeding unnecessary; the rule survives to here (eg_init not re-called inside ag_cycle after ag_invent_scan). (3) APPRENTICE GATE BITES: ag_commit(cap_auto,0u64)==AG_E_UNAUTH (cap lacks AUTOGENESIS_COMMIT, sig=0) AND ag_inflight()==1 (refused commit leaves the cycle open -- VERIFIED ag_commit returns before clearing inflight). (4) AUTHORIZED commit: ag_commit(cap_commit,0u64)==0 AND ag_inflight()==0. (5) FALSIFIER: fresh eg_init() [VERIFIED clears the rule table at egraph.iii:389/393, wiping the invention rules] + cl_init(); sov_isa_rules()==0 [seeds ONLY the hand rules: mul-c2, not mul-cpow_5]; descend mul(x,cpow_5); assert OUT[0]==si_slot(31u8) [head STILL mul] -- proves the arm-2 reduction came from the proposer, not the hand set. return 99. (Mirrors 1370's payoff/falsifier + 1406's cap-gating; the falsifier's reliance on eg_init clearing rules is VERIFIED, a point 1370 does not itself exercise.)
- RISK: PRIMARY (soundness-adjacent, gate-caught): ag_risk()=AG_NCAND+(BEFORE_byte&0x0F), gated vs AG_RISK_THRESHOLD=64; 1406 requires ag_valid()==99+commit, so today ag_risk()<=64. The fold adds EXACTLY +1 (VERIFIED: ims_admitted_count==1 for hot op 3 per KAT 1742; the box BEFORE-root is unchanged by runtime rule registration so the &0x0F term is unperturbed). At zero margin (current==64) the cross reddens 1406 AND -- CORRECTED, the spec under-listed -- 1409 (authorized-commit arm) AND 1732 (ag_valid/risk_acceptable arms) AND cascades 1748 (refused commit -> AG_INFLIGHT stays 1 -> second ag_cycle returns AG_E_INFLIGHT -> identical ledgers -> arm 10 fails). MITIGATION: the canary set is mandatory; a cross is LOUD; one-line AG_RISK_THRESHOLD 64->65 + reverify. A read-only design cannot measure baseline ag_risk -- the build is the measurement. SEAL (CORRECTED): golden structurally unmoved (autogenesis NOT in compiler closure, build_stdlib.sh:1151 vs the additive-consumer note at :1017/:1025), but RUN the drift-driven seal gate to CONFIRM, do not assert. DETERMINISM: holds -- eg_init clears RULE_LIVE/RULE_USED (egraph.iii:389/393) so the ~9 diagonal rules re-register identically each cycle (no accumulation); exhaustive enumeration + fixed hot op -> +1 every cycle. CLOBBER: ag_invent_scan's eg_init resets the egraph as the LAST gather step (after os_apply_in_model+lf_forge return); VERIFIED nothing after it (vbd_open/state_root/stage/tg_root/validate/ledger_extend) reads the egraph; rp_scan already eg_init's mid-cycle. 1748's pre-cycle sov_isa_adopt_shift_dream rules are wiped by the in-cycle eg_init, but 1748 reads none of them after line 51 -- no assertion broken. RESIDUAL (build-caught): (a) new direct edges autogenesis->{egraph,cg_autocatalyst,isa_macro_synth} face the carto architectural-invariant gate -- acyclic (autogenesis is a sink except autogenesis_cli; the three targets do not import it) so no cycle, but confirm green / allowlist if a ring invariant flags R-1->R0; (b) 1765_weave_oneeval/1766_weave_invent are in-flight WIP -- assert FAIL=0 on a clean baseline so their state is not confounded. GATING: stays operator/cap-gated -- ag_cycle is CAP_RIGHT_AUTOGENESIS-gated, ag_commit apprentice-gated + re-checks risk; only the volatile, per-rule-kernel-proven runtime optimizer set is touched (the bounded daemon_dream effect), and a larger invention count makes the commit gate bite HARDER.
- DEPENDS: Independently buildable -- the adoption pipeline (cga_bv_discover, ims_synth, sov_isa_adopt_dream) is already live + corpus-green (1370/1742/1748), so no hard build dependency. ORDERING (gap-analysis §6 Phase 4): land AFTER the compiler-touching cash-ins so it consumes the hardened base -- #1 (cg_r3 non-pow2 mul), #3 (sov_isa shift-combine generalization, which MOVES the golden seal), #4 (bb_equal->tc_conv, which strengthens the convertibility this proposer's re-proofs ride on). It MUST come AFTER any seal-moving cash-in's reseal so its stdlib-only rebuild is not confounded with a golden delta. Pairs forward with §3 #3 (gil_forge as a richer proposer) and §2 #7 (gil_cycle_validate cost sieve), which extend this same wiring.

### Clean enhancements bundle: (1) interval_lattice L6_NULL bottom; (2) fe25519 *38 fold via gilr_proves; (3) persist admitted proof terms into theorem_commons.  [feasible=yes-with-care]
Verified against source: all cited files/functions/line-ranges exist as described and the three verdicts hold. (1) FEASIBLE/value-preserving: il_meet's only live consumer is kleene_fixpoint.kf_step (grep-confirmed), which always passes state lo=0 so the meet never yields bottom; the optimizer predicates use il_add/il_mul, never il_meet; appending an IL_BOT classifier leaves IL_LO/IL_HI values untouched. Two correctness refinements: place IL_BOT[0]=0 at the TOP of il_add/il_mul (before their overflow early-returns) so the flag is meaningful on every path; and hoisting il_max/il_min to locals is an equally value-preserving, spill-safer alternative to the spec's recompute-inline. (2) INFEASIBLE/DECLINE confirmed: gilr_proves runs at bb's hard 64-bit width (bb_reset clamps ww>64->64, at ~:195 not the cited :155); _fz_fold38 is a 256-bit fold over a 512-bit product, so the 64-bit prover cannot certify it; no edit. (3) Original pt_serialize gap is wrong (proof is a tc-arena id per tc_check(proof,goal)); redirect to the already-externed tc_serialize is correct AND round-trips proofs (tc_ser_walk/tc_ds_walk are symmetric 4-child walks over the full tag set). One missed edge: tc_serialize returns 0xFFFF...FFFF on >cap overflow, which must be guarded before storing the length.
- `STDLIB/iii/numera/interval_lattice.iii` @ module vars after IL_LO/IL_HI (:26-27); new accessor near il_lo/il_hi (:32-33); il_meet (:42-46); il_join (:36-40); il_add (:60-63); il_mul (:64-68): PURE-ADD bottom classifier, VERIFIED value-preserving. Add: const IL_L6_NULL:u32=4u32 ; const IL_L6_ALL:u32=5u32 ; var IL_BOT:[u8;1]. In il_meet, AFTER the existing IL_LO[0]=il_max(l1,l2) and IL_HI[0]=il_min(h1,h2) stores, append the classifier. CORRECTION/option: prefer hoisting once -- let mx:u32=il_max(l1,l2); let mn:u32=il_min(h1,h2); store IL_LO=mx,IL_HI=mn; then if mx>mn {IL_BOT[0u64]=1u8} else {IL_BOT[0u64]=0u8} -- this is equally value-preserving and avoids relying on multi-use param spill (the documented historical trap; iiis-2 resolves it, but hoisting is defensive). The spec's recompute-inline also works. Add accessor: fn il_is_bottom()->u32 @export { if IL_BOT[0u64]==1u8 {return IL_L6_NULL} return IL_L6_ALL }. CORRECTION: in il_add and il_mul, put IL_BOT[0u64]=0u8 at the TOP of the function (before the overflow->TOP early-return), NOT only on the normal-return path, so the flag correctly reflects the LAST op on the overflow path too; il_join has no early return so any position is fine. il_meet is il_meet's ONLY live consumer kf_step (lo always 0) so the flag never reads bottom live -- this is sound HARDENING, not a live-bug fix.
- `STDLIB/iii/numera/range_check.iii` @ rc_proven_safe (:53-56): OPTIONAL fail-closed input guard (ISLAND/unreachable-input hardening, honestly flagged). At top of rc_proven_safe add `if hi1 < lo1 { return 0u32 }` and `if hi2 < lo2 { return 0u32 }`. Signature is (lo1,hi1,lo2,hi2,op,bound) -- params match. Value-preserving: every range_check_kat vector has lo<=hi so the guard never fires. Keep the u32{0,1} contract (NOT L6_NULL=4) that the sopt KATs pin. Note: no live caller passes hi<lo, so this defends an unreachable state.
- `STDLIB/iii/numera/branch_elim.iii` @ be_eliminable (:47-50): OPTIONAL fail-closed input guard. At top of be_eliminable add `if hi < lo { return 0u32 }`. Signature (lo,hi,c) matches. Byte/value-identical for valid ranges (all be_verdict/be_eliminable KAT vectors are valid). Same u32{0,1} contract + unreachable-input caveat as range_check.
- `STDLIB/iii/numera/value_range_prover.iii` @ vrp_abstract_max (:48-51) / vrp_no_overflow (:69-72): NO edit needed (confirmed): vrp_abstract_max calls il_mul(0,cn,0,ca) and vrp_no_overflow calls rc_overflows(0,cn,0,ca,...) -- operand intervals are intrinsically valid (lo=0), so a bottom can never arise. Listed only to record the audit.
- `STDLIB/iii/numera/fe25519.iii + STDLIB/iii/numera/invent.iii` @ _fz_fold38/fz_mul (fe25519 :109-165) ; gilr_proves (invent :291-303) ; bb_reset clamp (bv_bits :195, NOT the spec's cited :155): DECLINE (item 2), VERIFIED. No edit. gilr_proves bit-blasts at GILR_W=64 and bb_reset hard-clamps ww>64 to 64 (line ~195). _fz_fold38 implements 2^256==38 (mod 2^255-19) as a multi-limb fold over the 512-bit FZ_T[16] product -- k=256/255 exceeds the 64-bit blaster AND the host 1u64<<255 (x86 masks the count to 6 bits -> 1u64<<63). gilr_proves(255,19,38) models a 64-bit relation that is NOT the 256-bit fold and returns 0u8 (analysis: bb_shr(x,255) zero-fills, hi=0, so lhs=top-bit-of-x != rhs=0 for a free x). The hardcoded fold stays pinned by corpus 388/973/992 (and the fe25519 source comment forbids absorbing it into Montgomery). A correct certificate needs a >=256-bit blaster or the one-line number-theory proof (2^255==19 => 2^256==38) -- neither is lean.
- `STDLIB/iii/numera/theorem_commons.iii` @ module vars near TCOM_HASH (:39-41); tcom_admit new-admission path between copy loop (:80) and increment (:81); tc_serialize already externed (:29); tcom_receive (:191): Item 3 redirected to a PURE-ADD using tc_serialize, VERIFIED correct (proof is a tc-arena id, and tc_ser_walk(:2176)/tc_ds_walk(:2275) are symmetric 4-child walks so proofs round-trip). Add var TCOM_PROOF:[u8;1048576] (1024 slots x 1024B) and var TCOM_PROOF_LEN:[u64;1024]. In tcom_admit, after the `while i<32 {dp[i]=sp[i]}` copy and before TCOM_COUNT increment (so TCOM_COUNT still equals the slot being written, matching dp's TCOM_COUNT*32 idiom): let pl:u64 = tc_serialize(proof, ((&TCOM_PROOF as u64)+(TCOM_COUNT as u64)*1024u64) as *u8, 1024u64). CORRECTION (missed edge): tc_serialize returns 0xFFFFFFFFFFFFFFFF when the proof exceeds the 1024B cap (TC_SER_ERR), so guard it: if pl == 0xFFFFFFFFFFFFFFFFu64 { pl = 0u64 } before TCOM_PROOF_LEN[TCOM_COUNT as u64]=pl, so an over-cap proof reads as absent (len 0) and is never replayed truncated. Admit DECISION is unchanged (still gated solely by tc_check at :73; duplicates short-circuit at :75 before the copy, so only the first proof persists). Add accessor fn tcom_proof_bytes(slot:u32,out:u64)->i64 returning the stored length (or -1 if slot>=TCOM_COUNT or len==0), so tcom_receive can re-ship admitted proofs (otherwise the buffer is a dead island).
- VERIFY: Items 1+3 are stdlib-only (no COMPILER/BOOT, no golden reseal; the compiler does not reference interval_lattice or theorem_commons). Build: bash STDLIB/scripts/build_stdlib.sh ; assert it prints 'FAIL = 0' (per feedback_buildstdlib_fail_masks_stale_lib). CORRECTION to the spec's corpus path: the cited STDLIB oracles live under STDLIB/corpus/ and are run by STDLIB/scripts/run_corpus.sh (NOT STDLIB/stage1_corpus/run_corpus.sh -- that is the COMPILER stage1 corpus). Run: bash STDLIB/scripts/run_corpus.sh ; confirm these stay 99: 1265_interval_lattice, 1542_interval_lattice_overflow_sound, range_check/branch_elim KATs, 1752_logic6, 1208_theorem_commons, 1209_theorem_commons_distinct, 636_proof_term, plus the untouched fe25519 set 388_fe25519_ed/973_fe25519_canonical_decode/992_bench_fe25519_mul/193-197/1493_fe25519_sqr_differential (item 2 declined). Add the new KAT(s) below and confirm =99. Run the determinism gate (bash COMPILER/BOOT/build_iiis2.sh, seal-gated) per feedback_determinism to CONFIRM (let the gate decide) the stdlib changes do not drift the compiler seal -- none expected since neither module is compiler-referenced; no reseal. For item 2, optionally add the negative KAT to PIN that the 64-bit prover is inapplicable.
- NEW KAT: PRIMARY (item 1): fn il_bottom_kat()->u64 @export in interval_lattice.iii + corpus 17xx_interval_bottom.iii (extern il_meet/il_lo/il_hi/il_is_bottom + rc_proven_safe + be_eliminable). Arms: il_meet(5,8,0,2)=>empty (max 5>min 2) so il_is_bottom()==4u32 (L6_NULL); il_meet(2,5,3,8)=>[3,5] valid so il_is_bottom()==5u32 (L6_ALL) AND il_lo()==3 && il_hi()==5 (value-preservation on the non-bottom path); rc_proven_safe(5,2,0,5,RC_ADD,255)==0 while rc_proven_safe(0,5,0,5,RC_ADD,255)==1; be_eliminable(hi<lo)==0 while a valid eliminable range==1; ALSO add an il_add-overflow arm proving the TOP path sets IL_BOT=0 (il_add(0,0xFFFFFFFF,0,1) then il_is_bottom()==5u32) to pin the top-of-function IL_BOT placement; return 99. SECONDARY (item 2, honest negative): fn gilr_inapplicable_kat() pinning gilr_proves(255u64,19u64,38u64)==0u8 -- confirm this value empirically once at build time (analysis says 0, but it is an out-of-range-width call); documents the decline. TERTIARY (item 3): fn tcom_proof_roundtrip_kat() -- call tcom_init() FIRST (so the receive goes through the full gate, not the duplicate short-circuit at :75), tcom_admit a true (proof,goal), assert tcom_proof_bytes(slot,...) returns len>0, then on a fresh tcom_init() call tcom_receive(goal_bytes, tc_serialize(goal), stored proof bytes, len) and assert TCOM_OK (proof persisted + replayable via the symmetric serializer); admit count for a FALSE proof stays unchanged (decision still tc_check-gated). Also add an over-cap arm: a proof serializing >1024B must store len 0 (tcom_proof_bytes==-1), proving the sentinel guard.
- RISK: Item 1: ISLAND/cosmetic -- il_is_bottom and the three input guards defend a state no live caller produces (kf_step's lo is always 0; the predicates never receive hi<lo). Sound fail-closed HARDENING, not a reachable-bug fix (per feedback_workflow_finding_prefix_check) -- the III user may judge unreachable-input hardening as cosmetic, so the guards are marked Optional. Mitigation: keep IL_LO/IL_HI values identical (hoist-to-locals is the spill-safe way; recompute-inline also works under iiis-2) and keep the predicates' return u32{0,1} (never L6_NULL=4) so sopt_analysis_kat/sopt_refined_kat/sopt_branch_crossval_kat stay green; place IL_BOT=0 at the TOP of il_add/il_mul so the overflow early-return does not leave a stale bottom flag. Item 2: wiring gilr_proves(255,19,38) would inject a FALSE refutation (returns 0) mistakable for 'fold unproven' -- explicitly DECLINE and optionally pin the negative KAT (verify the ==0u8 value empirically since it is an out-of-width call). Item 3: the real risk is a dead 1MiB buffer (BSS +1MiB is acceptable per feedback_no_practicality) -- mitigate by shipping tcom_proof_bytes so tcom_receive can consume it; AND guard the tc_serialize 0xFFFF...FFFF overflow sentinel before storing the length, or an over-cap proof persists a truncated/garbage slot reported as a huge length. Admit soundness is untouched (tc_check at :73 remains the sole gate; duplicates short-circuit before the new write).
- DEPENDS: All three items are independent (different files, no shared symbols; verified). Within item 1 the consumer-side guards (range_check/branch_elim) do NOT depend on the il_is_bottom producer classifier -- they guard their own inputs -- so the classifier and the predicate guards can land separately. No ordering dependency on any other cash-in; items 1 and 3 are byte-safe stdlib (Phase-1 class, no golden seal, no reseal), so they precede any cg_r3/seal-moving work rather than following it. Item 2 is a no-op decline (no dependency in either direction).
