=== ROUTE-S BLAST-RADIUS ENUMERATION (IMPL-PLAN Step B.0) — 2026-05-26 ===
Empirically grounded (nm + grep), not assumed. Every dependency classified affected/unaffected.

[1] COMPILER BINARY (iiis-2/3): AFFECTED — bytes change; behaviour UNCHANGED.
    nm COMPILED/iiis-2.exe shows xii_canonicalise / xii_rewrite_apply_one / xii_term_make_fusion2
    as DEFINED (T) symbols IN the binary. xii_term.h is a "C-side bridge to omnia/xii_term.iii" —
    extern DECLARATIONS only, no C impl; cg_r3_xii.c resolves them from libiii_native.a's
    omnia_xii_term.iii.o. So editing omnia/xii_term.iii (B.1) changes bytes linked into iiis-2.
    => RESEAL REQUIRED (build_iiis1/2/3 + reseal COMPILER/BOOT/iiis-1.mhash + verify fixpoint
    iiis-2 == iiis-3 at the new hash). This is the "changed" branch of B.6's drift decision —
    PREDICTED here, to be PROVEN by B.6's hash check.
    BUT: @lattice is DORMANT — grep across STDLIB/ + COMPILER/ finds @lattice only in comments /
    detection logic (cg_r3_xii.c:189 "ZERO @lattice-annotated functions"; no live use). The
    compiler never CALLS xii_canonicalise/make_fusion2 during compilation, so its OUTPUT is
    byte-identical before/after route S => the non-XII corpus + the stdlib iiis-2 builds are
    UNAFFECTED. Reseal moves the binary hash; it does not change what the binary produces.

[2] NO C-TWIN TO SYNC: xii_term.h / xii_rewrite.h / xii_canon.h are declaration bridges; the
    implementations are the .iii modules. Route S edits ONLY omnia/{xii_term,xii_rewrite,
    xii_rule_patterns}.iii. No BOOT *.c implementation to keep in lock-step.

[3] CANONICALISE RESULTS (most XII corpus 280-372): UNAFFECTED. Route S makes right-association
    STRUCTURAL (born right-nested at the constructor) instead of via R001 (rewrite). The final
    normal form is the SAME right-nested form R001 produced; R042/R032 still sort the spine. So
    canonicalise(X) yields the identical NF => tests asserting canonicalise RESULTS do not change.

[4] RULE-STRUCTURE selftests: AFFECTED — these reference the pre-route-S rule set and must be
    re-derived in B.5 (they assert the 49-rule / R001-R004 / 35-non-join structure that route S
    changes to 45-rule / no-R001-R004 / 20-non-join):
      - omnia/xii_rule_patterns.iii (xrp_selftest, corpus 810): 49->45, first rid 1->5, table rows.
      - omnia/xii_rule_overlap.iii   (corpus 811): overlap counts referencing R001-R004.
      - omnia/xii_critpair_enum.iii  (corpus 812): enumerated-pair count.
      - omnia/xii_joinability.iii    (xjn_selftest, corpus 813): asserts R001xR008 non-join + count.
      - omnia/xii_termination.iii    (xtm_selftest, corpus 814): asserts R001-R004 are DEC_P.
      - omnia/xii_admission.iii       (corpus 815): rule-set admission references.
      - omnia/xii_discharge.iii       (xdc_selftest, corpus 825): anchors 35 / S=15 / R=20 -> 20 / S=0 / R=20.
    (Precise per-line edits enumerated at B.5 via grep "R00[1-4]\|49u\|35u" over these modules.)

[5] RULE-COUNT CONSUMERS auto-adjust: xii_rule_overlap / critpair_enum / joinability / termination
    / discharge read xrp_rule_count() dynamically (49->45 needs no code edit in them; only their
    selftest ASSERTIONS in [4] change).

[6] SEALED LATTICE / HORIZON / MANIFEST (COMPILED/xii_lattice.bin, *.mhash.golden,
    gen_xii_manifest.c): UNAFFECTED by the standard build. They are dormant (no @lattice exercises
    them) and are NOT regenerated by build_stdlib or build_iiis* (only build_xii.sh regenerates,
    which route S does NOT run). Their old-form content is inert. DO NOT run build_xii.sh during
    route S. (C.5 separately records the cert mhash into the manifest, a distinct controlled step.)

GATE: enumeration COMPLETE — no "unknown". Affected = {iiis chain (reseal), the [4] selftests}.
Unaffected = {compiler output, non-XII corpus, C-twins (none), lattice/Horizon (dormant)}.
The single empirical confirmer is B.6: iiis-2 hash WILL drift (route S is NOT seal-neutral); the
reseal+fixpoint is the expected path, with the non-XII corpus staying green proving behaviour-neutrality.
