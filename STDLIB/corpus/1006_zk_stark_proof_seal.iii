/* 1006_zk_stark_proof_seal.iii -- falsifier for zk_stark's real-proof content-address seal
 * (APOTHEOSIS C.3 item 5).  Proves a REAL STARK proof (not fake commitments): the seal is
 * deterministic across re-seals, and tampering the cp root OR a query changes the proof's
 * identity (the seal's negative arm -- a proof's commitments are bound to its identity).
 * 99 = pass. */
module corpus_1006

extern @abi(c-msvc-x64) fn zk_stark_proof_seal_kat() -> u64 from "zk_stark.iii"

fn main() -> u64 { return zk_stark_proof_seal_kat() }
