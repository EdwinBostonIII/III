/* 1038 — zkf_fp12_selftest: BLS12-381 Fp12 (pairing target field). Closes a gating gap (selftest existed, ungated). */
module corpus_1038
extern @abi(c-msvc-x64) fn zkf_fp12_selftest() -> u64 from "zk_field.iii"
fn main() -> u64 { return zkf_fp12_selftest() }
