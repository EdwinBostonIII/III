/* 1258_logic_synth.iii -- verified logic synthesis: the source-to-silicon stage.
 * Drives numera::logic_synth::logic_synth_kat: 99 = synthesizes the 4-gate MAJORITY (a&b)|(c&(a^b)) proven
 * identical (truth table 232) to the 5-gate canonical anchor; the cheaper 2-gate XOR3 (150) is rejected
 * despite fewer gates; the chosen netlist is strictly smaller than the anchor (4<5). Software intent lowered
 * to the cheapest gate netlist proven to compute the exact same function. */
module corpus_1258

extern @abi(c-msvc-x64) fn logic_synth_kat() -> u64 from "logic_synth.iii"

fn main() -> u64 {
    let r : u64 = logic_synth_kat()
    if r != 99u64 { return r }
    return 99u64
}
