/* 1349_xii_cost_monotone.iii -- THE COST-MONOTONICITY GATE (harmony item 27).
 *
 * The XII termination argument (MPO weight, S9.3) promises every rewrite goes downhill-or-level
 * in summed K-cost; that promise was prose.  xcm_gate re-proves it mechanically across EVERY live
 * rule fired on its own joinability witness -- the exact sibling of the capability gate (1251).
 * 99 = the gate holds across all live rules AND the weight order has teeth (a fabricated
 * leaf->composite expansion strictly increases weight, so an uphill rewrite WOULD be caught). */
module corpus_1349

extern @abi(c-msvc-x64) fn xcm_selftest() -> u64 from "xii_cost_monotone.iii"

fn main() -> u64 {
    return xcm_selftest()
}
