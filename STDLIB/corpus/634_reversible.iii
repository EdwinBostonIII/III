/* 634 — reversible: LIFO transactional envelopes, capability-scoped, witnessed rollback (M9). */
module corpus_634
extern @abi(c-msvc-x64) fn rev_selftest() -> u64 from "reversible.iii"
fn main() -> u64 { return rev_selftest() }
