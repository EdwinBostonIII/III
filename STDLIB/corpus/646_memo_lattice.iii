/* 646 — memo_lattice: confidence-keyed memoization lattice with staleness. */
module corpus_646
extern @abi(c-msvc-x64) fn ml_selftest() -> u64 from "memo_lattice.iii"
fn main() -> u64 { return ml_selftest() }
