/* 649 — reversibility_audit: SMT-checked bijection audit + admission of reversible ops. */
module corpus_649
extern @abi(c-msvc-x64) fn rva_selftest() -> u64 from "reversibility_audit.iii"
fn main() -> u64 { return rva_selftest() }
