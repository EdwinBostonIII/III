/* 998_zk_air_merkle.iii -- C.3 general-STARK commitment layer: builds a valid 2-column
 * AIR LDE, Merkle-commits column 0 (D=32 leaves) via zk_air's leaf-array-parameterized
 * Merkle organ, then opens+verifies a leaf and proves a 1-bit-tampered leaf is REJECTED.
 * The query-opening machinery the general prover/verifier wires onto. 99 = pass. */
module corpus_998

extern @abi(c-msvc-x64) fn zk_air_merkle_selftest() -> u64 from "zk_air.iii"

fn main() -> u64 {
    return zk_air_merkle_selftest()
}
