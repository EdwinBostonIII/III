/* STDLIB/iii/aether/glossa.iii -- THE TONGUE (GENERATED by `iii-substrate glossa`;
 * edit NOTHING by hand -- regeneration + the parliament's chamber II are the only
 * lawful amendment road).  Each fn below IS a sealed word of the language: sema
 * binds the bare name at every call site (no extern needed), cg_r3 emits the call
 * to THIS seat, and a decl of the same name anywhere else is a LOUD sema error
 * (the shadow law).  Body = spelling 0, the sealed definiens, proven equal over
 * ALL 2^64 to its dual engine through the ladder (see cg_glossa_rules.iii).
 * Hexad: kind_essence.  Ring: R0.  NIH: pure integer forms, zero deps. */
module glossa

fn andn64(x: u64, y: u64) -> u64 @export {
    return (18446744073709551615u64 ^ x) & y
}
fn blsi64(x: u64) -> u64 @export {
    return x & (0u64 - x)
}
fn blsmsk64(x: u64) -> u64 @export {
    return x ^ (x - 1u64)
}
fn blsr64(x: u64) -> u64 @export {
    return x & (x - 1u64)
}
fn lzcnt64(x: u64) -> u64 @export {
    let s1 : u64 = x | (x >> 1u64)
    let s2 : u64 = s1 | (s1 >> 2u64)
    let s3 : u64 = s2 | (s2 >> 4u64)
    let s4 : u64 = s3 | (s3 >> 8u64)
    let s5 : u64 = s4 | (s4 >> 16u64)
    let s6 : u64 = s5 | (s5 >> 32u64)
    let b : u64 = s6 - ((s6 >> 1u64) & 6148914691236517205u64)
    let c : u64 = (b & 3689348814741910323u64) + ((b >> 2u64) & 3689348814741910323u64)
    let d : u64 = (c + (c >> 4u64)) & 1085102592571150095u64
    let e : u64 = d + (d >> 8u64)
    let g : u64 = e + (e >> 16u64)
    let h : u64 = g + (g >> 32u64)
    return 64u64 - (h & 127u64)
}
fn popcnt64(x: u64) -> u64 @export {
    let b : u64 = x - ((x >> 1u64) & 6148914691236517205u64)
    let c : u64 = (b & 3689348814741910323u64) + ((b >> 2u64) & 3689348814741910323u64)
    let d : u64 = (c + (c >> 4u64)) & 1085102592571150095u64
    let e : u64 = d + (d >> 8u64)
    let g : u64 = e + (e >> 16u64)
    let h : u64 = g + (g >> 32u64)
    return h & 127u64
}
fn tzcnt64(x: u64) -> u64 @export {
    let m : u64 = (x & (0u64 - x)) - 1u64
    let b : u64 = m - ((m >> 1u64) & 6148914691236517205u64)
    let c : u64 = (b & 3689348814741910323u64) + ((b >> 2u64) & 3689348814741910323u64)
    let d : u64 = (c + (c >> 4u64)) & 1085102592571150095u64
    let e : u64 = d + (d >> 8u64)
    let g : u64 = e + (e >> 16u64)
    let h : u64 = g + (g >> 32u64)
    return h & 127u64
}

fn glossa_nwords() -> u64 @export { return 7u64 }
fn glossa_set_id() -> u64 @export { return 8964422122728055925u64 }
