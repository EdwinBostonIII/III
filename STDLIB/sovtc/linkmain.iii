/* linkmain.iii -- module A.  Calls lib_answer (cross-module) + defines its OWN L_str_0 (collides if concatenated). */
module linkmain
extern @abi(c-msvc-x64) fn lib_answer() -> u64 from "linklib.iii"
fn main() -> u64 {
    let s : *u8 = "main" as *u8       /* L_str_0 in THIS object too */
    let a : u64 = lib_answer()        /* cross-module REL32 to a UND symbol */
    if s[0u64] == 109u8 { if a == 42u64 { return 99u64 } }
    return 1u64
}
